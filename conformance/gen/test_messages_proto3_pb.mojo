# Generated by protoc-gen-mojo from google/protobuf/test_messages_proto3.proto. Do not edit.

"""Generated protobuf messages from `google/protobuf/test_messages_proto3.proto`.

Regenerate with tools/protoc-gen-mojo; do not edit by hand.
"""

from proto import (
    WIRE_FIXED32,
    WIRE_FIXED64,
    WIRE_LEN,
    WIRE_VARINT,
    ProtoMessage,
    WireReader,
    WireWriter,
    zigzag_encode32,
    zigzag_encode64,
)
from proto import (
    ProtoJsonMessage,
    ProtoJsonReader,
    ProtoJsonWriter,
)
from any_pb import Any
from duration_pb import Duration
from field_mask_pb import FieldMask
from struct_pb import ListValue, Struct, Value
from timestamp_pb import Timestamp
from wrappers_pb import BoolValue, BytesValue, DoubleValue, FloatValue, Int32Value, Int64Value, StringValue, UInt32Value, UInt64Value


struct ForeignEnum:
    """Values of the `ForeignEnum` protobuf enum (fields carry `Int32`)."""

    comptime FOREIGN_FOO = 0
    """`FOREIGN_FOO` = 0."""
    comptime FOREIGN_BAR = 1
    """`FOREIGN_BAR` = 1."""
    comptime FOREIGN_BAZ = 2
    """`FOREIGN_BAZ` = 2."""


struct TestAllTypesProto3_NestedEnum:
    """Values of the `NestedEnum` protobuf enum (fields carry `Int32`)."""

    comptime FOO = 0
    """`FOO` = 0."""
    comptime BAR = 1
    """`BAR` = 1."""
    comptime BAZ = 2
    """`BAZ` = 2."""
    comptime NEG = -1
    """`NEG` = -1."""


struct TestAllTypesProto3_AliasedEnum:
    """Values of the `AliasedEnum` protobuf enum (fields carry `Int32`)."""

    comptime ALIAS_FOO = 0
    """`ALIAS_FOO` = 0."""
    comptime ALIAS_BAR = 1
    """`ALIAS_BAR` = 1."""
    comptime ALIAS_BAZ = 2
    """`ALIAS_BAZ` = 2."""
    comptime MOO = 2
    """`MOO` = 2."""
    comptime moo = 2
    """`moo` = 2."""
    comptime bAz = 2
    """`bAz` = 2."""


struct TestAllTypesProto3_NestedMessage(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `NestedMessage` protobuf message."""

    var a: Int32
    """Field `a` (number 1)."""
    var corecursive: List[TestAllTypesProto3]
    """Field `corecursive` (number 2, recursive: boxed as a 0-or-1 list)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.a = 0
        self.corecursive = List[TestAllTypesProto3]()
        self._unknown = List[Byte]()

    def __deinit__(deinit self):
        """Destroys the message (explicit: breaks the trait-synthesis cycle for recursive types)."""
        pass

    def __init__(out self, *, copy: Self):
        """Deep-copies another message.

        Args:
            copy: The message to copy from.
        """
        self.a = copy.a
        self.corecursive = copy.corecursive.copy()
        self._unknown = copy._unknown.copy()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.a != 0:
            writer.int32(1, self.a)
        if len(self.corecursive) != 0:
            var sub = WireWriter()
            self.corecursive[0].encode_to(sub)
            writer.len_prefixed(2, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.a = reader.int32_value()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: TestAllTypesProto3
                    if len(self.corecursive) != 0:
                        m = self.corecursive.pop()
                    else:
                        m = TestAllTypesProto3()
                    m.merge_from(sub)
                    self.corecursive.append(m^)
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.a != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("a", "a")
            writer.int32_value(self.a)
        if len(self.corecursive) != 0:
            writer.field("corecursive", "corecursive")
            writer.message_value(self.corecursive[0])
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "a" or field_name == "a":
                if seen_1:
                    raise Error("proto json: duplicate field a")
                seen_1 = True
                if reader.read_null():
                    self.a = 0
                else:
                    self.a = reader.int32_value()
            elif field_name == "corecursive" or field_name == "corecursive":
                if seen_2:
                    raise Error("proto json: duplicate field corecursive")
                seen_2 = True
                if reader.read_null():
                    self.corecursive = List[TestAllTypesProto3]()
                else:
                    self.corecursive = List[TestAllTypesProto3]()
                    self.corecursive.append(reader.message_value[TestAllTypesProto3]())
            else:
                reader.skip_unknown_value()


struct TestAllTypesProto3(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `TestAllTypesProto3` protobuf message."""

    var optional_int32: Int32
    """Field `optional_int32` (number 1)."""
    var optional_int64: Int64
    """Field `optional_int64` (number 2)."""
    var optional_uint32: UInt32
    """Field `optional_uint32` (number 3)."""
    var optional_uint64: UInt64
    """Field `optional_uint64` (number 4)."""
    var optional_sint32: Int32
    """Field `optional_sint32` (number 5)."""
    var optional_sint64: Int64
    """Field `optional_sint64` (number 6)."""
    var optional_fixed32: UInt32
    """Field `optional_fixed32` (number 7)."""
    var optional_fixed64: UInt64
    """Field `optional_fixed64` (number 8)."""
    var optional_sfixed32: Int32
    """Field `optional_sfixed32` (number 9)."""
    var optional_sfixed64: Int64
    """Field `optional_sfixed64` (number 10)."""
    var optional_float: Float32
    """Field `optional_float` (number 11)."""
    var optional_double: Float64
    """Field `optional_double` (number 12)."""
    var optional_bool: Bool
    """Field `optional_bool` (number 13)."""
    var optional_string: String
    """Field `optional_string` (number 14)."""
    var optional_bytes: List[Byte]
    """Field `optional_bytes` (number 15)."""
    var optional_nested_message: List[TestAllTypesProto3_NestedMessage]
    """Field `optional_nested_message` (number 18, recursive: boxed as a 0-or-1 list)."""
    var optional_foreign_message: Optional[ForeignMessage]
    """Field `optional_foreign_message` (number 19)."""
    var optional_nested_enum: Int32
    """Field `optional_nested_enum` (number 21)."""
    var optional_foreign_enum: Int32
    """Field `optional_foreign_enum` (number 22)."""
    var optional_aliased_enum: Int32
    """Field `optional_aliased_enum` (number 23)."""
    var optional_string_piece: String
    """Field `optional_string_piece` (number 24)."""
    var optional_cord: String
    """Field `optional_cord` (number 25)."""
    var recursive_message: List[TestAllTypesProto3]
    """Field `recursive_message` (number 27, recursive: boxed as a 0-or-1 list)."""
    var repeated_int32: List[Int32]
    """Field `repeated_int32` (number 31, repeated)."""
    var repeated_int64: List[Int64]
    """Field `repeated_int64` (number 32, repeated)."""
    var repeated_uint32: List[UInt32]
    """Field `repeated_uint32` (number 33, repeated)."""
    var repeated_uint64: List[UInt64]
    """Field `repeated_uint64` (number 34, repeated)."""
    var repeated_sint32: List[Int32]
    """Field `repeated_sint32` (number 35, repeated)."""
    var repeated_sint64: List[Int64]
    """Field `repeated_sint64` (number 36, repeated)."""
    var repeated_fixed32: List[UInt32]
    """Field `repeated_fixed32` (number 37, repeated)."""
    var repeated_fixed64: List[UInt64]
    """Field `repeated_fixed64` (number 38, repeated)."""
    var repeated_sfixed32: List[Int32]
    """Field `repeated_sfixed32` (number 39, repeated)."""
    var repeated_sfixed64: List[Int64]
    """Field `repeated_sfixed64` (number 40, repeated)."""
    var repeated_float: List[Float32]
    """Field `repeated_float` (number 41, repeated)."""
    var repeated_double: List[Float64]
    """Field `repeated_double` (number 42, repeated)."""
    var repeated_bool: List[Bool]
    """Field `repeated_bool` (number 43, repeated)."""
    var repeated_string: List[String]
    """Field `repeated_string` (number 44, repeated)."""
    var repeated_bytes: List[List[Byte]]
    """Field `repeated_bytes` (number 45, repeated)."""
    var repeated_nested_message: List[TestAllTypesProto3_NestedMessage]
    """Field `repeated_nested_message` (number 48, repeated)."""
    var repeated_foreign_message: List[ForeignMessage]
    """Field `repeated_foreign_message` (number 49, repeated)."""
    var repeated_nested_enum: List[Int32]
    """Field `repeated_nested_enum` (number 51, repeated)."""
    var repeated_foreign_enum: List[Int32]
    """Field `repeated_foreign_enum` (number 52, repeated)."""
    var repeated_string_piece: List[String]
    """Field `repeated_string_piece` (number 54, repeated)."""
    var repeated_cord: List[String]
    """Field `repeated_cord` (number 55, repeated)."""
    var packed_int32: List[Int32]
    """Field `packed_int32` (number 75, repeated)."""
    var packed_int64: List[Int64]
    """Field `packed_int64` (number 76, repeated)."""
    var packed_uint32: List[UInt32]
    """Field `packed_uint32` (number 77, repeated)."""
    var packed_uint64: List[UInt64]
    """Field `packed_uint64` (number 78, repeated)."""
    var packed_sint32: List[Int32]
    """Field `packed_sint32` (number 79, repeated)."""
    var packed_sint64: List[Int64]
    """Field `packed_sint64` (number 80, repeated)."""
    var packed_fixed32: List[UInt32]
    """Field `packed_fixed32` (number 81, repeated)."""
    var packed_fixed64: List[UInt64]
    """Field `packed_fixed64` (number 82, repeated)."""
    var packed_sfixed32: List[Int32]
    """Field `packed_sfixed32` (number 83, repeated)."""
    var packed_sfixed64: List[Int64]
    """Field `packed_sfixed64` (number 84, repeated)."""
    var packed_float: List[Float32]
    """Field `packed_float` (number 85, repeated)."""
    var packed_double: List[Float64]
    """Field `packed_double` (number 86, repeated)."""
    var packed_bool: List[Bool]
    """Field `packed_bool` (number 87, repeated)."""
    var packed_nested_enum: List[Int32]
    """Field `packed_nested_enum` (number 88, repeated)."""
    var unpacked_int32: List[Int32]
    """Field `unpacked_int32` (number 89, repeated)."""
    var unpacked_int64: List[Int64]
    """Field `unpacked_int64` (number 90, repeated)."""
    var unpacked_uint32: List[UInt32]
    """Field `unpacked_uint32` (number 91, repeated)."""
    var unpacked_uint64: List[UInt64]
    """Field `unpacked_uint64` (number 92, repeated)."""
    var unpacked_sint32: List[Int32]
    """Field `unpacked_sint32` (number 93, repeated)."""
    var unpacked_sint64: List[Int64]
    """Field `unpacked_sint64` (number 94, repeated)."""
    var unpacked_fixed32: List[UInt32]
    """Field `unpacked_fixed32` (number 95, repeated)."""
    var unpacked_fixed64: List[UInt64]
    """Field `unpacked_fixed64` (number 96, repeated)."""
    var unpacked_sfixed32: List[Int32]
    """Field `unpacked_sfixed32` (number 97, repeated)."""
    var unpacked_sfixed64: List[Int64]
    """Field `unpacked_sfixed64` (number 98, repeated)."""
    var unpacked_float: List[Float32]
    """Field `unpacked_float` (number 99, repeated)."""
    var unpacked_double: List[Float64]
    """Field `unpacked_double` (number 100, repeated)."""
    var unpacked_bool: List[Bool]
    """Field `unpacked_bool` (number 101, repeated)."""
    var unpacked_nested_enum: List[Int32]
    """Field `unpacked_nested_enum` (number 102, repeated)."""
    var map_int32_int32: Dict[Int32, Int32]
    """Field `map_int32_int32` (number 56, map field)."""
    var map_int64_int64: Dict[Int64, Int64]
    """Field `map_int64_int64` (number 57, map field)."""
    var map_uint32_uint32: Dict[UInt32, UInt32]
    """Field `map_uint32_uint32` (number 58, map field)."""
    var map_uint64_uint64: Dict[UInt64, UInt64]
    """Field `map_uint64_uint64` (number 59, map field)."""
    var map_sint32_sint32: Dict[Int32, Int32]
    """Field `map_sint32_sint32` (number 60, map field)."""
    var map_sint64_sint64: Dict[Int64, Int64]
    """Field `map_sint64_sint64` (number 61, map field)."""
    var map_fixed32_fixed32: Dict[UInt32, UInt32]
    """Field `map_fixed32_fixed32` (number 62, map field)."""
    var map_fixed64_fixed64: Dict[UInt64, UInt64]
    """Field `map_fixed64_fixed64` (number 63, map field)."""
    var map_sfixed32_sfixed32: Dict[Int32, Int32]
    """Field `map_sfixed32_sfixed32` (number 64, map field)."""
    var map_sfixed64_sfixed64: Dict[Int64, Int64]
    """Field `map_sfixed64_sfixed64` (number 65, map field)."""
    var map_int32_float: Dict[Int32, Float32]
    """Field `map_int32_float` (number 66, map field)."""
    var map_int32_double: Dict[Int32, Float64]
    """Field `map_int32_double` (number 67, map field)."""
    var map_bool_bool: Dict[Bool, Bool]
    """Field `map_bool_bool` (number 68, map field)."""
    var map_string_string: Dict[String, String]
    """Field `map_string_string` (number 69, map field)."""
    var map_string_bytes: Dict[String, List[Byte]]
    """Field `map_string_bytes` (number 70, map field)."""
    var map_string_nested_message: Dict[String, TestAllTypesProto3_NestedMessage]
    """Field `map_string_nested_message` (number 71, map field)."""
    var map_string_foreign_message: Dict[String, ForeignMessage]
    """Field `map_string_foreign_message` (number 72, map field)."""
    var map_string_nested_enum: Dict[String, Int32]
    """Field `map_string_nested_enum` (number 73, map field)."""
    var map_string_foreign_enum: Dict[String, Int32]
    """Field `map_string_foreign_enum` (number 74, map field)."""
    var oneof_uint32: UInt32
    """Field `oneof_uint32` (number 111)."""
    var oneof_nested_message: List[TestAllTypesProto3_NestedMessage]
    """Field `oneof_nested_message` (number 112, recursive: boxed as a 0-or-1 list)."""
    var oneof_string: String
    """Field `oneof_string` (number 113)."""
    var oneof_bytes: List[Byte]
    """Field `oneof_bytes` (number 114)."""
    var oneof_bool: Bool
    """Field `oneof_bool` (number 115)."""
    var oneof_uint64: UInt64
    """Field `oneof_uint64` (number 116)."""
    var oneof_float: Float32
    """Field `oneof_float` (number 117)."""
    var oneof_double: Float64
    """Field `oneof_double` (number 118)."""
    var oneof_enum: Int32
    """Field `oneof_enum` (number 119)."""
    var oneof_null_value: Int32
    """Field `oneof_null_value` (number 120)."""
    var optional_bool_wrapper: Optional[BoolValue]
    """Field `optional_bool_wrapper` (number 201)."""
    var optional_int32_wrapper: Optional[Int32Value]
    """Field `optional_int32_wrapper` (number 202)."""
    var optional_int64_wrapper: Optional[Int64Value]
    """Field `optional_int64_wrapper` (number 203)."""
    var optional_uint32_wrapper: Optional[UInt32Value]
    """Field `optional_uint32_wrapper` (number 204)."""
    var optional_uint64_wrapper: Optional[UInt64Value]
    """Field `optional_uint64_wrapper` (number 205)."""
    var optional_float_wrapper: Optional[FloatValue]
    """Field `optional_float_wrapper` (number 206)."""
    var optional_double_wrapper: Optional[DoubleValue]
    """Field `optional_double_wrapper` (number 207)."""
    var optional_string_wrapper: Optional[StringValue]
    """Field `optional_string_wrapper` (number 208)."""
    var optional_bytes_wrapper: Optional[BytesValue]
    """Field `optional_bytes_wrapper` (number 209)."""
    var repeated_bool_wrapper: List[BoolValue]
    """Field `repeated_bool_wrapper` (number 211, repeated)."""
    var repeated_int32_wrapper: List[Int32Value]
    """Field `repeated_int32_wrapper` (number 212, repeated)."""
    var repeated_int64_wrapper: List[Int64Value]
    """Field `repeated_int64_wrapper` (number 213, repeated)."""
    var repeated_uint32_wrapper: List[UInt32Value]
    """Field `repeated_uint32_wrapper` (number 214, repeated)."""
    var repeated_uint64_wrapper: List[UInt64Value]
    """Field `repeated_uint64_wrapper` (number 215, repeated)."""
    var repeated_float_wrapper: List[FloatValue]
    """Field `repeated_float_wrapper` (number 216, repeated)."""
    var repeated_double_wrapper: List[DoubleValue]
    """Field `repeated_double_wrapper` (number 217, repeated)."""
    var repeated_string_wrapper: List[StringValue]
    """Field `repeated_string_wrapper` (number 218, repeated)."""
    var repeated_bytes_wrapper: List[BytesValue]
    """Field `repeated_bytes_wrapper` (number 219, repeated)."""
    var optional_duration: Optional[Duration]
    """Field `optional_duration` (number 301)."""
    var optional_timestamp: Optional[Timestamp]
    """Field `optional_timestamp` (number 302)."""
    var optional_field_mask: Optional[FieldMask]
    """Field `optional_field_mask` (number 303)."""
    var optional_struct: Optional[Struct]
    """Field `optional_struct` (number 304)."""
    var optional_any: Optional[Any]
    """Field `optional_any` (number 305)."""
    var optional_value: Optional[Value]
    """Field `optional_value` (number 306)."""
    var optional_null_value: Int32
    """Field `optional_null_value` (number 307)."""
    var repeated_duration: List[Duration]
    """Field `repeated_duration` (number 311, repeated)."""
    var repeated_timestamp: List[Timestamp]
    """Field `repeated_timestamp` (number 312, repeated)."""
    var repeated_fieldmask: List[FieldMask]
    """Field `repeated_fieldmask` (number 313, repeated)."""
    var repeated_struct: List[Struct]
    """Field `repeated_struct` (number 324, repeated)."""
    var repeated_any: List[Any]
    """Field `repeated_any` (number 315, repeated)."""
    var repeated_value: List[Value]
    """Field `repeated_value` (number 316, repeated)."""
    var repeated_list_value: List[ListValue]
    """Field `repeated_list_value` (number 317, repeated)."""
    var fieldname1: Int32
    """Field `fieldname1` (number 401)."""
    var field_name2: Int32
    """Field `field_name2` (number 402)."""
    var _field_name3: Int32
    """Field `_field_name3` (number 403)."""
    var field__name4_: Int32
    """Field `field__name4_` (number 404)."""
    var field0name5: Int32
    """Field `field0name5` (number 405)."""
    var field_0_name6: Int32
    """Field `field_0_name6` (number 406)."""
    var fieldName7: Int32
    """Field `fieldName7` (number 407)."""
    var FieldName8: Int32
    """Field `FieldName8` (number 408)."""
    var field_Name9: Int32
    """Field `field_Name9` (number 409)."""
    var Field_Name10: Int32
    """Field `Field_Name10` (number 410)."""
    var FIELD_NAME11: Int32
    """Field `FIELD_NAME11` (number 411)."""
    var FIELD_name12: Int32
    """Field `FIELD_name12` (number 412)."""
    var __field_name13: Int32
    """Field `__field_name13` (number 413)."""
    var __Field_name14: Int32
    """Field `__Field_name14` (number 414)."""
    var field__name15: Int32
    """Field `field__name15` (number 415)."""
    var field__Name16: Int32
    """Field `field__Name16` (number 416)."""
    var field_name17__: Int32
    """Field `field_name17__` (number 417)."""
    var Field_name18__: Int32
    """Field `Field_name18__` (number 418)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    var oneof_field_case: Int
    """Active member of oneof `oneof_field`: 0 when unset, else the set field number."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.optional_int32 = 0
        self.optional_int64 = 0
        self.optional_uint32 = 0
        self.optional_uint64 = 0
        self.optional_sint32 = 0
        self.optional_sint64 = 0
        self.optional_fixed32 = 0
        self.optional_fixed64 = 0
        self.optional_sfixed32 = 0
        self.optional_sfixed64 = 0
        self.optional_float = 0.0
        self.optional_double = 0.0
        self.optional_bool = False
        self.optional_string = String()
        self.optional_bytes = List[Byte]()
        self.optional_nested_message = List[TestAllTypesProto3_NestedMessage]()
        self.optional_foreign_message = None
        self.optional_nested_enum = 0
        self.optional_foreign_enum = 0
        self.optional_aliased_enum = 0
        self.optional_string_piece = String()
        self.optional_cord = String()
        self.recursive_message = List[TestAllTypesProto3]()
        self.repeated_int32 = List[Int32]()
        self.repeated_int64 = List[Int64]()
        self.repeated_uint32 = List[UInt32]()
        self.repeated_uint64 = List[UInt64]()
        self.repeated_sint32 = List[Int32]()
        self.repeated_sint64 = List[Int64]()
        self.repeated_fixed32 = List[UInt32]()
        self.repeated_fixed64 = List[UInt64]()
        self.repeated_sfixed32 = List[Int32]()
        self.repeated_sfixed64 = List[Int64]()
        self.repeated_float = List[Float32]()
        self.repeated_double = List[Float64]()
        self.repeated_bool = List[Bool]()
        self.repeated_string = List[String]()
        self.repeated_bytes = List[List[Byte]]()
        self.repeated_nested_message = List[TestAllTypesProto3_NestedMessage]()
        self.repeated_foreign_message = List[ForeignMessage]()
        self.repeated_nested_enum = List[Int32]()
        self.repeated_foreign_enum = List[Int32]()
        self.repeated_string_piece = List[String]()
        self.repeated_cord = List[String]()
        self.packed_int32 = List[Int32]()
        self.packed_int64 = List[Int64]()
        self.packed_uint32 = List[UInt32]()
        self.packed_uint64 = List[UInt64]()
        self.packed_sint32 = List[Int32]()
        self.packed_sint64 = List[Int64]()
        self.packed_fixed32 = List[UInt32]()
        self.packed_fixed64 = List[UInt64]()
        self.packed_sfixed32 = List[Int32]()
        self.packed_sfixed64 = List[Int64]()
        self.packed_float = List[Float32]()
        self.packed_double = List[Float64]()
        self.packed_bool = List[Bool]()
        self.packed_nested_enum = List[Int32]()
        self.unpacked_int32 = List[Int32]()
        self.unpacked_int64 = List[Int64]()
        self.unpacked_uint32 = List[UInt32]()
        self.unpacked_uint64 = List[UInt64]()
        self.unpacked_sint32 = List[Int32]()
        self.unpacked_sint64 = List[Int64]()
        self.unpacked_fixed32 = List[UInt32]()
        self.unpacked_fixed64 = List[UInt64]()
        self.unpacked_sfixed32 = List[Int32]()
        self.unpacked_sfixed64 = List[Int64]()
        self.unpacked_float = List[Float32]()
        self.unpacked_double = List[Float64]()
        self.unpacked_bool = List[Bool]()
        self.unpacked_nested_enum = List[Int32]()
        self.map_int32_int32 = Dict[Int32, Int32]()
        self.map_int64_int64 = Dict[Int64, Int64]()
        self.map_uint32_uint32 = Dict[UInt32, UInt32]()
        self.map_uint64_uint64 = Dict[UInt64, UInt64]()
        self.map_sint32_sint32 = Dict[Int32, Int32]()
        self.map_sint64_sint64 = Dict[Int64, Int64]()
        self.map_fixed32_fixed32 = Dict[UInt32, UInt32]()
        self.map_fixed64_fixed64 = Dict[UInt64, UInt64]()
        self.map_sfixed32_sfixed32 = Dict[Int32, Int32]()
        self.map_sfixed64_sfixed64 = Dict[Int64, Int64]()
        self.map_int32_float = Dict[Int32, Float32]()
        self.map_int32_double = Dict[Int32, Float64]()
        self.map_bool_bool = Dict[Bool, Bool]()
        self.map_string_string = Dict[String, String]()
        self.map_string_bytes = Dict[String, List[Byte]]()
        self.map_string_nested_message = Dict[String, TestAllTypesProto3_NestedMessage]()
        self.map_string_foreign_message = Dict[String, ForeignMessage]()
        self.map_string_nested_enum = Dict[String, Int32]()
        self.map_string_foreign_enum = Dict[String, Int32]()
        self.oneof_uint32 = 0
        self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
        self.oneof_string = String()
        self.oneof_bytes = List[Byte]()
        self.oneof_bool = False
        self.oneof_uint64 = 0
        self.oneof_float = 0.0
        self.oneof_double = 0.0
        self.oneof_enum = 0
        self.oneof_null_value = 0
        self.optional_bool_wrapper = None
        self.optional_int32_wrapper = None
        self.optional_int64_wrapper = None
        self.optional_uint32_wrapper = None
        self.optional_uint64_wrapper = None
        self.optional_float_wrapper = None
        self.optional_double_wrapper = None
        self.optional_string_wrapper = None
        self.optional_bytes_wrapper = None
        self.repeated_bool_wrapper = List[BoolValue]()
        self.repeated_int32_wrapper = List[Int32Value]()
        self.repeated_int64_wrapper = List[Int64Value]()
        self.repeated_uint32_wrapper = List[UInt32Value]()
        self.repeated_uint64_wrapper = List[UInt64Value]()
        self.repeated_float_wrapper = List[FloatValue]()
        self.repeated_double_wrapper = List[DoubleValue]()
        self.repeated_string_wrapper = List[StringValue]()
        self.repeated_bytes_wrapper = List[BytesValue]()
        self.optional_duration = None
        self.optional_timestamp = None
        self.optional_field_mask = None
        self.optional_struct = None
        self.optional_any = None
        self.optional_value = None
        self.optional_null_value = 0
        self.repeated_duration = List[Duration]()
        self.repeated_timestamp = List[Timestamp]()
        self.repeated_fieldmask = List[FieldMask]()
        self.repeated_struct = List[Struct]()
        self.repeated_any = List[Any]()
        self.repeated_value = List[Value]()
        self.repeated_list_value = List[ListValue]()
        self.fieldname1 = 0
        self.field_name2 = 0
        self._field_name3 = 0
        self.field__name4_ = 0
        self.field0name5 = 0
        self.field_0_name6 = 0
        self.fieldName7 = 0
        self.FieldName8 = 0
        self.field_Name9 = 0
        self.Field_Name10 = 0
        self.FIELD_NAME11 = 0
        self.FIELD_name12 = 0
        self.__field_name13 = 0
        self.__Field_name14 = 0
        self.field__name15 = 0
        self.field__Name16 = 0
        self.field_name17__ = 0
        self.Field_name18__ = 0
        self._unknown = List[Byte]()
        self.oneof_field_case = 0

    def __deinit__(deinit self):
        """Destroys the message (explicit: breaks the trait-synthesis cycle for recursive types)."""
        pass

    def __init__(out self, *, copy: Self):
        """Deep-copies another message.

        Args:
            copy: The message to copy from.
        """
        self.optional_int32 = copy.optional_int32
        self.optional_int64 = copy.optional_int64
        self.optional_uint32 = copy.optional_uint32
        self.optional_uint64 = copy.optional_uint64
        self.optional_sint32 = copy.optional_sint32
        self.optional_sint64 = copy.optional_sint64
        self.optional_fixed32 = copy.optional_fixed32
        self.optional_fixed64 = copy.optional_fixed64
        self.optional_sfixed32 = copy.optional_sfixed32
        self.optional_sfixed64 = copy.optional_sfixed64
        self.optional_float = copy.optional_float
        self.optional_double = copy.optional_double
        self.optional_bool = copy.optional_bool
        self.optional_string = copy.optional_string.copy()
        self.optional_bytes = copy.optional_bytes.copy()
        self.optional_nested_message = copy.optional_nested_message.copy()
        self.optional_foreign_message = copy.optional_foreign_message.copy()
        self.optional_nested_enum = copy.optional_nested_enum
        self.optional_foreign_enum = copy.optional_foreign_enum
        self.optional_aliased_enum = copy.optional_aliased_enum
        self.optional_string_piece = copy.optional_string_piece.copy()
        self.optional_cord = copy.optional_cord.copy()
        self.recursive_message = copy.recursive_message.copy()
        self.repeated_int32 = copy.repeated_int32.copy()
        self.repeated_int64 = copy.repeated_int64.copy()
        self.repeated_uint32 = copy.repeated_uint32.copy()
        self.repeated_uint64 = copy.repeated_uint64.copy()
        self.repeated_sint32 = copy.repeated_sint32.copy()
        self.repeated_sint64 = copy.repeated_sint64.copy()
        self.repeated_fixed32 = copy.repeated_fixed32.copy()
        self.repeated_fixed64 = copy.repeated_fixed64.copy()
        self.repeated_sfixed32 = copy.repeated_sfixed32.copy()
        self.repeated_sfixed64 = copy.repeated_sfixed64.copy()
        self.repeated_float = copy.repeated_float.copy()
        self.repeated_double = copy.repeated_double.copy()
        self.repeated_bool = copy.repeated_bool.copy()
        self.repeated_string = copy.repeated_string.copy()
        self.repeated_bytes = copy.repeated_bytes.copy()
        self.repeated_nested_message = copy.repeated_nested_message.copy()
        self.repeated_foreign_message = copy.repeated_foreign_message.copy()
        self.repeated_nested_enum = copy.repeated_nested_enum.copy()
        self.repeated_foreign_enum = copy.repeated_foreign_enum.copy()
        self.repeated_string_piece = copy.repeated_string_piece.copy()
        self.repeated_cord = copy.repeated_cord.copy()
        self.packed_int32 = copy.packed_int32.copy()
        self.packed_int64 = copy.packed_int64.copy()
        self.packed_uint32 = copy.packed_uint32.copy()
        self.packed_uint64 = copy.packed_uint64.copy()
        self.packed_sint32 = copy.packed_sint32.copy()
        self.packed_sint64 = copy.packed_sint64.copy()
        self.packed_fixed32 = copy.packed_fixed32.copy()
        self.packed_fixed64 = copy.packed_fixed64.copy()
        self.packed_sfixed32 = copy.packed_sfixed32.copy()
        self.packed_sfixed64 = copy.packed_sfixed64.copy()
        self.packed_float = copy.packed_float.copy()
        self.packed_double = copy.packed_double.copy()
        self.packed_bool = copy.packed_bool.copy()
        self.packed_nested_enum = copy.packed_nested_enum.copy()
        self.unpacked_int32 = copy.unpacked_int32.copy()
        self.unpacked_int64 = copy.unpacked_int64.copy()
        self.unpacked_uint32 = copy.unpacked_uint32.copy()
        self.unpacked_uint64 = copy.unpacked_uint64.copy()
        self.unpacked_sint32 = copy.unpacked_sint32.copy()
        self.unpacked_sint64 = copy.unpacked_sint64.copy()
        self.unpacked_fixed32 = copy.unpacked_fixed32.copy()
        self.unpacked_fixed64 = copy.unpacked_fixed64.copy()
        self.unpacked_sfixed32 = copy.unpacked_sfixed32.copy()
        self.unpacked_sfixed64 = copy.unpacked_sfixed64.copy()
        self.unpacked_float = copy.unpacked_float.copy()
        self.unpacked_double = copy.unpacked_double.copy()
        self.unpacked_bool = copy.unpacked_bool.copy()
        self.unpacked_nested_enum = copy.unpacked_nested_enum.copy()
        self.map_int32_int32 = copy.map_int32_int32.copy()
        self.map_int64_int64 = copy.map_int64_int64.copy()
        self.map_uint32_uint32 = copy.map_uint32_uint32.copy()
        self.map_uint64_uint64 = copy.map_uint64_uint64.copy()
        self.map_sint32_sint32 = copy.map_sint32_sint32.copy()
        self.map_sint64_sint64 = copy.map_sint64_sint64.copy()
        self.map_fixed32_fixed32 = copy.map_fixed32_fixed32.copy()
        self.map_fixed64_fixed64 = copy.map_fixed64_fixed64.copy()
        self.map_sfixed32_sfixed32 = copy.map_sfixed32_sfixed32.copy()
        self.map_sfixed64_sfixed64 = copy.map_sfixed64_sfixed64.copy()
        self.map_int32_float = copy.map_int32_float.copy()
        self.map_int32_double = copy.map_int32_double.copy()
        self.map_bool_bool = copy.map_bool_bool.copy()
        self.map_string_string = copy.map_string_string.copy()
        self.map_string_bytes = copy.map_string_bytes.copy()
        self.map_string_nested_message = copy.map_string_nested_message.copy()
        self.map_string_foreign_message = copy.map_string_foreign_message.copy()
        self.map_string_nested_enum = copy.map_string_nested_enum.copy()
        self.map_string_foreign_enum = copy.map_string_foreign_enum.copy()
        self.oneof_uint32 = copy.oneof_uint32
        self.oneof_nested_message = copy.oneof_nested_message.copy()
        self.oneof_string = copy.oneof_string.copy()
        self.oneof_bytes = copy.oneof_bytes.copy()
        self.oneof_bool = copy.oneof_bool
        self.oneof_uint64 = copy.oneof_uint64
        self.oneof_float = copy.oneof_float
        self.oneof_double = copy.oneof_double
        self.oneof_enum = copy.oneof_enum
        self.oneof_null_value = copy.oneof_null_value
        self.optional_bool_wrapper = copy.optional_bool_wrapper.copy()
        self.optional_int32_wrapper = copy.optional_int32_wrapper.copy()
        self.optional_int64_wrapper = copy.optional_int64_wrapper.copy()
        self.optional_uint32_wrapper = copy.optional_uint32_wrapper.copy()
        self.optional_uint64_wrapper = copy.optional_uint64_wrapper.copy()
        self.optional_float_wrapper = copy.optional_float_wrapper.copy()
        self.optional_double_wrapper = copy.optional_double_wrapper.copy()
        self.optional_string_wrapper = copy.optional_string_wrapper.copy()
        self.optional_bytes_wrapper = copy.optional_bytes_wrapper.copy()
        self.repeated_bool_wrapper = copy.repeated_bool_wrapper.copy()
        self.repeated_int32_wrapper = copy.repeated_int32_wrapper.copy()
        self.repeated_int64_wrapper = copy.repeated_int64_wrapper.copy()
        self.repeated_uint32_wrapper = copy.repeated_uint32_wrapper.copy()
        self.repeated_uint64_wrapper = copy.repeated_uint64_wrapper.copy()
        self.repeated_float_wrapper = copy.repeated_float_wrapper.copy()
        self.repeated_double_wrapper = copy.repeated_double_wrapper.copy()
        self.repeated_string_wrapper = copy.repeated_string_wrapper.copy()
        self.repeated_bytes_wrapper = copy.repeated_bytes_wrapper.copy()
        self.optional_duration = copy.optional_duration.copy()
        self.optional_timestamp = copy.optional_timestamp.copy()
        self.optional_field_mask = copy.optional_field_mask.copy()
        self.optional_struct = copy.optional_struct.copy()
        self.optional_any = copy.optional_any.copy()
        self.optional_value = copy.optional_value.copy()
        self.optional_null_value = copy.optional_null_value
        self.repeated_duration = copy.repeated_duration.copy()
        self.repeated_timestamp = copy.repeated_timestamp.copy()
        self.repeated_fieldmask = copy.repeated_fieldmask.copy()
        self.repeated_struct = copy.repeated_struct.copy()
        self.repeated_any = copy.repeated_any.copy()
        self.repeated_value = copy.repeated_value.copy()
        self.repeated_list_value = copy.repeated_list_value.copy()
        self.fieldname1 = copy.fieldname1
        self.field_name2 = copy.field_name2
        self._field_name3 = copy._field_name3
        self.field__name4_ = copy.field__name4_
        self.field0name5 = copy.field0name5
        self.field_0_name6 = copy.field_0_name6
        self.fieldName7 = copy.fieldName7
        self.FieldName8 = copy.FieldName8
        self.field_Name9 = copy.field_Name9
        self.Field_Name10 = copy.Field_Name10
        self.FIELD_NAME11 = copy.FIELD_NAME11
        self.FIELD_name12 = copy.FIELD_name12
        self.__field_name13 = copy.__field_name13
        self.__Field_name14 = copy.__Field_name14
        self.field__name15 = copy.field__name15
        self.field__Name16 = copy.field__Name16
        self.field_name17__ = copy.field_name17__
        self.Field_name18__ = copy.Field_name18__
        self.oneof_field_case = copy.oneof_field_case
        self._unknown = copy._unknown.copy()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.optional_int32 != 0:
            writer.int32(1, self.optional_int32)
        if self.optional_int64 != 0:
            writer.int64(2, self.optional_int64)
        if self.optional_uint32 != 0:
            writer.uint32(3, self.optional_uint32)
        if self.optional_uint64 != 0:
            writer.uint64(4, self.optional_uint64)
        if self.optional_sint32 != 0:
            writer.sint32(5, self.optional_sint32)
        if self.optional_sint64 != 0:
            writer.sint64(6, self.optional_sint64)
        if self.optional_fixed32 != 0:
            writer.fixed32_field(7, self.optional_fixed32)
        if self.optional_fixed64 != 0:
            writer.fixed64_field(8, self.optional_fixed64)
        if self.optional_sfixed32 != 0:
            writer.sfixed32_field(9, self.optional_sfixed32)
        if self.optional_sfixed64 != 0:
            writer.sfixed64_field(10, self.optional_sfixed64)
        if UInt32(self.optional_float.to_bits()) != 0:
            writer.float_field(11, self.optional_float)
        if UInt64(self.optional_double.to_bits()) != 0:
            writer.double_field(12, self.optional_double)
        if self.optional_bool:
            writer.bool_field(13, self.optional_bool)
        if self.optional_string.byte_length() != 0:
            writer.string_field(14, self.optional_string)
        if len(self.optional_bytes) != 0:
            writer.bytes_field(15, Span(self.optional_bytes))
        if len(self.optional_nested_message) != 0:
            var sub = WireWriter()
            self.optional_nested_message[0].encode_to(sub)
            writer.len_prefixed(18, Span(sub.buf))
        if self.optional_foreign_message:
            var sub = WireWriter()
            self.optional_foreign_message.value().encode_to(sub)
            writer.len_prefixed(19, Span(sub.buf))
        if self.optional_nested_enum != 0:
            writer.int32(21, self.optional_nested_enum)
        if self.optional_foreign_enum != 0:
            writer.int32(22, self.optional_foreign_enum)
        if self.optional_aliased_enum != 0:
            writer.int32(23, self.optional_aliased_enum)
        if self.optional_string_piece.byte_length() != 0:
            writer.string_field(24, self.optional_string_piece)
        if self.optional_cord.byte_length() != 0:
            writer.string_field(25, self.optional_cord)
        if len(self.recursive_message) != 0:
            var sub = WireWriter()
            self.recursive_message[0].encode_to(sub)
            writer.len_prefixed(27, Span(sub.buf))
        if len(self.repeated_int32) != 0:
            var sub = WireWriter()
            for v in self.repeated_int32:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(31, Span(sub.buf))
        if len(self.repeated_int64) != 0:
            var sub = WireWriter()
            for v in self.repeated_int64:
                sub.varint(UInt64(v))
            writer.len_prefixed(32, Span(sub.buf))
        if len(self.repeated_uint32) != 0:
            var sub = WireWriter()
            for v in self.repeated_uint32:
                sub.varint(UInt64(v))
            writer.len_prefixed(33, Span(sub.buf))
        if len(self.repeated_uint64) != 0:
            var sub = WireWriter()
            for v in self.repeated_uint64:
                sub.varint(v)
            writer.len_prefixed(34, Span(sub.buf))
        if len(self.repeated_sint32) != 0:
            var sub = WireWriter()
            for v in self.repeated_sint32:
                sub.varint(zigzag_encode64(Int64(v)))
            writer.len_prefixed(35, Span(sub.buf))
        if len(self.repeated_sint64) != 0:
            var sub = WireWriter()
            for v in self.repeated_sint64:
                sub.varint(zigzag_encode64(v))
            writer.len_prefixed(36, Span(sub.buf))
        if len(self.repeated_fixed32) != 0:
            var sub = WireWriter()
            for v in self.repeated_fixed32:
                sub.fixed32(v)
            writer.len_prefixed(37, Span(sub.buf))
        if len(self.repeated_fixed64) != 0:
            var sub = WireWriter()
            for v in self.repeated_fixed64:
                sub.fixed64(v)
            writer.len_prefixed(38, Span(sub.buf))
        if len(self.repeated_sfixed32) != 0:
            var sub = WireWriter()
            for v in self.repeated_sfixed32:
                sub.fixed32(UInt32(from_bits=v))
            writer.len_prefixed(39, Span(sub.buf))
        if len(self.repeated_sfixed64) != 0:
            var sub = WireWriter()
            for v in self.repeated_sfixed64:
                sub.fixed64(UInt64(from_bits=v))
            writer.len_prefixed(40, Span(sub.buf))
        if len(self.repeated_float) != 0:
            var sub = WireWriter()
            for v in self.repeated_float:
                sub.fixed32(UInt32(v.to_bits()))
            writer.len_prefixed(41, Span(sub.buf))
        if len(self.repeated_double) != 0:
            var sub = WireWriter()
            for v in self.repeated_double:
                sub.fixed64(UInt64(v.to_bits()))
            writer.len_prefixed(42, Span(sub.buf))
        if len(self.repeated_bool) != 0:
            var sub = WireWriter()
            for v in self.repeated_bool:
                sub.varint(UInt64(1) if v else UInt64(0))
            writer.len_prefixed(43, Span(sub.buf))
        for v in self.repeated_string:
            writer.string_field(44, v)
        for v in self.repeated_bytes:
            writer.bytes_field(45, Span(v))
        for v in self.repeated_nested_message:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(48, Span(sub.buf))
        for v in self.repeated_foreign_message:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(49, Span(sub.buf))
        if len(self.repeated_nested_enum) != 0:
            var sub = WireWriter()
            for v in self.repeated_nested_enum:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(51, Span(sub.buf))
        if len(self.repeated_foreign_enum) != 0:
            var sub = WireWriter()
            for v in self.repeated_foreign_enum:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(52, Span(sub.buf))
        for v in self.repeated_string_piece:
            writer.string_field(54, v)
        for v in self.repeated_cord:
            writer.string_field(55, v)
        for entry in self.map_int32_int32.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int32(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(56, Span(sub.buf))
        for entry in self.map_int64_int64.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int64(1, entry.key)
            if entry.value != 0:
                sub.int64(2, entry.value)
            writer.len_prefixed(57, Span(sub.buf))
        for entry in self.map_uint32_uint32.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.uint32(1, entry.key)
            if entry.value != 0:
                sub.uint32(2, entry.value)
            writer.len_prefixed(58, Span(sub.buf))
        for entry in self.map_uint64_uint64.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.uint64(1, entry.key)
            if entry.value != 0:
                sub.uint64(2, entry.value)
            writer.len_prefixed(59, Span(sub.buf))
        for entry in self.map_sint32_sint32.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.sint32(1, entry.key)
            if entry.value != 0:
                sub.sint32(2, entry.value)
            writer.len_prefixed(60, Span(sub.buf))
        for entry in self.map_sint64_sint64.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.sint64(1, entry.key)
            if entry.value != 0:
                sub.sint64(2, entry.value)
            writer.len_prefixed(61, Span(sub.buf))
        for entry in self.map_fixed32_fixed32.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.fixed32_field(1, entry.key)
            if entry.value != 0:
                sub.fixed32_field(2, entry.value)
            writer.len_prefixed(62, Span(sub.buf))
        for entry in self.map_fixed64_fixed64.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.fixed64_field(1, entry.key)
            if entry.value != 0:
                sub.fixed64_field(2, entry.value)
            writer.len_prefixed(63, Span(sub.buf))
        for entry in self.map_sfixed32_sfixed32.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.sfixed32_field(1, entry.key)
            if entry.value != 0:
                sub.sfixed32_field(2, entry.value)
            writer.len_prefixed(64, Span(sub.buf))
        for entry in self.map_sfixed64_sfixed64.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.sfixed64_field(1, entry.key)
            if entry.value != 0:
                sub.sfixed64_field(2, entry.value)
            writer.len_prefixed(65, Span(sub.buf))
        for entry in self.map_int32_float.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int32(1, entry.key)
            if UInt32(entry.value.to_bits()) != 0:
                sub.float_field(2, entry.value)
            writer.len_prefixed(66, Span(sub.buf))
        for entry in self.map_int32_double.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int32(1, entry.key)
            if UInt64(entry.value.to_bits()) != 0:
                sub.double_field(2, entry.value)
            writer.len_prefixed(67, Span(sub.buf))
        for entry in self.map_bool_bool.items():
            var sub = WireWriter()
            if entry.key:
                sub.bool_field(1, entry.key)
            if entry.value:
                sub.bool_field(2, entry.value)
            writer.len_prefixed(68, Span(sub.buf))
        for entry in self.map_string_string.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(69, Span(sub.buf))
        for entry in self.map_string_bytes.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if len(entry.value) != 0:
                sub.bytes_field(2, Span(entry.value))
            writer.len_prefixed(70, Span(sub.buf))
        for entry in self.map_string_nested_message.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            var vsub = WireWriter()
            entry.value.encode_to(vsub)
            sub.len_prefixed(2, Span(vsub.buf))
            writer.len_prefixed(71, Span(sub.buf))
        for entry in self.map_string_foreign_message.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            var vsub = WireWriter()
            entry.value.encode_to(vsub)
            sub.len_prefixed(2, Span(vsub.buf))
            writer.len_prefixed(72, Span(sub.buf))
        for entry in self.map_string_nested_enum.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(73, Span(sub.buf))
        for entry in self.map_string_foreign_enum.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(74, Span(sub.buf))
        if len(self.packed_int32) != 0:
            var sub = WireWriter()
            for v in self.packed_int32:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(75, Span(sub.buf))
        if len(self.packed_int64) != 0:
            var sub = WireWriter()
            for v in self.packed_int64:
                sub.varint(UInt64(v))
            writer.len_prefixed(76, Span(sub.buf))
        if len(self.packed_uint32) != 0:
            var sub = WireWriter()
            for v in self.packed_uint32:
                sub.varint(UInt64(v))
            writer.len_prefixed(77, Span(sub.buf))
        if len(self.packed_uint64) != 0:
            var sub = WireWriter()
            for v in self.packed_uint64:
                sub.varint(v)
            writer.len_prefixed(78, Span(sub.buf))
        if len(self.packed_sint32) != 0:
            var sub = WireWriter()
            for v in self.packed_sint32:
                sub.varint(zigzag_encode64(Int64(v)))
            writer.len_prefixed(79, Span(sub.buf))
        if len(self.packed_sint64) != 0:
            var sub = WireWriter()
            for v in self.packed_sint64:
                sub.varint(zigzag_encode64(v))
            writer.len_prefixed(80, Span(sub.buf))
        if len(self.packed_fixed32) != 0:
            var sub = WireWriter()
            for v in self.packed_fixed32:
                sub.fixed32(v)
            writer.len_prefixed(81, Span(sub.buf))
        if len(self.packed_fixed64) != 0:
            var sub = WireWriter()
            for v in self.packed_fixed64:
                sub.fixed64(v)
            writer.len_prefixed(82, Span(sub.buf))
        if len(self.packed_sfixed32) != 0:
            var sub = WireWriter()
            for v in self.packed_sfixed32:
                sub.fixed32(UInt32(from_bits=v))
            writer.len_prefixed(83, Span(sub.buf))
        if len(self.packed_sfixed64) != 0:
            var sub = WireWriter()
            for v in self.packed_sfixed64:
                sub.fixed64(UInt64(from_bits=v))
            writer.len_prefixed(84, Span(sub.buf))
        if len(self.packed_float) != 0:
            var sub = WireWriter()
            for v in self.packed_float:
                sub.fixed32(UInt32(v.to_bits()))
            writer.len_prefixed(85, Span(sub.buf))
        if len(self.packed_double) != 0:
            var sub = WireWriter()
            for v in self.packed_double:
                sub.fixed64(UInt64(v.to_bits()))
            writer.len_prefixed(86, Span(sub.buf))
        if len(self.packed_bool) != 0:
            var sub = WireWriter()
            for v in self.packed_bool:
                sub.varint(UInt64(1) if v else UInt64(0))
            writer.len_prefixed(87, Span(sub.buf))
        if len(self.packed_nested_enum) != 0:
            var sub = WireWriter()
            for v in self.packed_nested_enum:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(88, Span(sub.buf))
        for v in self.unpacked_int32:
            writer.int32(89, v)
        for v in self.unpacked_int64:
            writer.int64(90, v)
        for v in self.unpacked_uint32:
            writer.uint32(91, v)
        for v in self.unpacked_uint64:
            writer.uint64(92, v)
        for v in self.unpacked_sint32:
            writer.sint32(93, v)
        for v in self.unpacked_sint64:
            writer.sint64(94, v)
        for v in self.unpacked_fixed32:
            writer.fixed32_field(95, v)
        for v in self.unpacked_fixed64:
            writer.fixed64_field(96, v)
        for v in self.unpacked_sfixed32:
            writer.sfixed32_field(97, v)
        for v in self.unpacked_sfixed64:
            writer.sfixed64_field(98, v)
        for v in self.unpacked_float:
            writer.float_field(99, v)
        for v in self.unpacked_double:
            writer.double_field(100, v)
        for v in self.unpacked_bool:
            writer.bool_field(101, v)
        for v in self.unpacked_nested_enum:
            writer.int32(102, v)
        if self.optional_bool_wrapper:
            var sub = WireWriter()
            self.optional_bool_wrapper.value().encode_to(sub)
            writer.len_prefixed(201, Span(sub.buf))
        if self.optional_int32_wrapper:
            var sub = WireWriter()
            self.optional_int32_wrapper.value().encode_to(sub)
            writer.len_prefixed(202, Span(sub.buf))
        if self.optional_int64_wrapper:
            var sub = WireWriter()
            self.optional_int64_wrapper.value().encode_to(sub)
            writer.len_prefixed(203, Span(sub.buf))
        if self.optional_uint32_wrapper:
            var sub = WireWriter()
            self.optional_uint32_wrapper.value().encode_to(sub)
            writer.len_prefixed(204, Span(sub.buf))
        if self.optional_uint64_wrapper:
            var sub = WireWriter()
            self.optional_uint64_wrapper.value().encode_to(sub)
            writer.len_prefixed(205, Span(sub.buf))
        if self.optional_float_wrapper:
            var sub = WireWriter()
            self.optional_float_wrapper.value().encode_to(sub)
            writer.len_prefixed(206, Span(sub.buf))
        if self.optional_double_wrapper:
            var sub = WireWriter()
            self.optional_double_wrapper.value().encode_to(sub)
            writer.len_prefixed(207, Span(sub.buf))
        if self.optional_string_wrapper:
            var sub = WireWriter()
            self.optional_string_wrapper.value().encode_to(sub)
            writer.len_prefixed(208, Span(sub.buf))
        if self.optional_bytes_wrapper:
            var sub = WireWriter()
            self.optional_bytes_wrapper.value().encode_to(sub)
            writer.len_prefixed(209, Span(sub.buf))
        for v in self.repeated_bool_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(211, Span(sub.buf))
        for v in self.repeated_int32_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(212, Span(sub.buf))
        for v in self.repeated_int64_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(213, Span(sub.buf))
        for v in self.repeated_uint32_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(214, Span(sub.buf))
        for v in self.repeated_uint64_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(215, Span(sub.buf))
        for v in self.repeated_float_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(216, Span(sub.buf))
        for v in self.repeated_double_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(217, Span(sub.buf))
        for v in self.repeated_string_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(218, Span(sub.buf))
        for v in self.repeated_bytes_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(219, Span(sub.buf))
        if self.optional_duration:
            var sub = WireWriter()
            self.optional_duration.value().encode_to(sub)
            writer.len_prefixed(301, Span(sub.buf))
        if self.optional_timestamp:
            var sub = WireWriter()
            self.optional_timestamp.value().encode_to(sub)
            writer.len_prefixed(302, Span(sub.buf))
        if self.optional_field_mask:
            var sub = WireWriter()
            self.optional_field_mask.value().encode_to(sub)
            writer.len_prefixed(303, Span(sub.buf))
        if self.optional_struct:
            var sub = WireWriter()
            self.optional_struct.value().encode_to(sub)
            writer.len_prefixed(304, Span(sub.buf))
        if self.optional_any:
            var sub = WireWriter()
            self.optional_any.value().encode_to(sub)
            writer.len_prefixed(305, Span(sub.buf))
        if self.optional_value:
            var sub = WireWriter()
            self.optional_value.value().encode_to(sub)
            writer.len_prefixed(306, Span(sub.buf))
        if self.optional_null_value != 0:
            writer.int32(307, self.optional_null_value)
        for v in self.repeated_duration:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(311, Span(sub.buf))
        for v in self.repeated_timestamp:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(312, Span(sub.buf))
        for v in self.repeated_fieldmask:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(313, Span(sub.buf))
        for v in self.repeated_any:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(315, Span(sub.buf))
        for v in self.repeated_value:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(316, Span(sub.buf))
        for v in self.repeated_list_value:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(317, Span(sub.buf))
        for v in self.repeated_struct:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(324, Span(sub.buf))
        if self.fieldname1 != 0:
            writer.int32(401, self.fieldname1)
        if self.field_name2 != 0:
            writer.int32(402, self.field_name2)
        if self._field_name3 != 0:
            writer.int32(403, self._field_name3)
        if self.field__name4_ != 0:
            writer.int32(404, self.field__name4_)
        if self.field0name5 != 0:
            writer.int32(405, self.field0name5)
        if self.field_0_name6 != 0:
            writer.int32(406, self.field_0_name6)
        if self.fieldName7 != 0:
            writer.int32(407, self.fieldName7)
        if self.FieldName8 != 0:
            writer.int32(408, self.FieldName8)
        if self.field_Name9 != 0:
            writer.int32(409, self.field_Name9)
        if self.Field_Name10 != 0:
            writer.int32(410, self.Field_Name10)
        if self.FIELD_NAME11 != 0:
            writer.int32(411, self.FIELD_NAME11)
        if self.FIELD_name12 != 0:
            writer.int32(412, self.FIELD_name12)
        if self.__field_name13 != 0:
            writer.int32(413, self.__field_name13)
        if self.__Field_name14 != 0:
            writer.int32(414, self.__Field_name14)
        if self.field__name15 != 0:
            writer.int32(415, self.field__name15)
        if self.field__Name16 != 0:
            writer.int32(416, self.field__Name16)
        if self.field_name17__ != 0:
            writer.int32(417, self.field_name17__)
        if self.Field_name18__ != 0:
            writer.int32(418, self.Field_name18__)
        if self.oneof_field_case == 111:
            writer.uint32(111, self.oneof_uint32)
        elif self.oneof_field_case == 112:
            if len(self.oneof_nested_message) != 0:
                var sub = WireWriter()
                self.oneof_nested_message[0].encode_to(sub)
                writer.len_prefixed(112, Span(sub.buf))
        elif self.oneof_field_case == 113:
            writer.string_field(113, self.oneof_string)
        elif self.oneof_field_case == 114:
            writer.bytes_field(114, Span(self.oneof_bytes))
        elif self.oneof_field_case == 115:
            writer.bool_field(115, self.oneof_bool)
        elif self.oneof_field_case == 116:
            writer.uint64(116, self.oneof_uint64)
        elif self.oneof_field_case == 117:
            writer.float_field(117, self.oneof_float)
        elif self.oneof_field_case == 118:
            writer.double_field(118, self.oneof_double)
        elif self.oneof_field_case == 119:
            writer.int32(119, self.oneof_enum)
        elif self.oneof_field_case == 120:
            writer.int32(120, self.oneof_null_value)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_int32 = reader.int32_value()
            elif field == 2:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_int64 = reader.int64_value()
            elif field == 3:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_uint32 = reader.uint32_value()
            elif field == 4:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_uint64 = reader.varint()
            elif field == 5:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_sint32 = reader.sint32_value()
            elif field == 6:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_sint64 = reader.sint64_value()
            elif field == 7:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_fixed32 = reader.fixed32()
            elif field == 8:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_fixed64 = reader.fixed64()
            elif field == 9:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_sfixed32 = reader.sfixed32_value()
            elif field == 10:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_sfixed64 = reader.sfixed64_value()
            elif field == 11:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_float = reader.float_value()
            elif field == 12:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_double = reader.double_value()
            elif field == 13:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_bool = reader.bool_value()
            elif field == 14:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_string = reader.string_value()
            elif field == 15:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_bytes = reader.bytes_value()
            elif field == 18:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: TestAllTypesProto3_NestedMessage
                    if len(self.optional_nested_message) != 0:
                        m = self.optional_nested_message.pop()
                    else:
                        m = TestAllTypesProto3_NestedMessage()
                    m.merge_from(sub)
                    self.optional_nested_message.append(m^)
            elif field == 19:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: ForeignMessage
                    if self.optional_foreign_message:
                        m = self.optional_foreign_message.take()
                    else:
                        m = ForeignMessage()
                    m.merge_from(sub)
                    self.optional_foreign_message = m^
            elif field == 21:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_nested_enum = reader.int32_value()
            elif field == 22:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_foreign_enum = reader.int32_value()
            elif field == 23:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_aliased_enum = reader.int32_value()
            elif field == 24:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_string_piece = reader.string_value()
            elif field == 25:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_cord = reader.string_value()
            elif field == 27:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: TestAllTypesProto3
                    if len(self.recursive_message) != 0:
                        m = self.recursive_message.pop()
                    else:
                        m = TestAllTypesProto3()
                    m.merge_from(sub)
                    self.recursive_message.append(m^)
            elif field == 31:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_int32.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_int32.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 32:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_int64.append(sub.int64_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_int64.append(reader.int64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 33:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_uint32.append(sub.uint32_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_uint32.append(reader.uint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 34:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_uint64.append(sub.varint())
                elif wire_type == WIRE_VARINT:
                    self.repeated_uint64.append(reader.varint())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 35:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_sint32.append(sub.sint32_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_sint32.append(reader.sint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 36:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_sint64.append(sub.sint64_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_sint64.append(reader.sint64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 37:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_fixed32.append(sub.fixed32())
                elif wire_type == WIRE_FIXED32:
                    self.repeated_fixed32.append(reader.fixed32())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 38:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_fixed64.append(sub.fixed64())
                elif wire_type == WIRE_FIXED64:
                    self.repeated_fixed64.append(reader.fixed64())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 39:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_sfixed32.append(sub.sfixed32_value())
                elif wire_type == WIRE_FIXED32:
                    self.repeated_sfixed32.append(reader.sfixed32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 40:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_sfixed64.append(sub.sfixed64_value())
                elif wire_type == WIRE_FIXED64:
                    self.repeated_sfixed64.append(reader.sfixed64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 41:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_float.append(sub.float_value())
                elif wire_type == WIRE_FIXED32:
                    self.repeated_float.append(reader.float_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 42:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_double.append(sub.double_value())
                elif wire_type == WIRE_FIXED64:
                    self.repeated_double.append(reader.double_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 43:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_bool.append(sub.bool_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_bool.append(reader.bool_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 44:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.repeated_string.append(reader.string_value())
            elif field == 45:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.repeated_bytes.append(reader.bytes_value())
            elif field == 48:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = TestAllTypesProto3_NestedMessage()
                    m.merge_from(sub)
                    self.repeated_nested_message.append(m^)
            elif field == 49:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = ForeignMessage()
                    m.merge_from(sub)
                    self.repeated_foreign_message.append(m^)
            elif field == 51:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_nested_enum.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_nested_enum.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 52:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_foreign_enum.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_foreign_enum.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 54:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.repeated_string_piece.append(reader.string_value())
            elif field == 55:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.repeated_cord.append(reader.string_value())
            elif field == 56:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.int32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_int32_int32[key] = value
            elif field == 57:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int64 = 0
                    var value: Int64 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.int64_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int64_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_int64_int64[key] = value
            elif field == 58:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: UInt32 = 0
                    var value: UInt32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.uint32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.uint32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_uint32_uint32[key] = value
            elif field == 59:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: UInt64 = 0
                    var value: UInt64 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.varint()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.varint()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_uint64_uint64[key] = value
            elif field == 60:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.sint32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.sint32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_sint32_sint32[key] = value
            elif field == 61:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int64 = 0
                    var value: Int64 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.sint64_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.sint64_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_sint64_sint64[key] = value
            elif field == 62:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: UInt32 = 0
                    var value: UInt32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_FIXED32:
                            key = sub.fixed32()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED32:
                            value = sub.fixed32()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_fixed32_fixed32[key] = value
            elif field == 63:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: UInt64 = 0
                    var value: UInt64 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_FIXED64:
                            key = sub.fixed64()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED64:
                            value = sub.fixed64()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_fixed64_fixed64[key] = value
            elif field == 64:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_FIXED32:
                            key = sub.sfixed32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED32:
                            value = sub.sfixed32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_sfixed32_sfixed32[key] = value
            elif field == 65:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int64 = 0
                    var value: Int64 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_FIXED64:
                            key = sub.sfixed64_value()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED64:
                            value = sub.sfixed64_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_sfixed64_sfixed64[key] = value
            elif field == 66:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: Float32 = 0.0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.int32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED32:
                            value = sub.float_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_int32_float[key] = value
            elif field == 67:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: Float64 = 0.0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.int32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED64:
                            value = sub.double_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_int32_double[key] = value
            elif field == 68:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Bool = False
                    var value: Bool = False
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.bool_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.bool_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_bool_bool[key] = value
            elif field == 69:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_string_string[key^] = value^
            elif field == 70:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: List[Byte] = List[Byte]()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.bytes_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_string_bytes[key^] = value^
            elif field == 71:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: TestAllTypesProto3_NestedMessage = TestAllTypesProto3_NestedMessage()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            var msub = sub.sub_reader()
                            value.merge_from(msub)
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_string_nested_message[key^] = value^
            elif field == 72:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: ForeignMessage = ForeignMessage()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            var msub = sub.sub_reader()
                            value.merge_from(msub)
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_string_foreign_message[key^] = value^
            elif field == 73:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_string_nested_enum[key^] = value
            elif field == 74:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.map_string_foreign_enum[key^] = value
            elif field == 75:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_int32.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_int32.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 76:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_int64.append(sub.int64_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_int64.append(reader.int64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 77:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_uint32.append(sub.uint32_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_uint32.append(reader.uint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 78:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_uint64.append(sub.varint())
                elif wire_type == WIRE_VARINT:
                    self.packed_uint64.append(reader.varint())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 79:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_sint32.append(sub.sint32_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_sint32.append(reader.sint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 80:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_sint64.append(sub.sint64_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_sint64.append(reader.sint64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 81:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_fixed32.append(sub.fixed32())
                elif wire_type == WIRE_FIXED32:
                    self.packed_fixed32.append(reader.fixed32())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 82:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_fixed64.append(sub.fixed64())
                elif wire_type == WIRE_FIXED64:
                    self.packed_fixed64.append(reader.fixed64())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 83:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_sfixed32.append(sub.sfixed32_value())
                elif wire_type == WIRE_FIXED32:
                    self.packed_sfixed32.append(reader.sfixed32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 84:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_sfixed64.append(sub.sfixed64_value())
                elif wire_type == WIRE_FIXED64:
                    self.packed_sfixed64.append(reader.sfixed64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 85:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_float.append(sub.float_value())
                elif wire_type == WIRE_FIXED32:
                    self.packed_float.append(reader.float_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 86:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_double.append(sub.double_value())
                elif wire_type == WIRE_FIXED64:
                    self.packed_double.append(reader.double_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 87:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_bool.append(sub.bool_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_bool.append(reader.bool_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 88:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_nested_enum.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_nested_enum.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 89:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_int32.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_int32.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 90:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_int64.append(sub.int64_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_int64.append(reader.int64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 91:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_uint32.append(sub.uint32_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_uint32.append(reader.uint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 92:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_uint64.append(sub.varint())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_uint64.append(reader.varint())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 93:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_sint32.append(sub.sint32_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_sint32.append(reader.sint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 94:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_sint64.append(sub.sint64_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_sint64.append(reader.sint64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 95:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_fixed32.append(sub.fixed32())
                elif wire_type == WIRE_FIXED32:
                    self.unpacked_fixed32.append(reader.fixed32())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 96:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_fixed64.append(sub.fixed64())
                elif wire_type == WIRE_FIXED64:
                    self.unpacked_fixed64.append(reader.fixed64())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 97:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_sfixed32.append(sub.sfixed32_value())
                elif wire_type == WIRE_FIXED32:
                    self.unpacked_sfixed32.append(reader.sfixed32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 98:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_sfixed64.append(sub.sfixed64_value())
                elif wire_type == WIRE_FIXED64:
                    self.unpacked_sfixed64.append(reader.sfixed64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 99:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_float.append(sub.float_value())
                elif wire_type == WIRE_FIXED32:
                    self.unpacked_float.append(reader.float_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 100:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_double.append(sub.double_value())
                elif wire_type == WIRE_FIXED64:
                    self.unpacked_double.append(reader.double_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 101:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_bool.append(sub.bool_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_bool.append(reader.bool_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 102:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_nested_enum.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_nested_enum.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 111:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_uint32 = reader.uint32_value()
                    self.oneof_field_case = 111
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 112:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: TestAllTypesProto3_NestedMessage
                    if self.oneof_field_case == 112 and len(self.oneof_nested_message) != 0:
                        m = self.oneof_nested_message.pop()
                    else:
                        m = TestAllTypesProto3_NestedMessage()
                    m.merge_from(sub)
                    self.oneof_nested_message.append(m^)
                    self.oneof_field_case = 112
                    self.oneof_uint32 = 0
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 113:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_string = reader.string_value()
                    self.oneof_field_case = 113
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 114:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_bytes = reader.bytes_value()
                    self.oneof_field_case = 114
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 115:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_bool = reader.bool_value()
                    self.oneof_field_case = 115
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 116:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_uint64 = reader.varint()
                    self.oneof_field_case = 116
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 117:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_float = reader.float_value()
                    self.oneof_field_case = 117
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 118:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_double = reader.double_value()
                    self.oneof_field_case = 118
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 119:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_enum = reader.int32_value()
                    self.oneof_field_case = 119
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_null_value = 0
            elif field == 120:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_null_value = reader.int32_value()
                    self.oneof_field_case = 120
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
            elif field == 201:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: BoolValue
                    if self.optional_bool_wrapper:
                        m = self.optional_bool_wrapper.take()
                    else:
                        m = BoolValue()
                    m.merge_from(sub)
                    self.optional_bool_wrapper = m^
            elif field == 202:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Int32Value
                    if self.optional_int32_wrapper:
                        m = self.optional_int32_wrapper.take()
                    else:
                        m = Int32Value()
                    m.merge_from(sub)
                    self.optional_int32_wrapper = m^
            elif field == 203:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Int64Value
                    if self.optional_int64_wrapper:
                        m = self.optional_int64_wrapper.take()
                    else:
                        m = Int64Value()
                    m.merge_from(sub)
                    self.optional_int64_wrapper = m^
            elif field == 204:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: UInt32Value
                    if self.optional_uint32_wrapper:
                        m = self.optional_uint32_wrapper.take()
                    else:
                        m = UInt32Value()
                    m.merge_from(sub)
                    self.optional_uint32_wrapper = m^
            elif field == 205:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: UInt64Value
                    if self.optional_uint64_wrapper:
                        m = self.optional_uint64_wrapper.take()
                    else:
                        m = UInt64Value()
                    m.merge_from(sub)
                    self.optional_uint64_wrapper = m^
            elif field == 206:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: FloatValue
                    if self.optional_float_wrapper:
                        m = self.optional_float_wrapper.take()
                    else:
                        m = FloatValue()
                    m.merge_from(sub)
                    self.optional_float_wrapper = m^
            elif field == 207:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: DoubleValue
                    if self.optional_double_wrapper:
                        m = self.optional_double_wrapper.take()
                    else:
                        m = DoubleValue()
                    m.merge_from(sub)
                    self.optional_double_wrapper = m^
            elif field == 208:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: StringValue
                    if self.optional_string_wrapper:
                        m = self.optional_string_wrapper.take()
                    else:
                        m = StringValue()
                    m.merge_from(sub)
                    self.optional_string_wrapper = m^
            elif field == 209:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: BytesValue
                    if self.optional_bytes_wrapper:
                        m = self.optional_bytes_wrapper.take()
                    else:
                        m = BytesValue()
                    m.merge_from(sub)
                    self.optional_bytes_wrapper = m^
            elif field == 211:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = BoolValue()
                    m.merge_from(sub)
                    self.repeated_bool_wrapper.append(m^)
            elif field == 212:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Int32Value()
                    m.merge_from(sub)
                    self.repeated_int32_wrapper.append(m^)
            elif field == 213:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Int64Value()
                    m.merge_from(sub)
                    self.repeated_int64_wrapper.append(m^)
            elif field == 214:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = UInt32Value()
                    m.merge_from(sub)
                    self.repeated_uint32_wrapper.append(m^)
            elif field == 215:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = UInt64Value()
                    m.merge_from(sub)
                    self.repeated_uint64_wrapper.append(m^)
            elif field == 216:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = FloatValue()
                    m.merge_from(sub)
                    self.repeated_float_wrapper.append(m^)
            elif field == 217:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = DoubleValue()
                    m.merge_from(sub)
                    self.repeated_double_wrapper.append(m^)
            elif field == 218:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = StringValue()
                    m.merge_from(sub)
                    self.repeated_string_wrapper.append(m^)
            elif field == 219:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = BytesValue()
                    m.merge_from(sub)
                    self.repeated_bytes_wrapper.append(m^)
            elif field == 301:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Duration
                    if self.optional_duration:
                        m = self.optional_duration.take()
                    else:
                        m = Duration()
                    m.merge_from(sub)
                    self.optional_duration = m^
            elif field == 302:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Timestamp
                    if self.optional_timestamp:
                        m = self.optional_timestamp.take()
                    else:
                        m = Timestamp()
                    m.merge_from(sub)
                    self.optional_timestamp = m^
            elif field == 303:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: FieldMask
                    if self.optional_field_mask:
                        m = self.optional_field_mask.take()
                    else:
                        m = FieldMask()
                    m.merge_from(sub)
                    self.optional_field_mask = m^
            elif field == 304:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Struct
                    if self.optional_struct:
                        m = self.optional_struct.take()
                    else:
                        m = Struct()
                    m.merge_from(sub)
                    self.optional_struct = m^
            elif field == 305:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Any
                    if self.optional_any:
                        m = self.optional_any.take()
                    else:
                        m = Any()
                    m.merge_from(sub)
                    self.optional_any = m^
            elif field == 306:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Value
                    if self.optional_value:
                        m = self.optional_value.take()
                    else:
                        m = Value()
                    m.merge_from(sub)
                    self.optional_value = m^
            elif field == 307:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_null_value = reader.int32_value()
            elif field == 311:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Duration()
                    m.merge_from(sub)
                    self.repeated_duration.append(m^)
            elif field == 312:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Timestamp()
                    m.merge_from(sub)
                    self.repeated_timestamp.append(m^)
            elif field == 313:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = FieldMask()
                    m.merge_from(sub)
                    self.repeated_fieldmask.append(m^)
            elif field == 315:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Any()
                    m.merge_from(sub)
                    self.repeated_any.append(m^)
            elif field == 316:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Value()
                    m.merge_from(sub)
                    self.repeated_value.append(m^)
            elif field == 317:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = ListValue()
                    m.merge_from(sub)
                    self.repeated_list_value.append(m^)
            elif field == 324:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Struct()
                    m.merge_from(sub)
                    self.repeated_struct.append(m^)
            elif field == 401:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.fieldname1 = reader.int32_value()
            elif field == 402:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field_name2 = reader.int32_value()
            elif field == 403:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self._field_name3 = reader.int32_value()
            elif field == 404:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field__name4_ = reader.int32_value()
            elif field == 405:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field0name5 = reader.int32_value()
            elif field == 406:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field_0_name6 = reader.int32_value()
            elif field == 407:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.fieldName7 = reader.int32_value()
            elif field == 408:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.FieldName8 = reader.int32_value()
            elif field == 409:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field_Name9 = reader.int32_value()
            elif field == 410:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.Field_Name10 = reader.int32_value()
            elif field == 411:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.FIELD_NAME11 = reader.int32_value()
            elif field == 412:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.FIELD_name12 = reader.int32_value()
            elif field == 413:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.__field_name13 = reader.int32_value()
            elif field == 414:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.__Field_name14 = reader.int32_value()
            elif field == 415:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field__name15 = reader.int32_value()
            elif field == 416:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field__Name16 = reader.int32_value()
            elif field == 417:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field_name17__ = reader.int32_value()
            elif field == 418:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.Field_name18__ = reader.int32_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.optional_int32 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalInt32", "optional_int32")
            writer.int32_value(self.optional_int32)
        if self.optional_int64 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalInt64", "optional_int64")
            writer.int64_value(self.optional_int64)
        if self.optional_uint32 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalUint32", "optional_uint32")
            writer.uint32_value(self.optional_uint32)
        if self.optional_uint64 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalUint64", "optional_uint64")
            writer.uint64_value(self.optional_uint64)
        if self.optional_sint32 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalSint32", "optional_sint32")
            writer.int32_value(self.optional_sint32)
        if self.optional_sint64 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalSint64", "optional_sint64")
            writer.int64_value(self.optional_sint64)
        if self.optional_fixed32 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalFixed32", "optional_fixed32")
            writer.uint32_value(self.optional_fixed32)
        if self.optional_fixed64 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalFixed64", "optional_fixed64")
            writer.uint64_value(self.optional_fixed64)
        if self.optional_sfixed32 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalSfixed32", "optional_sfixed32")
            writer.int32_value(self.optional_sfixed32)
        if self.optional_sfixed64 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalSfixed64", "optional_sfixed64")
            writer.int64_value(self.optional_sfixed64)
        if UInt32(self.optional_float.to_bits()) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalFloat", "optional_float")
            writer.float32_value(self.optional_float)
        if UInt64(self.optional_double.to_bits()) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalDouble", "optional_double")
            writer.float64_value(self.optional_double)
        if self.optional_bool or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalBool", "optional_bool")
            writer.bool_value(self.optional_bool)
        if self.optional_string.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalString", "optional_string")
            writer.string_value(self.optional_string)
        if len(self.optional_bytes) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalBytes", "optional_bytes")
            writer.bytes_value(Span(self.optional_bytes))
        if len(self.optional_nested_message) != 0:
            writer.field("optionalNestedMessage", "optional_nested_message")
            writer.message_value(self.optional_nested_message[0])
        if self.optional_foreign_message:
            writer.field("optionalForeignMessage", "optional_foreign_message")
            writer.message_value(self.optional_foreign_message.value())
        if self.optional_nested_enum != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalNestedEnum", "optional_nested_enum")
            if self.optional_nested_enum == 0:
                writer.string_value("FOO")
            elif self.optional_nested_enum == 1:
                writer.string_value("BAR")
            elif self.optional_nested_enum == 2:
                writer.string_value("BAZ")
            elif self.optional_nested_enum == -1:
                writer.string_value("NEG")
            else:
                writer.int32_value(self.optional_nested_enum)
        if self.optional_foreign_enum != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalForeignEnum", "optional_foreign_enum")
            if self.optional_foreign_enum == 0:
                writer.string_value("FOREIGN_FOO")
            elif self.optional_foreign_enum == 1:
                writer.string_value("FOREIGN_BAR")
            elif self.optional_foreign_enum == 2:
                writer.string_value("FOREIGN_BAZ")
            else:
                writer.int32_value(self.optional_foreign_enum)
        if self.optional_aliased_enum != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalAliasedEnum", "optional_aliased_enum")
            if self.optional_aliased_enum == 0:
                writer.string_value("ALIAS_FOO")
            elif self.optional_aliased_enum == 1:
                writer.string_value("ALIAS_BAR")
            elif self.optional_aliased_enum == 2:
                writer.string_value("ALIAS_BAZ")
            else:
                writer.int32_value(self.optional_aliased_enum)
        if self.optional_string_piece.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalStringPiece", "optional_string_piece")
            writer.string_value(self.optional_string_piece)
        if self.optional_cord.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalCord", "optional_cord")
            writer.string_value(self.optional_cord)
        if len(self.recursive_message) != 0:
            writer.field("recursiveMessage", "recursive_message")
            writer.message_value(self.recursive_message[0])
        if len(self.repeated_int32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedInt32", "repeated_int32")
            writer.begin_array()
            for item in self.repeated_int32:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.repeated_int64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedInt64", "repeated_int64")
            writer.begin_array()
            for item in self.repeated_int64:
                writer.array_item()
                writer.int64_value(item)
            writer.end_array()
        if len(self.repeated_uint32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedUint32", "repeated_uint32")
            writer.begin_array()
            for item in self.repeated_uint32:
                writer.array_item()
                writer.uint32_value(item)
            writer.end_array()
        if len(self.repeated_uint64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedUint64", "repeated_uint64")
            writer.begin_array()
            for item in self.repeated_uint64:
                writer.array_item()
                writer.uint64_value(item)
            writer.end_array()
        if len(self.repeated_sint32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedSint32", "repeated_sint32")
            writer.begin_array()
            for item in self.repeated_sint32:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.repeated_sint64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedSint64", "repeated_sint64")
            writer.begin_array()
            for item in self.repeated_sint64:
                writer.array_item()
                writer.int64_value(item)
            writer.end_array()
        if len(self.repeated_fixed32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedFixed32", "repeated_fixed32")
            writer.begin_array()
            for item in self.repeated_fixed32:
                writer.array_item()
                writer.uint32_value(item)
            writer.end_array()
        if len(self.repeated_fixed64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedFixed64", "repeated_fixed64")
            writer.begin_array()
            for item in self.repeated_fixed64:
                writer.array_item()
                writer.uint64_value(item)
            writer.end_array()
        if len(self.repeated_sfixed32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedSfixed32", "repeated_sfixed32")
            writer.begin_array()
            for item in self.repeated_sfixed32:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.repeated_sfixed64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedSfixed64", "repeated_sfixed64")
            writer.begin_array()
            for item in self.repeated_sfixed64:
                writer.array_item()
                writer.int64_value(item)
            writer.end_array()
        if len(self.repeated_float) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedFloat", "repeated_float")
            writer.begin_array()
            for item in self.repeated_float:
                writer.array_item()
                writer.float32_value(item)
            writer.end_array()
        if len(self.repeated_double) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedDouble", "repeated_double")
            writer.begin_array()
            for item in self.repeated_double:
                writer.array_item()
                writer.float64_value(item)
            writer.end_array()
        if len(self.repeated_bool) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedBool", "repeated_bool")
            writer.begin_array()
            for item in self.repeated_bool:
                writer.array_item()
                writer.bool_value(item)
            writer.end_array()
        if len(self.repeated_string) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedString", "repeated_string")
            writer.begin_array()
            for item in self.repeated_string:
                writer.array_item()
                writer.string_value(item)
            writer.end_array()
        if len(self.repeated_bytes) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedBytes", "repeated_bytes")
            writer.begin_array()
            for item in self.repeated_bytes:
                writer.array_item()
                writer.bytes_value(Span(item))
            writer.end_array()
        if len(self.repeated_nested_message) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedNestedMessage", "repeated_nested_message")
            writer.begin_array()
            for item in self.repeated_nested_message:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_foreign_message) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedForeignMessage", "repeated_foreign_message")
            writer.begin_array()
            for item in self.repeated_foreign_message:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_nested_enum) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedNestedEnum", "repeated_nested_enum")
            writer.begin_array()
            for item in self.repeated_nested_enum:
                writer.array_item()
                if item == 0:
                    writer.string_value("FOO")
                elif item == 1:
                    writer.string_value("BAR")
                elif item == 2:
                    writer.string_value("BAZ")
                elif item == -1:
                    writer.string_value("NEG")
                else:
                    writer.int32_value(item)
            writer.end_array()
        if len(self.repeated_foreign_enum) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedForeignEnum", "repeated_foreign_enum")
            writer.begin_array()
            for item in self.repeated_foreign_enum:
                writer.array_item()
                if item == 0:
                    writer.string_value("FOREIGN_FOO")
                elif item == 1:
                    writer.string_value("FOREIGN_BAR")
                elif item == 2:
                    writer.string_value("FOREIGN_BAZ")
                else:
                    writer.int32_value(item)
            writer.end_array()
        if len(self.repeated_string_piece) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedStringPiece", "repeated_string_piece")
            writer.begin_array()
            for item in self.repeated_string_piece:
                writer.array_item()
                writer.string_value(item)
            writer.end_array()
        if len(self.repeated_cord) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedCord", "repeated_cord")
            writer.begin_array()
            for item in self.repeated_cord:
                writer.array_item()
                writer.string_value(item)
            writer.end_array()
        if len(self.packed_int32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedInt32", "packed_int32")
            writer.begin_array()
            for item in self.packed_int32:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.packed_int64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedInt64", "packed_int64")
            writer.begin_array()
            for item in self.packed_int64:
                writer.array_item()
                writer.int64_value(item)
            writer.end_array()
        if len(self.packed_uint32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedUint32", "packed_uint32")
            writer.begin_array()
            for item in self.packed_uint32:
                writer.array_item()
                writer.uint32_value(item)
            writer.end_array()
        if len(self.packed_uint64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedUint64", "packed_uint64")
            writer.begin_array()
            for item in self.packed_uint64:
                writer.array_item()
                writer.uint64_value(item)
            writer.end_array()
        if len(self.packed_sint32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedSint32", "packed_sint32")
            writer.begin_array()
            for item in self.packed_sint32:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.packed_sint64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedSint64", "packed_sint64")
            writer.begin_array()
            for item in self.packed_sint64:
                writer.array_item()
                writer.int64_value(item)
            writer.end_array()
        if len(self.packed_fixed32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedFixed32", "packed_fixed32")
            writer.begin_array()
            for item in self.packed_fixed32:
                writer.array_item()
                writer.uint32_value(item)
            writer.end_array()
        if len(self.packed_fixed64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedFixed64", "packed_fixed64")
            writer.begin_array()
            for item in self.packed_fixed64:
                writer.array_item()
                writer.uint64_value(item)
            writer.end_array()
        if len(self.packed_sfixed32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedSfixed32", "packed_sfixed32")
            writer.begin_array()
            for item in self.packed_sfixed32:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.packed_sfixed64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedSfixed64", "packed_sfixed64")
            writer.begin_array()
            for item in self.packed_sfixed64:
                writer.array_item()
                writer.int64_value(item)
            writer.end_array()
        if len(self.packed_float) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedFloat", "packed_float")
            writer.begin_array()
            for item in self.packed_float:
                writer.array_item()
                writer.float32_value(item)
            writer.end_array()
        if len(self.packed_double) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedDouble", "packed_double")
            writer.begin_array()
            for item in self.packed_double:
                writer.array_item()
                writer.float64_value(item)
            writer.end_array()
        if len(self.packed_bool) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedBool", "packed_bool")
            writer.begin_array()
            for item in self.packed_bool:
                writer.array_item()
                writer.bool_value(item)
            writer.end_array()
        if len(self.packed_nested_enum) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedNestedEnum", "packed_nested_enum")
            writer.begin_array()
            for item in self.packed_nested_enum:
                writer.array_item()
                if item == 0:
                    writer.string_value("FOO")
                elif item == 1:
                    writer.string_value("BAR")
                elif item == 2:
                    writer.string_value("BAZ")
                elif item == -1:
                    writer.string_value("NEG")
                else:
                    writer.int32_value(item)
            writer.end_array()
        if len(self.unpacked_int32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedInt32", "unpacked_int32")
            writer.begin_array()
            for item in self.unpacked_int32:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.unpacked_int64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedInt64", "unpacked_int64")
            writer.begin_array()
            for item in self.unpacked_int64:
                writer.array_item()
                writer.int64_value(item)
            writer.end_array()
        if len(self.unpacked_uint32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedUint32", "unpacked_uint32")
            writer.begin_array()
            for item in self.unpacked_uint32:
                writer.array_item()
                writer.uint32_value(item)
            writer.end_array()
        if len(self.unpacked_uint64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedUint64", "unpacked_uint64")
            writer.begin_array()
            for item in self.unpacked_uint64:
                writer.array_item()
                writer.uint64_value(item)
            writer.end_array()
        if len(self.unpacked_sint32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedSint32", "unpacked_sint32")
            writer.begin_array()
            for item in self.unpacked_sint32:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.unpacked_sint64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedSint64", "unpacked_sint64")
            writer.begin_array()
            for item in self.unpacked_sint64:
                writer.array_item()
                writer.int64_value(item)
            writer.end_array()
        if len(self.unpacked_fixed32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedFixed32", "unpacked_fixed32")
            writer.begin_array()
            for item in self.unpacked_fixed32:
                writer.array_item()
                writer.uint32_value(item)
            writer.end_array()
        if len(self.unpacked_fixed64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedFixed64", "unpacked_fixed64")
            writer.begin_array()
            for item in self.unpacked_fixed64:
                writer.array_item()
                writer.uint64_value(item)
            writer.end_array()
        if len(self.unpacked_sfixed32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedSfixed32", "unpacked_sfixed32")
            writer.begin_array()
            for item in self.unpacked_sfixed32:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.unpacked_sfixed64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedSfixed64", "unpacked_sfixed64")
            writer.begin_array()
            for item in self.unpacked_sfixed64:
                writer.array_item()
                writer.int64_value(item)
            writer.end_array()
        if len(self.unpacked_float) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedFloat", "unpacked_float")
            writer.begin_array()
            for item in self.unpacked_float:
                writer.array_item()
                writer.float32_value(item)
            writer.end_array()
        if len(self.unpacked_double) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedDouble", "unpacked_double")
            writer.begin_array()
            for item in self.unpacked_double:
                writer.array_item()
                writer.float64_value(item)
            writer.end_array()
        if len(self.unpacked_bool) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedBool", "unpacked_bool")
            writer.begin_array()
            for item in self.unpacked_bool:
                writer.array_item()
                writer.bool_value(item)
            writer.end_array()
        if len(self.unpacked_nested_enum) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("unpackedNestedEnum", "unpacked_nested_enum")
            writer.begin_array()
            for item in self.unpacked_nested_enum:
                writer.array_item()
                if item == 0:
                    writer.string_value("FOO")
                elif item == 1:
                    writer.string_value("BAR")
                elif item == 2:
                    writer.string_value("BAZ")
                elif item == -1:
                    writer.string_value("NEG")
                else:
                    writer.int32_value(item)
            writer.end_array()
        if len(self.map_int32_int32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapInt32Int32", "map_int32_int32")
            writer.begin_map()
            for entry in self.map_int32_int32.items():
                writer.map_key(String(entry.key))
                writer.int32_value(entry.value)
            writer.end_map()
        if len(self.map_int64_int64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapInt64Int64", "map_int64_int64")
            writer.begin_map()
            for entry in self.map_int64_int64.items():
                writer.map_key(String(entry.key))
                writer.int64_value(entry.value)
            writer.end_map()
        if len(self.map_uint32_uint32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapUint32Uint32", "map_uint32_uint32")
            writer.begin_map()
            for entry in self.map_uint32_uint32.items():
                writer.map_key(String(entry.key))
                writer.uint32_value(entry.value)
            writer.end_map()
        if len(self.map_uint64_uint64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapUint64Uint64", "map_uint64_uint64")
            writer.begin_map()
            for entry in self.map_uint64_uint64.items():
                writer.map_key(String(entry.key))
                writer.uint64_value(entry.value)
            writer.end_map()
        if len(self.map_sint32_sint32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapSint32Sint32", "map_sint32_sint32")
            writer.begin_map()
            for entry in self.map_sint32_sint32.items():
                writer.map_key(String(entry.key))
                writer.int32_value(entry.value)
            writer.end_map()
        if len(self.map_sint64_sint64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapSint64Sint64", "map_sint64_sint64")
            writer.begin_map()
            for entry in self.map_sint64_sint64.items():
                writer.map_key(String(entry.key))
                writer.int64_value(entry.value)
            writer.end_map()
        if len(self.map_fixed32_fixed32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapFixed32Fixed32", "map_fixed32_fixed32")
            writer.begin_map()
            for entry in self.map_fixed32_fixed32.items():
                writer.map_key(String(entry.key))
                writer.uint32_value(entry.value)
            writer.end_map()
        if len(self.map_fixed64_fixed64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapFixed64Fixed64", "map_fixed64_fixed64")
            writer.begin_map()
            for entry in self.map_fixed64_fixed64.items():
                writer.map_key(String(entry.key))
                writer.uint64_value(entry.value)
            writer.end_map()
        if len(self.map_sfixed32_sfixed32) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapSfixed32Sfixed32", "map_sfixed32_sfixed32")
            writer.begin_map()
            for entry in self.map_sfixed32_sfixed32.items():
                writer.map_key(String(entry.key))
                writer.int32_value(entry.value)
            writer.end_map()
        if len(self.map_sfixed64_sfixed64) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapSfixed64Sfixed64", "map_sfixed64_sfixed64")
            writer.begin_map()
            for entry in self.map_sfixed64_sfixed64.items():
                writer.map_key(String(entry.key))
                writer.int64_value(entry.value)
            writer.end_map()
        if len(self.map_int32_float) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapInt32Float", "map_int32_float")
            writer.begin_map()
            for entry in self.map_int32_float.items():
                writer.map_key(String(entry.key))
                writer.float32_value(entry.value)
            writer.end_map()
        if len(self.map_int32_double) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapInt32Double", "map_int32_double")
            writer.begin_map()
            for entry in self.map_int32_double.items():
                writer.map_key(String(entry.key))
                writer.float64_value(entry.value)
            writer.end_map()
        if len(self.map_bool_bool) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapBoolBool", "map_bool_bool")
            writer.begin_map()
            for entry in self.map_bool_bool.items():
                writer.map_key("true" if entry.key else "false")
                writer.bool_value(entry.value)
            writer.end_map()
        if len(self.map_string_string) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapStringString", "map_string_string")
            writer.begin_map()
            for entry in self.map_string_string.items():
                writer.map_key(entry.key)
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.map_string_bytes) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapStringBytes", "map_string_bytes")
            writer.begin_map()
            for entry in self.map_string_bytes.items():
                writer.map_key(entry.key)
                writer.bytes_value(Span(entry.value))
            writer.end_map()
        if len(self.map_string_nested_message) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapStringNestedMessage", "map_string_nested_message")
            writer.begin_map()
            for entry in self.map_string_nested_message.items():
                writer.map_key(entry.key)
                writer.message_value(entry.value)
            writer.end_map()
        if len(self.map_string_foreign_message) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapStringForeignMessage", "map_string_foreign_message")
            writer.begin_map()
            for entry in self.map_string_foreign_message.items():
                writer.map_key(entry.key)
                writer.message_value(entry.value)
            writer.end_map()
        if len(self.map_string_nested_enum) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapStringNestedEnum", "map_string_nested_enum")
            writer.begin_map()
            for entry in self.map_string_nested_enum.items():
                writer.map_key(entry.key)
                if entry.value == 0:
                    writer.string_value("FOO")
                elif entry.value == 1:
                    writer.string_value("BAR")
                elif entry.value == 2:
                    writer.string_value("BAZ")
                elif entry.value == -1:
                    writer.string_value("NEG")
                else:
                    writer.int32_value(entry.value)
            writer.end_map()
        if len(self.map_string_foreign_enum) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("mapStringForeignEnum", "map_string_foreign_enum")
            writer.begin_map()
            for entry in self.map_string_foreign_enum.items():
                writer.map_key(entry.key)
                if entry.value == 0:
                    writer.string_value("FOREIGN_FOO")
                elif entry.value == 1:
                    writer.string_value("FOREIGN_BAR")
                elif entry.value == 2:
                    writer.string_value("FOREIGN_BAZ")
                else:
                    writer.int32_value(entry.value)
            writer.end_map()
        if self.oneof_field_case == 111:
            writer.field("oneofUint32", "oneof_uint32")
            writer.uint32_value(self.oneof_uint32)
        if self.oneof_field_case == 112:
            writer.field("oneofNestedMessage", "oneof_nested_message")
            writer.message_value(self.oneof_nested_message[0])
        if self.oneof_field_case == 113:
            writer.field("oneofString", "oneof_string")
            writer.string_value(self.oneof_string)
        if self.oneof_field_case == 114:
            writer.field("oneofBytes", "oneof_bytes")
            writer.bytes_value(Span(self.oneof_bytes))
        if self.oneof_field_case == 115:
            writer.field("oneofBool", "oneof_bool")
            writer.bool_value(self.oneof_bool)
        if self.oneof_field_case == 116:
            writer.field("oneofUint64", "oneof_uint64")
            writer.uint64_value(self.oneof_uint64)
        if self.oneof_field_case == 117:
            writer.field("oneofFloat", "oneof_float")
            writer.float32_value(self.oneof_float)
        if self.oneof_field_case == 118:
            writer.field("oneofDouble", "oneof_double")
            writer.float64_value(self.oneof_double)
        if self.oneof_field_case == 119:
            writer.field("oneofEnum", "oneof_enum")
            if self.oneof_enum == 0:
                writer.string_value("FOO")
            elif self.oneof_enum == 1:
                writer.string_value("BAR")
            elif self.oneof_enum == 2:
                writer.string_value("BAZ")
            elif self.oneof_enum == -1:
                writer.string_value("NEG")
            else:
                writer.int32_value(self.oneof_enum)
        if self.oneof_field_case == 120:
            writer.field("oneofNullValue", "oneof_null_value")
            _ = self.oneof_null_value
            writer.null_value()
        if self.optional_bool_wrapper:
            writer.field("optionalBoolWrapper", "optional_bool_wrapper")
            writer.message_value(self.optional_bool_wrapper.value())
        if self.optional_int32_wrapper:
            writer.field("optionalInt32Wrapper", "optional_int32_wrapper")
            writer.message_value(self.optional_int32_wrapper.value())
        if self.optional_int64_wrapper:
            writer.field("optionalInt64Wrapper", "optional_int64_wrapper")
            writer.message_value(self.optional_int64_wrapper.value())
        if self.optional_uint32_wrapper:
            writer.field("optionalUint32Wrapper", "optional_uint32_wrapper")
            writer.message_value(self.optional_uint32_wrapper.value())
        if self.optional_uint64_wrapper:
            writer.field("optionalUint64Wrapper", "optional_uint64_wrapper")
            writer.message_value(self.optional_uint64_wrapper.value())
        if self.optional_float_wrapper:
            writer.field("optionalFloatWrapper", "optional_float_wrapper")
            writer.message_value(self.optional_float_wrapper.value())
        if self.optional_double_wrapper:
            writer.field("optionalDoubleWrapper", "optional_double_wrapper")
            writer.message_value(self.optional_double_wrapper.value())
        if self.optional_string_wrapper:
            writer.field("optionalStringWrapper", "optional_string_wrapper")
            writer.message_value(self.optional_string_wrapper.value())
        if self.optional_bytes_wrapper:
            writer.field("optionalBytesWrapper", "optional_bytes_wrapper")
            writer.message_value(self.optional_bytes_wrapper.value())
        if len(self.repeated_bool_wrapper) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedBoolWrapper", "repeated_bool_wrapper")
            writer.begin_array()
            for item in self.repeated_bool_wrapper:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_int32_wrapper) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedInt32Wrapper", "repeated_int32_wrapper")
            writer.begin_array()
            for item in self.repeated_int32_wrapper:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_int64_wrapper) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedInt64Wrapper", "repeated_int64_wrapper")
            writer.begin_array()
            for item in self.repeated_int64_wrapper:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_uint32_wrapper) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedUint32Wrapper", "repeated_uint32_wrapper")
            writer.begin_array()
            for item in self.repeated_uint32_wrapper:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_uint64_wrapper) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedUint64Wrapper", "repeated_uint64_wrapper")
            writer.begin_array()
            for item in self.repeated_uint64_wrapper:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_float_wrapper) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedFloatWrapper", "repeated_float_wrapper")
            writer.begin_array()
            for item in self.repeated_float_wrapper:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_double_wrapper) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedDoubleWrapper", "repeated_double_wrapper")
            writer.begin_array()
            for item in self.repeated_double_wrapper:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_string_wrapper) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedStringWrapper", "repeated_string_wrapper")
            writer.begin_array()
            for item in self.repeated_string_wrapper:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_bytes_wrapper) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedBytesWrapper", "repeated_bytes_wrapper")
            writer.begin_array()
            for item in self.repeated_bytes_wrapper:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if self.optional_duration:
            writer.field("optionalDuration", "optional_duration")
            writer.message_value(self.optional_duration.value())
        if self.optional_timestamp:
            writer.field("optionalTimestamp", "optional_timestamp")
            writer.message_value(self.optional_timestamp.value())
        if self.optional_field_mask:
            writer.field("optionalFieldMask", "optional_field_mask")
            writer.message_value(self.optional_field_mask.value())
        if self.optional_struct:
            writer.field("optionalStruct", "optional_struct")
            writer.message_value(self.optional_struct.value())
        if self.optional_any:
            writer.field("optionalAny", "optional_any")
            writer.message_value(self.optional_any.value())
        if self.optional_value:
            writer.field("optionalValue", "optional_value")
            writer.message_value(self.optional_value.value())
        if self.optional_null_value != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("optionalNullValue", "optional_null_value")
            _ = self.optional_null_value
            writer.null_value()
        if len(self.repeated_duration) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedDuration", "repeated_duration")
            writer.begin_array()
            for item in self.repeated_duration:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_timestamp) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedTimestamp", "repeated_timestamp")
            writer.begin_array()
            for item in self.repeated_timestamp:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_fieldmask) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedFieldmask", "repeated_fieldmask")
            writer.begin_array()
            for item in self.repeated_fieldmask:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_struct) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedStruct", "repeated_struct")
            writer.begin_array()
            for item in self.repeated_struct:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_any) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedAny", "repeated_any")
            writer.begin_array()
            for item in self.repeated_any:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_value) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedValue", "repeated_value")
            writer.begin_array()
            for item in self.repeated_value:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.repeated_list_value) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("repeatedListValue", "repeated_list_value")
            writer.begin_array()
            for item in self.repeated_list_value:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if self.fieldname1 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fieldname1", "fieldname1")
            writer.int32_value(self.fieldname1)
        if self.field_name2 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fieldName2", "field_name2")
            writer.int32_value(self.field_name2)
        if self._field_name3 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("FieldName3", "_field_name3")
            writer.int32_value(self._field_name3)
        if self.field__name4_ != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fieldName4", "field__name4_")
            writer.int32_value(self.field__name4_)
        if self.field0name5 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("field0name5", "field0name5")
            writer.int32_value(self.field0name5)
        if self.field_0_name6 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("field0Name6", "field_0_name6")
            writer.int32_value(self.field_0_name6)
        if self.fieldName7 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fieldName7", "fieldName7")
            writer.int32_value(self.fieldName7)
        if self.FieldName8 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("FieldName8", "FieldName8")
            writer.int32_value(self.FieldName8)
        if self.field_Name9 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fieldName9", "field_Name9")
            writer.int32_value(self.field_Name9)
        if self.Field_Name10 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("FieldName10", "Field_Name10")
            writer.int32_value(self.Field_Name10)
        if self.FIELD_NAME11 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("FIELDNAME11", "FIELD_NAME11")
            writer.int32_value(self.FIELD_NAME11)
        if self.FIELD_name12 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("FIELDName12", "FIELD_name12")
            writer.int32_value(self.FIELD_name12)
        if self.__field_name13 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("FieldName13", "__field_name13")
            writer.int32_value(self.__field_name13)
        if self.__Field_name14 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("FieldName14", "__Field_name14")
            writer.int32_value(self.__Field_name14)
        if self.field__name15 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fieldName15", "field__name15")
            writer.int32_value(self.field__name15)
        if self.field__Name16 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fieldName16", "field__Name16")
            writer.int32_value(self.field__Name16)
        if self.field_name17__ != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fieldName17", "field_name17__")
            writer.int32_value(self.field_name17__)
        if self.Field_name18__ != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("FieldName18", "Field_name18__")
            writer.int32_value(self.Field_name18__)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_6 = False
        var seen_7 = False
        var seen_8 = False
        var seen_9 = False
        var seen_10 = False
        var seen_11 = False
        var seen_12 = False
        var seen_13 = False
        var seen_14 = False
        var seen_15 = False
        var seen_18 = False
        var seen_19 = False
        var seen_21 = False
        var seen_22 = False
        var seen_23 = False
        var seen_24 = False
        var seen_25 = False
        var seen_27 = False
        var seen_31 = False
        var seen_32 = False
        var seen_33 = False
        var seen_34 = False
        var seen_35 = False
        var seen_36 = False
        var seen_37 = False
        var seen_38 = False
        var seen_39 = False
        var seen_40 = False
        var seen_41 = False
        var seen_42 = False
        var seen_43 = False
        var seen_44 = False
        var seen_45 = False
        var seen_48 = False
        var seen_49 = False
        var seen_51 = False
        var seen_52 = False
        var seen_54 = False
        var seen_55 = False
        var seen_75 = False
        var seen_76 = False
        var seen_77 = False
        var seen_78 = False
        var seen_79 = False
        var seen_80 = False
        var seen_81 = False
        var seen_82 = False
        var seen_83 = False
        var seen_84 = False
        var seen_85 = False
        var seen_86 = False
        var seen_87 = False
        var seen_88 = False
        var seen_89 = False
        var seen_90 = False
        var seen_91 = False
        var seen_92 = False
        var seen_93 = False
        var seen_94 = False
        var seen_95 = False
        var seen_96 = False
        var seen_97 = False
        var seen_98 = False
        var seen_99 = False
        var seen_100 = False
        var seen_101 = False
        var seen_102 = False
        var seen_56 = False
        var seen_57 = False
        var seen_58 = False
        var seen_59 = False
        var seen_60 = False
        var seen_61 = False
        var seen_62 = False
        var seen_63 = False
        var seen_64 = False
        var seen_65 = False
        var seen_66 = False
        var seen_67 = False
        var seen_68 = False
        var seen_69 = False
        var seen_70 = False
        var seen_71 = False
        var seen_72 = False
        var seen_73 = False
        var seen_74 = False
        var seen_111 = False
        var seen_112 = False
        var seen_113 = False
        var seen_114 = False
        var seen_115 = False
        var seen_116 = False
        var seen_117 = False
        var seen_118 = False
        var seen_119 = False
        var seen_120 = False
        var seen_201 = False
        var seen_202 = False
        var seen_203 = False
        var seen_204 = False
        var seen_205 = False
        var seen_206 = False
        var seen_207 = False
        var seen_208 = False
        var seen_209 = False
        var seen_211 = False
        var seen_212 = False
        var seen_213 = False
        var seen_214 = False
        var seen_215 = False
        var seen_216 = False
        var seen_217 = False
        var seen_218 = False
        var seen_219 = False
        var seen_301 = False
        var seen_302 = False
        var seen_303 = False
        var seen_304 = False
        var seen_305 = False
        var seen_306 = False
        var seen_307 = False
        var seen_311 = False
        var seen_312 = False
        var seen_313 = False
        var seen_324 = False
        var seen_315 = False
        var seen_316 = False
        var seen_317 = False
        var seen_401 = False
        var seen_402 = False
        var seen_403 = False
        var seen_404 = False
        var seen_405 = False
        var seen_406 = False
        var seen_407 = False
        var seen_408 = False
        var seen_409 = False
        var seen_410 = False
        var seen_411 = False
        var seen_412 = False
        var seen_413 = False
        var seen_414 = False
        var seen_415 = False
        var seen_416 = False
        var seen_417 = False
        var seen_418 = False
        var seen_oneof_0 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "optionalInt32" or field_name == "optional_int32":
                if seen_1:
                    raise Error("proto json: duplicate field optionalInt32")
                seen_1 = True
                if reader.read_null():
                    self.optional_int32 = 0
                else:
                    self.optional_int32 = reader.int32_value()
            elif field_name == "optionalInt64" or field_name == "optional_int64":
                if seen_2:
                    raise Error("proto json: duplicate field optionalInt64")
                seen_2 = True
                if reader.read_null():
                    self.optional_int64 = 0
                else:
                    self.optional_int64 = reader.int64_value()
            elif field_name == "optionalUint32" or field_name == "optional_uint32":
                if seen_3:
                    raise Error("proto json: duplicate field optionalUint32")
                seen_3 = True
                if reader.read_null():
                    self.optional_uint32 = 0
                else:
                    self.optional_uint32 = reader.uint32_value()
            elif field_name == "optionalUint64" or field_name == "optional_uint64":
                if seen_4:
                    raise Error("proto json: duplicate field optionalUint64")
                seen_4 = True
                if reader.read_null():
                    self.optional_uint64 = 0
                else:
                    self.optional_uint64 = reader.uint64_value()
            elif field_name == "optionalSint32" or field_name == "optional_sint32":
                if seen_5:
                    raise Error("proto json: duplicate field optionalSint32")
                seen_5 = True
                if reader.read_null():
                    self.optional_sint32 = 0
                else:
                    self.optional_sint32 = reader.int32_value()
            elif field_name == "optionalSint64" or field_name == "optional_sint64":
                if seen_6:
                    raise Error("proto json: duplicate field optionalSint64")
                seen_6 = True
                if reader.read_null():
                    self.optional_sint64 = 0
                else:
                    self.optional_sint64 = reader.int64_value()
            elif field_name == "optionalFixed32" or field_name == "optional_fixed32":
                if seen_7:
                    raise Error("proto json: duplicate field optionalFixed32")
                seen_7 = True
                if reader.read_null():
                    self.optional_fixed32 = 0
                else:
                    self.optional_fixed32 = reader.uint32_value()
            elif field_name == "optionalFixed64" or field_name == "optional_fixed64":
                if seen_8:
                    raise Error("proto json: duplicate field optionalFixed64")
                seen_8 = True
                if reader.read_null():
                    self.optional_fixed64 = 0
                else:
                    self.optional_fixed64 = reader.uint64_value()
            elif field_name == "optionalSfixed32" or field_name == "optional_sfixed32":
                if seen_9:
                    raise Error("proto json: duplicate field optionalSfixed32")
                seen_9 = True
                if reader.read_null():
                    self.optional_sfixed32 = 0
                else:
                    self.optional_sfixed32 = reader.int32_value()
            elif field_name == "optionalSfixed64" or field_name == "optional_sfixed64":
                if seen_10:
                    raise Error("proto json: duplicate field optionalSfixed64")
                seen_10 = True
                if reader.read_null():
                    self.optional_sfixed64 = 0
                else:
                    self.optional_sfixed64 = reader.int64_value()
            elif field_name == "optionalFloat" or field_name == "optional_float":
                if seen_11:
                    raise Error("proto json: duplicate field optionalFloat")
                seen_11 = True
                if reader.read_null():
                    self.optional_float = 0.0
                else:
                    self.optional_float = reader.float32_value()
            elif field_name == "optionalDouble" or field_name == "optional_double":
                if seen_12:
                    raise Error("proto json: duplicate field optionalDouble")
                seen_12 = True
                if reader.read_null():
                    self.optional_double = 0.0
                else:
                    self.optional_double = reader.float64_value()
            elif field_name == "optionalBool" or field_name == "optional_bool":
                if seen_13:
                    raise Error("proto json: duplicate field optionalBool")
                seen_13 = True
                if reader.read_null():
                    self.optional_bool = False
                else:
                    self.optional_bool = reader.bool_value()
            elif field_name == "optionalString" or field_name == "optional_string":
                if seen_14:
                    raise Error("proto json: duplicate field optionalString")
                seen_14 = True
                if reader.read_null():
                    self.optional_string = String()
                else:
                    self.optional_string = reader.string_value()
            elif field_name == "optionalBytes" or field_name == "optional_bytes":
                if seen_15:
                    raise Error("proto json: duplicate field optionalBytes")
                seen_15 = True
                if reader.read_null():
                    self.optional_bytes = List[Byte]()
                else:
                    self.optional_bytes = reader.bytes_value()
            elif field_name == "optionalNestedMessage" or field_name == "optional_nested_message":
                if seen_18:
                    raise Error("proto json: duplicate field optionalNestedMessage")
                seen_18 = True
                if reader.read_null():
                    self.optional_nested_message = List[TestAllTypesProto3_NestedMessage]()
                else:
                    self.optional_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.optional_nested_message.append(reader.message_value[TestAllTypesProto3_NestedMessage]())
            elif field_name == "optionalForeignMessage" or field_name == "optional_foreign_message":
                if seen_19:
                    raise Error("proto json: duplicate field optionalForeignMessage")
                seen_19 = True
                if reader.read_null():
                    self.optional_foreign_message = None
                else:
                    self.optional_foreign_message = reader.message_value[ForeignMessage]()
            elif field_name == "optionalNestedEnum" or field_name == "optional_nested_enum":
                if seen_21:
                    raise Error("proto json: duplicate field optionalNestedEnum")
                seen_21 = True
                if reader.read_null():
                    self.optional_nested_enum = 0
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "FOO":
                            self.optional_nested_enum = 0
                        elif enum_name.value() == "BAR":
                            self.optional_nested_enum = 1
                        elif enum_name.value() == "BAZ":
                            self.optional_nested_enum = 2
                        elif enum_name.value() == "NEG":
                            self.optional_nested_enum = -1
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.optional_nested_enum = reader.int32_value()
            elif field_name == "optionalForeignEnum" or field_name == "optional_foreign_enum":
                if seen_22:
                    raise Error("proto json: duplicate field optionalForeignEnum")
                seen_22 = True
                if reader.read_null():
                    self.optional_foreign_enum = 0
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "FOREIGN_FOO":
                            self.optional_foreign_enum = 0
                        elif enum_name.value() == "FOREIGN_BAR":
                            self.optional_foreign_enum = 1
                        elif enum_name.value() == "FOREIGN_BAZ":
                            self.optional_foreign_enum = 2
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.optional_foreign_enum = reader.int32_value()
            elif field_name == "optionalAliasedEnum" or field_name == "optional_aliased_enum":
                if seen_23:
                    raise Error("proto json: duplicate field optionalAliasedEnum")
                seen_23 = True
                if reader.read_null():
                    self.optional_aliased_enum = 0
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "ALIAS_FOO":
                            self.optional_aliased_enum = 0
                        elif enum_name.value() == "ALIAS_BAR":
                            self.optional_aliased_enum = 1
                        elif enum_name.value() == "ALIAS_BAZ":
                            self.optional_aliased_enum = 2
                        elif enum_name.value() == "MOO":
                            self.optional_aliased_enum = 2
                        elif enum_name.value() == "moo":
                            self.optional_aliased_enum = 2
                        elif enum_name.value() == "bAz":
                            self.optional_aliased_enum = 2
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.optional_aliased_enum = reader.int32_value()
            elif field_name == "optionalStringPiece" or field_name == "optional_string_piece":
                if seen_24:
                    raise Error("proto json: duplicate field optionalStringPiece")
                seen_24 = True
                if reader.read_null():
                    self.optional_string_piece = String()
                else:
                    self.optional_string_piece = reader.string_value()
            elif field_name == "optionalCord" or field_name == "optional_cord":
                if seen_25:
                    raise Error("proto json: duplicate field optionalCord")
                seen_25 = True
                if reader.read_null():
                    self.optional_cord = String()
                else:
                    self.optional_cord = reader.string_value()
            elif field_name == "recursiveMessage" or field_name == "recursive_message":
                if seen_27:
                    raise Error("proto json: duplicate field recursiveMessage")
                seen_27 = True
                if reader.read_null():
                    self.recursive_message = List[TestAllTypesProto3]()
                else:
                    self.recursive_message = List[TestAllTypesProto3]()
                    self.recursive_message.append(reader.message_value[TestAllTypesProto3]())
            elif field_name == "repeatedInt32" or field_name == "repeated_int32":
                if seen_31:
                    raise Error("proto json: duplicate field repeatedInt32")
                seen_31 = True
                if reader.read_null():
                    self.repeated_int32 = List[Int32]()
                else:
                    self.repeated_int32 = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_int32.append(reader.int32_value())
            elif field_name == "repeatedInt64" or field_name == "repeated_int64":
                if seen_32:
                    raise Error("proto json: duplicate field repeatedInt64")
                seen_32 = True
                if reader.read_null():
                    self.repeated_int64 = List[Int64]()
                else:
                    self.repeated_int64 = List[Int64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_int64.append(reader.int64_value())
            elif field_name == "repeatedUint32" or field_name == "repeated_uint32":
                if seen_33:
                    raise Error("proto json: duplicate field repeatedUint32")
                seen_33 = True
                if reader.read_null():
                    self.repeated_uint32 = List[UInt32]()
                else:
                    self.repeated_uint32 = List[UInt32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_uint32.append(reader.uint32_value())
            elif field_name == "repeatedUint64" or field_name == "repeated_uint64":
                if seen_34:
                    raise Error("proto json: duplicate field repeatedUint64")
                seen_34 = True
                if reader.read_null():
                    self.repeated_uint64 = List[UInt64]()
                else:
                    self.repeated_uint64 = List[UInt64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_uint64.append(reader.uint64_value())
            elif field_name == "repeatedSint32" or field_name == "repeated_sint32":
                if seen_35:
                    raise Error("proto json: duplicate field repeatedSint32")
                seen_35 = True
                if reader.read_null():
                    self.repeated_sint32 = List[Int32]()
                else:
                    self.repeated_sint32 = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_sint32.append(reader.int32_value())
            elif field_name == "repeatedSint64" or field_name == "repeated_sint64":
                if seen_36:
                    raise Error("proto json: duplicate field repeatedSint64")
                seen_36 = True
                if reader.read_null():
                    self.repeated_sint64 = List[Int64]()
                else:
                    self.repeated_sint64 = List[Int64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_sint64.append(reader.int64_value())
            elif field_name == "repeatedFixed32" or field_name == "repeated_fixed32":
                if seen_37:
                    raise Error("proto json: duplicate field repeatedFixed32")
                seen_37 = True
                if reader.read_null():
                    self.repeated_fixed32 = List[UInt32]()
                else:
                    self.repeated_fixed32 = List[UInt32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_fixed32.append(reader.uint32_value())
            elif field_name == "repeatedFixed64" or field_name == "repeated_fixed64":
                if seen_38:
                    raise Error("proto json: duplicate field repeatedFixed64")
                seen_38 = True
                if reader.read_null():
                    self.repeated_fixed64 = List[UInt64]()
                else:
                    self.repeated_fixed64 = List[UInt64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_fixed64.append(reader.uint64_value())
            elif field_name == "repeatedSfixed32" or field_name == "repeated_sfixed32":
                if seen_39:
                    raise Error("proto json: duplicate field repeatedSfixed32")
                seen_39 = True
                if reader.read_null():
                    self.repeated_sfixed32 = List[Int32]()
                else:
                    self.repeated_sfixed32 = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_sfixed32.append(reader.int32_value())
            elif field_name == "repeatedSfixed64" or field_name == "repeated_sfixed64":
                if seen_40:
                    raise Error("proto json: duplicate field repeatedSfixed64")
                seen_40 = True
                if reader.read_null():
                    self.repeated_sfixed64 = List[Int64]()
                else:
                    self.repeated_sfixed64 = List[Int64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_sfixed64.append(reader.int64_value())
            elif field_name == "repeatedFloat" or field_name == "repeated_float":
                if seen_41:
                    raise Error("proto json: duplicate field repeatedFloat")
                seen_41 = True
                if reader.read_null():
                    self.repeated_float = List[Float32]()
                else:
                    self.repeated_float = List[Float32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_float.append(reader.float32_value())
            elif field_name == "repeatedDouble" or field_name == "repeated_double":
                if seen_42:
                    raise Error("proto json: duplicate field repeatedDouble")
                seen_42 = True
                if reader.read_null():
                    self.repeated_double = List[Float64]()
                else:
                    self.repeated_double = List[Float64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_double.append(reader.float64_value())
            elif field_name == "repeatedBool" or field_name == "repeated_bool":
                if seen_43:
                    raise Error("proto json: duplicate field repeatedBool")
                seen_43 = True
                if reader.read_null():
                    self.repeated_bool = List[Bool]()
                else:
                    self.repeated_bool = List[Bool]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_bool.append(reader.bool_value())
            elif field_name == "repeatedString" or field_name == "repeated_string":
                if seen_44:
                    raise Error("proto json: duplicate field repeatedString")
                seen_44 = True
                if reader.read_null():
                    self.repeated_string = List[String]()
                else:
                    self.repeated_string = List[String]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_string.append(reader.string_value())
            elif field_name == "repeatedBytes" or field_name == "repeated_bytes":
                if seen_45:
                    raise Error("proto json: duplicate field repeatedBytes")
                seen_45 = True
                if reader.read_null():
                    self.repeated_bytes = List[List[Byte]]()
                else:
                    self.repeated_bytes = List[List[Byte]]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_bytes.append(reader.bytes_value())
            elif field_name == "repeatedNestedMessage" or field_name == "repeated_nested_message":
                if seen_48:
                    raise Error("proto json: duplicate field repeatedNestedMessage")
                seen_48 = True
                if reader.read_null():
                    self.repeated_nested_message = List[TestAllTypesProto3_NestedMessage]()
                else:
                    self.repeated_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_nested_message.append(reader.message_value[TestAllTypesProto3_NestedMessage]())
            elif field_name == "repeatedForeignMessage" or field_name == "repeated_foreign_message":
                if seen_49:
                    raise Error("proto json: duplicate field repeatedForeignMessage")
                seen_49 = True
                if reader.read_null():
                    self.repeated_foreign_message = List[ForeignMessage]()
                else:
                    self.repeated_foreign_message = List[ForeignMessage]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_foreign_message.append(reader.message_value[ForeignMessage]())
            elif field_name == "repeatedNestedEnum" or field_name == "repeated_nested_enum":
                if seen_51:
                    raise Error("proto json: duplicate field repeatedNestedEnum")
                seen_51 = True
                if reader.read_null():
                    self.repeated_nested_enum = List[Int32]()
                else:
                    self.repeated_nested_enum = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        var enum_name = reader.enum_name()
                        if enum_name:
                            if enum_name.value() == "FOO":
                                self.repeated_nested_enum.append(0)
                            elif enum_name.value() == "BAR":
                                self.repeated_nested_enum.append(1)
                            elif enum_name.value() == "BAZ":
                                self.repeated_nested_enum.append(2)
                            elif enum_name.value() == "NEG":
                                self.repeated_nested_enum.append(-1)
                            elif not reader.options.ignore_unknown_fields:
                                raise Error("proto json: unknown enum value")
                        else:
                            self.repeated_nested_enum.append(reader.int32_value())
            elif field_name == "repeatedForeignEnum" or field_name == "repeated_foreign_enum":
                if seen_52:
                    raise Error("proto json: duplicate field repeatedForeignEnum")
                seen_52 = True
                if reader.read_null():
                    self.repeated_foreign_enum = List[Int32]()
                else:
                    self.repeated_foreign_enum = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        var enum_name = reader.enum_name()
                        if enum_name:
                            if enum_name.value() == "FOREIGN_FOO":
                                self.repeated_foreign_enum.append(0)
                            elif enum_name.value() == "FOREIGN_BAR":
                                self.repeated_foreign_enum.append(1)
                            elif enum_name.value() == "FOREIGN_BAZ":
                                self.repeated_foreign_enum.append(2)
                            elif not reader.options.ignore_unknown_fields:
                                raise Error("proto json: unknown enum value")
                        else:
                            self.repeated_foreign_enum.append(reader.int32_value())
            elif field_name == "repeatedStringPiece" or field_name == "repeated_string_piece":
                if seen_54:
                    raise Error("proto json: duplicate field repeatedStringPiece")
                seen_54 = True
                if reader.read_null():
                    self.repeated_string_piece = List[String]()
                else:
                    self.repeated_string_piece = List[String]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_string_piece.append(reader.string_value())
            elif field_name == "repeatedCord" or field_name == "repeated_cord":
                if seen_55:
                    raise Error("proto json: duplicate field repeatedCord")
                seen_55 = True
                if reader.read_null():
                    self.repeated_cord = List[String]()
                else:
                    self.repeated_cord = List[String]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_cord.append(reader.string_value())
            elif field_name == "packedInt32" or field_name == "packed_int32":
                if seen_75:
                    raise Error("proto json: duplicate field packedInt32")
                seen_75 = True
                if reader.read_null():
                    self.packed_int32 = List[Int32]()
                else:
                    self.packed_int32 = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_int32.append(reader.int32_value())
            elif field_name == "packedInt64" or field_name == "packed_int64":
                if seen_76:
                    raise Error("proto json: duplicate field packedInt64")
                seen_76 = True
                if reader.read_null():
                    self.packed_int64 = List[Int64]()
                else:
                    self.packed_int64 = List[Int64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_int64.append(reader.int64_value())
            elif field_name == "packedUint32" or field_name == "packed_uint32":
                if seen_77:
                    raise Error("proto json: duplicate field packedUint32")
                seen_77 = True
                if reader.read_null():
                    self.packed_uint32 = List[UInt32]()
                else:
                    self.packed_uint32 = List[UInt32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_uint32.append(reader.uint32_value())
            elif field_name == "packedUint64" or field_name == "packed_uint64":
                if seen_78:
                    raise Error("proto json: duplicate field packedUint64")
                seen_78 = True
                if reader.read_null():
                    self.packed_uint64 = List[UInt64]()
                else:
                    self.packed_uint64 = List[UInt64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_uint64.append(reader.uint64_value())
            elif field_name == "packedSint32" or field_name == "packed_sint32":
                if seen_79:
                    raise Error("proto json: duplicate field packedSint32")
                seen_79 = True
                if reader.read_null():
                    self.packed_sint32 = List[Int32]()
                else:
                    self.packed_sint32 = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_sint32.append(reader.int32_value())
            elif field_name == "packedSint64" or field_name == "packed_sint64":
                if seen_80:
                    raise Error("proto json: duplicate field packedSint64")
                seen_80 = True
                if reader.read_null():
                    self.packed_sint64 = List[Int64]()
                else:
                    self.packed_sint64 = List[Int64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_sint64.append(reader.int64_value())
            elif field_name == "packedFixed32" or field_name == "packed_fixed32":
                if seen_81:
                    raise Error("proto json: duplicate field packedFixed32")
                seen_81 = True
                if reader.read_null():
                    self.packed_fixed32 = List[UInt32]()
                else:
                    self.packed_fixed32 = List[UInt32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_fixed32.append(reader.uint32_value())
            elif field_name == "packedFixed64" or field_name == "packed_fixed64":
                if seen_82:
                    raise Error("proto json: duplicate field packedFixed64")
                seen_82 = True
                if reader.read_null():
                    self.packed_fixed64 = List[UInt64]()
                else:
                    self.packed_fixed64 = List[UInt64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_fixed64.append(reader.uint64_value())
            elif field_name == "packedSfixed32" or field_name == "packed_sfixed32":
                if seen_83:
                    raise Error("proto json: duplicate field packedSfixed32")
                seen_83 = True
                if reader.read_null():
                    self.packed_sfixed32 = List[Int32]()
                else:
                    self.packed_sfixed32 = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_sfixed32.append(reader.int32_value())
            elif field_name == "packedSfixed64" or field_name == "packed_sfixed64":
                if seen_84:
                    raise Error("proto json: duplicate field packedSfixed64")
                seen_84 = True
                if reader.read_null():
                    self.packed_sfixed64 = List[Int64]()
                else:
                    self.packed_sfixed64 = List[Int64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_sfixed64.append(reader.int64_value())
            elif field_name == "packedFloat" or field_name == "packed_float":
                if seen_85:
                    raise Error("proto json: duplicate field packedFloat")
                seen_85 = True
                if reader.read_null():
                    self.packed_float = List[Float32]()
                else:
                    self.packed_float = List[Float32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_float.append(reader.float32_value())
            elif field_name == "packedDouble" or field_name == "packed_double":
                if seen_86:
                    raise Error("proto json: duplicate field packedDouble")
                seen_86 = True
                if reader.read_null():
                    self.packed_double = List[Float64]()
                else:
                    self.packed_double = List[Float64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_double.append(reader.float64_value())
            elif field_name == "packedBool" or field_name == "packed_bool":
                if seen_87:
                    raise Error("proto json: duplicate field packedBool")
                seen_87 = True
                if reader.read_null():
                    self.packed_bool = List[Bool]()
                else:
                    self.packed_bool = List[Bool]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_bool.append(reader.bool_value())
            elif field_name == "packedNestedEnum" or field_name == "packed_nested_enum":
                if seen_88:
                    raise Error("proto json: duplicate field packedNestedEnum")
                seen_88 = True
                if reader.read_null():
                    self.packed_nested_enum = List[Int32]()
                else:
                    self.packed_nested_enum = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        var enum_name = reader.enum_name()
                        if enum_name:
                            if enum_name.value() == "FOO":
                                self.packed_nested_enum.append(0)
                            elif enum_name.value() == "BAR":
                                self.packed_nested_enum.append(1)
                            elif enum_name.value() == "BAZ":
                                self.packed_nested_enum.append(2)
                            elif enum_name.value() == "NEG":
                                self.packed_nested_enum.append(-1)
                            elif not reader.options.ignore_unknown_fields:
                                raise Error("proto json: unknown enum value")
                        else:
                            self.packed_nested_enum.append(reader.int32_value())
            elif field_name == "unpackedInt32" or field_name == "unpacked_int32":
                if seen_89:
                    raise Error("proto json: duplicate field unpackedInt32")
                seen_89 = True
                if reader.read_null():
                    self.unpacked_int32 = List[Int32]()
                else:
                    self.unpacked_int32 = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_int32.append(reader.int32_value())
            elif field_name == "unpackedInt64" or field_name == "unpacked_int64":
                if seen_90:
                    raise Error("proto json: duplicate field unpackedInt64")
                seen_90 = True
                if reader.read_null():
                    self.unpacked_int64 = List[Int64]()
                else:
                    self.unpacked_int64 = List[Int64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_int64.append(reader.int64_value())
            elif field_name == "unpackedUint32" or field_name == "unpacked_uint32":
                if seen_91:
                    raise Error("proto json: duplicate field unpackedUint32")
                seen_91 = True
                if reader.read_null():
                    self.unpacked_uint32 = List[UInt32]()
                else:
                    self.unpacked_uint32 = List[UInt32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_uint32.append(reader.uint32_value())
            elif field_name == "unpackedUint64" or field_name == "unpacked_uint64":
                if seen_92:
                    raise Error("proto json: duplicate field unpackedUint64")
                seen_92 = True
                if reader.read_null():
                    self.unpacked_uint64 = List[UInt64]()
                else:
                    self.unpacked_uint64 = List[UInt64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_uint64.append(reader.uint64_value())
            elif field_name == "unpackedSint32" or field_name == "unpacked_sint32":
                if seen_93:
                    raise Error("proto json: duplicate field unpackedSint32")
                seen_93 = True
                if reader.read_null():
                    self.unpacked_sint32 = List[Int32]()
                else:
                    self.unpacked_sint32 = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_sint32.append(reader.int32_value())
            elif field_name == "unpackedSint64" or field_name == "unpacked_sint64":
                if seen_94:
                    raise Error("proto json: duplicate field unpackedSint64")
                seen_94 = True
                if reader.read_null():
                    self.unpacked_sint64 = List[Int64]()
                else:
                    self.unpacked_sint64 = List[Int64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_sint64.append(reader.int64_value())
            elif field_name == "unpackedFixed32" or field_name == "unpacked_fixed32":
                if seen_95:
                    raise Error("proto json: duplicate field unpackedFixed32")
                seen_95 = True
                if reader.read_null():
                    self.unpacked_fixed32 = List[UInt32]()
                else:
                    self.unpacked_fixed32 = List[UInt32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_fixed32.append(reader.uint32_value())
            elif field_name == "unpackedFixed64" or field_name == "unpacked_fixed64":
                if seen_96:
                    raise Error("proto json: duplicate field unpackedFixed64")
                seen_96 = True
                if reader.read_null():
                    self.unpacked_fixed64 = List[UInt64]()
                else:
                    self.unpacked_fixed64 = List[UInt64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_fixed64.append(reader.uint64_value())
            elif field_name == "unpackedSfixed32" or field_name == "unpacked_sfixed32":
                if seen_97:
                    raise Error("proto json: duplicate field unpackedSfixed32")
                seen_97 = True
                if reader.read_null():
                    self.unpacked_sfixed32 = List[Int32]()
                else:
                    self.unpacked_sfixed32 = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_sfixed32.append(reader.int32_value())
            elif field_name == "unpackedSfixed64" or field_name == "unpacked_sfixed64":
                if seen_98:
                    raise Error("proto json: duplicate field unpackedSfixed64")
                seen_98 = True
                if reader.read_null():
                    self.unpacked_sfixed64 = List[Int64]()
                else:
                    self.unpacked_sfixed64 = List[Int64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_sfixed64.append(reader.int64_value())
            elif field_name == "unpackedFloat" or field_name == "unpacked_float":
                if seen_99:
                    raise Error("proto json: duplicate field unpackedFloat")
                seen_99 = True
                if reader.read_null():
                    self.unpacked_float = List[Float32]()
                else:
                    self.unpacked_float = List[Float32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_float.append(reader.float32_value())
            elif field_name == "unpackedDouble" or field_name == "unpacked_double":
                if seen_100:
                    raise Error("proto json: duplicate field unpackedDouble")
                seen_100 = True
                if reader.read_null():
                    self.unpacked_double = List[Float64]()
                else:
                    self.unpacked_double = List[Float64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_double.append(reader.float64_value())
            elif field_name == "unpackedBool" or field_name == "unpacked_bool":
                if seen_101:
                    raise Error("proto json: duplicate field unpackedBool")
                seen_101 = True
                if reader.read_null():
                    self.unpacked_bool = List[Bool]()
                else:
                    self.unpacked_bool = List[Bool]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.unpacked_bool.append(reader.bool_value())
            elif field_name == "unpackedNestedEnum" or field_name == "unpacked_nested_enum":
                if seen_102:
                    raise Error("proto json: duplicate field unpackedNestedEnum")
                seen_102 = True
                if reader.read_null():
                    self.unpacked_nested_enum = List[Int32]()
                else:
                    self.unpacked_nested_enum = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        var enum_name = reader.enum_name()
                        if enum_name:
                            if enum_name.value() == "FOO":
                                self.unpacked_nested_enum.append(0)
                            elif enum_name.value() == "BAR":
                                self.unpacked_nested_enum.append(1)
                            elif enum_name.value() == "BAZ":
                                self.unpacked_nested_enum.append(2)
                            elif enum_name.value() == "NEG":
                                self.unpacked_nested_enum.append(-1)
                            elif not reader.options.ignore_unknown_fields:
                                raise Error("proto json: unknown enum value")
                        else:
                            self.unpacked_nested_enum.append(reader.int32_value())
            elif field_name == "mapInt32Int32" or field_name == "map_int32_int32":
                if seen_56:
                    raise Error("proto json: duplicate field mapInt32Int32")
                seen_56 = True
                if reader.read_null():
                    self.map_int32_int32 = Dict[Int32, Int32]()
                else:
                    self.map_int32_int32 = Dict[Int32, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int32_value()
                        map_key_reader.finish()
                        self.map_int32_int32[parsed_map_key] = reader.int32_value()
            elif field_name == "mapInt64Int64" or field_name == "map_int64_int64":
                if seen_57:
                    raise Error("proto json: duplicate field mapInt64Int64")
                seen_57 = True
                if reader.read_null():
                    self.map_int64_int64 = Dict[Int64, Int64]()
                else:
                    self.map_int64_int64 = Dict[Int64, Int64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int64_value()
                        map_key_reader.finish()
                        self.map_int64_int64[parsed_map_key] = reader.int64_value()
            elif field_name == "mapUint32Uint32" or field_name == "map_uint32_uint32":
                if seen_58:
                    raise Error("proto json: duplicate field mapUint32Uint32")
                seen_58 = True
                if reader.read_null():
                    self.map_uint32_uint32 = Dict[UInt32, UInt32]()
                else:
                    self.map_uint32_uint32 = Dict[UInt32, UInt32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.uint32_value()
                        map_key_reader.finish()
                        self.map_uint32_uint32[parsed_map_key] = reader.uint32_value()
            elif field_name == "mapUint64Uint64" or field_name == "map_uint64_uint64":
                if seen_59:
                    raise Error("proto json: duplicate field mapUint64Uint64")
                seen_59 = True
                if reader.read_null():
                    self.map_uint64_uint64 = Dict[UInt64, UInt64]()
                else:
                    self.map_uint64_uint64 = Dict[UInt64, UInt64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.uint64_value()
                        map_key_reader.finish()
                        self.map_uint64_uint64[parsed_map_key] = reader.uint64_value()
            elif field_name == "mapSint32Sint32" or field_name == "map_sint32_sint32":
                if seen_60:
                    raise Error("proto json: duplicate field mapSint32Sint32")
                seen_60 = True
                if reader.read_null():
                    self.map_sint32_sint32 = Dict[Int32, Int32]()
                else:
                    self.map_sint32_sint32 = Dict[Int32, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int32_value()
                        map_key_reader.finish()
                        self.map_sint32_sint32[parsed_map_key] = reader.int32_value()
            elif field_name == "mapSint64Sint64" or field_name == "map_sint64_sint64":
                if seen_61:
                    raise Error("proto json: duplicate field mapSint64Sint64")
                seen_61 = True
                if reader.read_null():
                    self.map_sint64_sint64 = Dict[Int64, Int64]()
                else:
                    self.map_sint64_sint64 = Dict[Int64, Int64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int64_value()
                        map_key_reader.finish()
                        self.map_sint64_sint64[parsed_map_key] = reader.int64_value()
            elif field_name == "mapFixed32Fixed32" or field_name == "map_fixed32_fixed32":
                if seen_62:
                    raise Error("proto json: duplicate field mapFixed32Fixed32")
                seen_62 = True
                if reader.read_null():
                    self.map_fixed32_fixed32 = Dict[UInt32, UInt32]()
                else:
                    self.map_fixed32_fixed32 = Dict[UInt32, UInt32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.uint32_value()
                        map_key_reader.finish()
                        self.map_fixed32_fixed32[parsed_map_key] = reader.uint32_value()
            elif field_name == "mapFixed64Fixed64" or field_name == "map_fixed64_fixed64":
                if seen_63:
                    raise Error("proto json: duplicate field mapFixed64Fixed64")
                seen_63 = True
                if reader.read_null():
                    self.map_fixed64_fixed64 = Dict[UInt64, UInt64]()
                else:
                    self.map_fixed64_fixed64 = Dict[UInt64, UInt64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.uint64_value()
                        map_key_reader.finish()
                        self.map_fixed64_fixed64[parsed_map_key] = reader.uint64_value()
            elif field_name == "mapSfixed32Sfixed32" or field_name == "map_sfixed32_sfixed32":
                if seen_64:
                    raise Error("proto json: duplicate field mapSfixed32Sfixed32")
                seen_64 = True
                if reader.read_null():
                    self.map_sfixed32_sfixed32 = Dict[Int32, Int32]()
                else:
                    self.map_sfixed32_sfixed32 = Dict[Int32, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int32_value()
                        map_key_reader.finish()
                        self.map_sfixed32_sfixed32[parsed_map_key] = reader.int32_value()
            elif field_name == "mapSfixed64Sfixed64" or field_name == "map_sfixed64_sfixed64":
                if seen_65:
                    raise Error("proto json: duplicate field mapSfixed64Sfixed64")
                seen_65 = True
                if reader.read_null():
                    self.map_sfixed64_sfixed64 = Dict[Int64, Int64]()
                else:
                    self.map_sfixed64_sfixed64 = Dict[Int64, Int64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int64_value()
                        map_key_reader.finish()
                        self.map_sfixed64_sfixed64[parsed_map_key] = reader.int64_value()
            elif field_name == "mapInt32Float" or field_name == "map_int32_float":
                if seen_66:
                    raise Error("proto json: duplicate field mapInt32Float")
                seen_66 = True
                if reader.read_null():
                    self.map_int32_float = Dict[Int32, Float32]()
                else:
                    self.map_int32_float = Dict[Int32, Float32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int32_value()
                        map_key_reader.finish()
                        self.map_int32_float[parsed_map_key] = reader.float32_value()
            elif field_name == "mapInt32Double" or field_name == "map_int32_double":
                if seen_67:
                    raise Error("proto json: duplicate field mapInt32Double")
                seen_67 = True
                if reader.read_null():
                    self.map_int32_double = Dict[Int32, Float64]()
                else:
                    self.map_int32_double = Dict[Int32, Float64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int32_value()
                        map_key_reader.finish()
                        self.map_int32_double[parsed_map_key] = reader.float64_value()
            elif field_name == "mapBoolBool" or field_name == "map_bool_bool":
                if seen_68:
                    raise Error("proto json: duplicate field mapBoolBool")
                seen_68 = True
                if reader.read_null():
                    self.map_bool_bool = Dict[Bool, Bool]()
                else:
                    self.map_bool_bool = Dict[Bool, Bool]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.bool_value()
                        map_key_reader.finish()
                        self.map_bool_bool[parsed_map_key] = reader.bool_value()
            elif field_name == "mapStringString" or field_name == "map_string_string":
                if seen_69:
                    raise Error("proto json: duplicate field mapStringString")
                seen_69 = True
                if reader.read_null():
                    self.map_string_string = Dict[String, String]()
                else:
                    self.map_string_string = Dict[String, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.map_string_string[map_key^] = reader.string_value()
            elif field_name == "mapStringBytes" or field_name == "map_string_bytes":
                if seen_70:
                    raise Error("proto json: duplicate field mapStringBytes")
                seen_70 = True
                if reader.read_null():
                    self.map_string_bytes = Dict[String, List[Byte]]()
                else:
                    self.map_string_bytes = Dict[String, List[Byte]]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.map_string_bytes[map_key^] = reader.bytes_value()
            elif field_name == "mapStringNestedMessage" or field_name == "map_string_nested_message":
                if seen_71:
                    raise Error("proto json: duplicate field mapStringNestedMessage")
                seen_71 = True
                if reader.read_null():
                    self.map_string_nested_message = Dict[String, TestAllTypesProto3_NestedMessage]()
                else:
                    self.map_string_nested_message = Dict[String, TestAllTypesProto3_NestedMessage]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.map_string_nested_message[map_key^] = reader.message_value[TestAllTypesProto3_NestedMessage]()
            elif field_name == "mapStringForeignMessage" or field_name == "map_string_foreign_message":
                if seen_72:
                    raise Error("proto json: duplicate field mapStringForeignMessage")
                seen_72 = True
                if reader.read_null():
                    self.map_string_foreign_message = Dict[String, ForeignMessage]()
                else:
                    self.map_string_foreign_message = Dict[String, ForeignMessage]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.map_string_foreign_message[map_key^] = reader.message_value[ForeignMessage]()
            elif field_name == "mapStringNestedEnum" or field_name == "map_string_nested_enum":
                if seen_73:
                    raise Error("proto json: duplicate field mapStringNestedEnum")
                seen_73 = True
                if reader.read_null():
                    self.map_string_nested_enum = Dict[String, Int32]()
                else:
                    self.map_string_nested_enum = Dict[String, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var enum_name = reader.enum_name()
                        var map_enum_value = Int32(0)
                        var map_enum_known = True
                        if enum_name:
                            if enum_name.value() == "FOO":
                                map_enum_value = 0
                            elif enum_name.value() == "BAR":
                                map_enum_value = 1
                            elif enum_name.value() == "BAZ":
                                map_enum_value = 2
                            elif enum_name.value() == "NEG":
                                map_enum_value = -1
                            elif not reader.options.ignore_unknown_fields:
                                raise Error("proto json: unknown enum value")
                            else:
                                map_enum_known = False
                        else:
                            map_enum_value = reader.int32_value()
                        if map_enum_known:
                            self.map_string_nested_enum[map_key^] = map_enum_value
            elif field_name == "mapStringForeignEnum" or field_name == "map_string_foreign_enum":
                if seen_74:
                    raise Error("proto json: duplicate field mapStringForeignEnum")
                seen_74 = True
                if reader.read_null():
                    self.map_string_foreign_enum = Dict[String, Int32]()
                else:
                    self.map_string_foreign_enum = Dict[String, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var enum_name = reader.enum_name()
                        var map_enum_value = Int32(0)
                        var map_enum_known = True
                        if enum_name:
                            if enum_name.value() == "FOREIGN_FOO":
                                map_enum_value = 0
                            elif enum_name.value() == "FOREIGN_BAR":
                                map_enum_value = 1
                            elif enum_name.value() == "FOREIGN_BAZ":
                                map_enum_value = 2
                            elif not reader.options.ignore_unknown_fields:
                                raise Error("proto json: unknown enum value")
                            else:
                                map_enum_known = False
                        else:
                            map_enum_value = reader.int32_value()
                        if map_enum_known:
                            self.map_string_foreign_enum[map_key^] = map_enum_value
            elif field_name == "oneofUint32" or field_name == "oneof_uint32":
                if seen_111:
                    raise Error("proto json: duplicate field oneofUint32")
                seen_111 = True
                if reader.read_null():
                    if self.oneof_field_case == 111:
                        self.oneof_uint32 = 0
                        self.oneof_field_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.oneof_uint32 = reader.uint32_value()
                    if self.oneof_field_case != 0 and self.oneof_field_case != 111:
                        raise Error("proto json: multiple oneof fields")
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
                    self.oneof_field_case = 111
            elif field_name == "oneofNestedMessage" or field_name == "oneof_nested_message":
                if seen_112:
                    raise Error("proto json: duplicate field oneofNestedMessage")
                seen_112 = True
                if reader.read_null():
                    if self.oneof_field_case == 112:
                        self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                        self.oneof_field_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_nested_message.append(reader.message_value[TestAllTypesProto3_NestedMessage]())
                    if self.oneof_field_case != 0 and self.oneof_field_case != 112:
                        raise Error("proto json: multiple oneof fields")
                    self.oneof_uint32 = 0
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
                    self.oneof_field_case = 112
            elif field_name == "oneofString" or field_name == "oneof_string":
                if seen_113:
                    raise Error("proto json: duplicate field oneofString")
                seen_113 = True
                if reader.read_null():
                    if self.oneof_field_case == 113:
                        self.oneof_string = String()
                        self.oneof_field_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.oneof_string = reader.string_value()
                    if self.oneof_field_case != 0 and self.oneof_field_case != 113:
                        raise Error("proto json: multiple oneof fields")
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
                    self.oneof_field_case = 113
            elif field_name == "oneofBytes" or field_name == "oneof_bytes":
                if seen_114:
                    raise Error("proto json: duplicate field oneofBytes")
                seen_114 = True
                if reader.read_null():
                    if self.oneof_field_case == 114:
                        self.oneof_bytes = List[Byte]()
                        self.oneof_field_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.oneof_bytes = reader.bytes_value()
                    if self.oneof_field_case != 0 and self.oneof_field_case != 114:
                        raise Error("proto json: multiple oneof fields")
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
                    self.oneof_field_case = 114
            elif field_name == "oneofBool" or field_name == "oneof_bool":
                if seen_115:
                    raise Error("proto json: duplicate field oneofBool")
                seen_115 = True
                if reader.read_null():
                    if self.oneof_field_case == 115:
                        self.oneof_bool = False
                        self.oneof_field_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.oneof_bool = reader.bool_value()
                    if self.oneof_field_case != 0 and self.oneof_field_case != 115:
                        raise Error("proto json: multiple oneof fields")
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
                    self.oneof_field_case = 115
            elif field_name == "oneofUint64" or field_name == "oneof_uint64":
                if seen_116:
                    raise Error("proto json: duplicate field oneofUint64")
                seen_116 = True
                if reader.read_null():
                    if self.oneof_field_case == 116:
                        self.oneof_uint64 = 0
                        self.oneof_field_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.oneof_uint64 = reader.uint64_value()
                    if self.oneof_field_case != 0 and self.oneof_field_case != 116:
                        raise Error("proto json: multiple oneof fields")
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
                    self.oneof_field_case = 116
            elif field_name == "oneofFloat" or field_name == "oneof_float":
                if seen_117:
                    raise Error("proto json: duplicate field oneofFloat")
                seen_117 = True
                if reader.read_null():
                    if self.oneof_field_case == 117:
                        self.oneof_float = 0.0
                        self.oneof_field_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.oneof_float = reader.float32_value()
                    if self.oneof_field_case != 0 and self.oneof_field_case != 117:
                        raise Error("proto json: multiple oneof fields")
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
                    self.oneof_field_case = 117
            elif field_name == "oneofDouble" or field_name == "oneof_double":
                if seen_118:
                    raise Error("proto json: duplicate field oneofDouble")
                seen_118 = True
                if reader.read_null():
                    if self.oneof_field_case == 118:
                        self.oneof_double = 0.0
                        self.oneof_field_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.oneof_double = reader.float64_value()
                    if self.oneof_field_case != 0 and self.oneof_field_case != 118:
                        raise Error("proto json: multiple oneof fields")
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
                    self.oneof_field_case = 118
            elif field_name == "oneofEnum" or field_name == "oneof_enum":
                if seen_119:
                    raise Error("proto json: duplicate field oneofEnum")
                seen_119 = True
                if reader.read_null():
                    if self.oneof_field_case == 119:
                        self.oneof_enum = 0
                        self.oneof_field_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    var oneof_value_known_119 = True
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "FOO":
                            self.oneof_enum = 0
                        elif enum_name.value() == "BAR":
                            self.oneof_enum = 1
                        elif enum_name.value() == "BAZ":
                            self.oneof_enum = 2
                        elif enum_name.value() == "NEG":
                            self.oneof_enum = -1
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                        else:
                            oneof_value_known_119 = False
                    else:
                        self.oneof_enum = reader.int32_value()
                    if oneof_value_known_119:
                        if self.oneof_field_case != 0 and self.oneof_field_case != 119:
                            raise Error("proto json: multiple oneof fields")
                        self.oneof_uint32 = 0
                        self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                        self.oneof_string = String()
                        self.oneof_bytes = List[Byte]()
                        self.oneof_bool = False
                        self.oneof_uint64 = 0
                        self.oneof_float = 0.0
                        self.oneof_double = 0.0
                        self.oneof_null_value = 0
                        self.oneof_field_case = 119
            elif field_name == "oneofNullValue" or field_name == "oneof_null_value":
                if seen_120:
                    raise Error("proto json: duplicate field oneofNullValue")
                seen_120 = True
                if reader.read_null():
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.oneof_null_value = 0
                    if self.oneof_field_case != 0 and self.oneof_field_case != 120:
                        raise Error("proto json: multiple oneof fields")
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_field_case = 120
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    var oneof_value_known_120 = True
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "NULL_VALUE":
                            self.oneof_null_value = 0
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                        else:
                            oneof_value_known_120 = False
                    else:
                        self.oneof_null_value = reader.int32_value()
                    if oneof_value_known_120:
                        if self.oneof_field_case != 0 and self.oneof_field_case != 120:
                            raise Error("proto json: multiple oneof fields")
                        self.oneof_uint32 = 0
                        self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
                        self.oneof_string = String()
                        self.oneof_bytes = List[Byte]()
                        self.oneof_bool = False
                        self.oneof_uint64 = 0
                        self.oneof_float = 0.0
                        self.oneof_double = 0.0
                        self.oneof_enum = 0
                        self.oneof_field_case = 120
            elif field_name == "optionalBoolWrapper" or field_name == "optional_bool_wrapper":
                if seen_201:
                    raise Error("proto json: duplicate field optionalBoolWrapper")
                seen_201 = True
                if reader.read_null():
                    self.optional_bool_wrapper = None
                else:
                    self.optional_bool_wrapper = reader.message_value[BoolValue]()
            elif field_name == "optionalInt32Wrapper" or field_name == "optional_int32_wrapper":
                if seen_202:
                    raise Error("proto json: duplicate field optionalInt32Wrapper")
                seen_202 = True
                if reader.read_null():
                    self.optional_int32_wrapper = None
                else:
                    self.optional_int32_wrapper = reader.message_value[Int32Value]()
            elif field_name == "optionalInt64Wrapper" or field_name == "optional_int64_wrapper":
                if seen_203:
                    raise Error("proto json: duplicate field optionalInt64Wrapper")
                seen_203 = True
                if reader.read_null():
                    self.optional_int64_wrapper = None
                else:
                    self.optional_int64_wrapper = reader.message_value[Int64Value]()
            elif field_name == "optionalUint32Wrapper" or field_name == "optional_uint32_wrapper":
                if seen_204:
                    raise Error("proto json: duplicate field optionalUint32Wrapper")
                seen_204 = True
                if reader.read_null():
                    self.optional_uint32_wrapper = None
                else:
                    self.optional_uint32_wrapper = reader.message_value[UInt32Value]()
            elif field_name == "optionalUint64Wrapper" or field_name == "optional_uint64_wrapper":
                if seen_205:
                    raise Error("proto json: duplicate field optionalUint64Wrapper")
                seen_205 = True
                if reader.read_null():
                    self.optional_uint64_wrapper = None
                else:
                    self.optional_uint64_wrapper = reader.message_value[UInt64Value]()
            elif field_name == "optionalFloatWrapper" or field_name == "optional_float_wrapper":
                if seen_206:
                    raise Error("proto json: duplicate field optionalFloatWrapper")
                seen_206 = True
                if reader.read_null():
                    self.optional_float_wrapper = None
                else:
                    self.optional_float_wrapper = reader.message_value[FloatValue]()
            elif field_name == "optionalDoubleWrapper" or field_name == "optional_double_wrapper":
                if seen_207:
                    raise Error("proto json: duplicate field optionalDoubleWrapper")
                seen_207 = True
                if reader.read_null():
                    self.optional_double_wrapper = None
                else:
                    self.optional_double_wrapper = reader.message_value[DoubleValue]()
            elif field_name == "optionalStringWrapper" or field_name == "optional_string_wrapper":
                if seen_208:
                    raise Error("proto json: duplicate field optionalStringWrapper")
                seen_208 = True
                if reader.read_null():
                    self.optional_string_wrapper = None
                else:
                    self.optional_string_wrapper = reader.message_value[StringValue]()
            elif field_name == "optionalBytesWrapper" or field_name == "optional_bytes_wrapper":
                if seen_209:
                    raise Error("proto json: duplicate field optionalBytesWrapper")
                seen_209 = True
                if reader.read_null():
                    self.optional_bytes_wrapper = None
                else:
                    self.optional_bytes_wrapper = reader.message_value[BytesValue]()
            elif field_name == "repeatedBoolWrapper" or field_name == "repeated_bool_wrapper":
                if seen_211:
                    raise Error("proto json: duplicate field repeatedBoolWrapper")
                seen_211 = True
                if reader.read_null():
                    self.repeated_bool_wrapper = List[BoolValue]()
                else:
                    self.repeated_bool_wrapper = List[BoolValue]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_bool_wrapper.append(reader.message_value[BoolValue]())
            elif field_name == "repeatedInt32Wrapper" or field_name == "repeated_int32_wrapper":
                if seen_212:
                    raise Error("proto json: duplicate field repeatedInt32Wrapper")
                seen_212 = True
                if reader.read_null():
                    self.repeated_int32_wrapper = List[Int32Value]()
                else:
                    self.repeated_int32_wrapper = List[Int32Value]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_int32_wrapper.append(reader.message_value[Int32Value]())
            elif field_name == "repeatedInt64Wrapper" or field_name == "repeated_int64_wrapper":
                if seen_213:
                    raise Error("proto json: duplicate field repeatedInt64Wrapper")
                seen_213 = True
                if reader.read_null():
                    self.repeated_int64_wrapper = List[Int64Value]()
                else:
                    self.repeated_int64_wrapper = List[Int64Value]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_int64_wrapper.append(reader.message_value[Int64Value]())
            elif field_name == "repeatedUint32Wrapper" or field_name == "repeated_uint32_wrapper":
                if seen_214:
                    raise Error("proto json: duplicate field repeatedUint32Wrapper")
                seen_214 = True
                if reader.read_null():
                    self.repeated_uint32_wrapper = List[UInt32Value]()
                else:
                    self.repeated_uint32_wrapper = List[UInt32Value]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_uint32_wrapper.append(reader.message_value[UInt32Value]())
            elif field_name == "repeatedUint64Wrapper" or field_name == "repeated_uint64_wrapper":
                if seen_215:
                    raise Error("proto json: duplicate field repeatedUint64Wrapper")
                seen_215 = True
                if reader.read_null():
                    self.repeated_uint64_wrapper = List[UInt64Value]()
                else:
                    self.repeated_uint64_wrapper = List[UInt64Value]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_uint64_wrapper.append(reader.message_value[UInt64Value]())
            elif field_name == "repeatedFloatWrapper" or field_name == "repeated_float_wrapper":
                if seen_216:
                    raise Error("proto json: duplicate field repeatedFloatWrapper")
                seen_216 = True
                if reader.read_null():
                    self.repeated_float_wrapper = List[FloatValue]()
                else:
                    self.repeated_float_wrapper = List[FloatValue]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_float_wrapper.append(reader.message_value[FloatValue]())
            elif field_name == "repeatedDoubleWrapper" or field_name == "repeated_double_wrapper":
                if seen_217:
                    raise Error("proto json: duplicate field repeatedDoubleWrapper")
                seen_217 = True
                if reader.read_null():
                    self.repeated_double_wrapper = List[DoubleValue]()
                else:
                    self.repeated_double_wrapper = List[DoubleValue]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_double_wrapper.append(reader.message_value[DoubleValue]())
            elif field_name == "repeatedStringWrapper" or field_name == "repeated_string_wrapper":
                if seen_218:
                    raise Error("proto json: duplicate field repeatedStringWrapper")
                seen_218 = True
                if reader.read_null():
                    self.repeated_string_wrapper = List[StringValue]()
                else:
                    self.repeated_string_wrapper = List[StringValue]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_string_wrapper.append(reader.message_value[StringValue]())
            elif field_name == "repeatedBytesWrapper" or field_name == "repeated_bytes_wrapper":
                if seen_219:
                    raise Error("proto json: duplicate field repeatedBytesWrapper")
                seen_219 = True
                if reader.read_null():
                    self.repeated_bytes_wrapper = List[BytesValue]()
                else:
                    self.repeated_bytes_wrapper = List[BytesValue]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_bytes_wrapper.append(reader.message_value[BytesValue]())
            elif field_name == "optionalDuration" or field_name == "optional_duration":
                if seen_301:
                    raise Error("proto json: duplicate field optionalDuration")
                seen_301 = True
                if reader.read_null():
                    self.optional_duration = None
                else:
                    self.optional_duration = reader.message_value[Duration]()
            elif field_name == "optionalTimestamp" or field_name == "optional_timestamp":
                if seen_302:
                    raise Error("proto json: duplicate field optionalTimestamp")
                seen_302 = True
                if reader.read_null():
                    self.optional_timestamp = None
                else:
                    self.optional_timestamp = reader.message_value[Timestamp]()
            elif field_name == "optionalFieldMask" or field_name == "optional_field_mask":
                if seen_303:
                    raise Error("proto json: duplicate field optionalFieldMask")
                seen_303 = True
                if reader.read_null():
                    self.optional_field_mask = None
                else:
                    self.optional_field_mask = reader.message_value[FieldMask]()
            elif field_name == "optionalStruct" or field_name == "optional_struct":
                if seen_304:
                    raise Error("proto json: duplicate field optionalStruct")
                seen_304 = True
                if reader.read_null():
                    self.optional_struct = None
                else:
                    self.optional_struct = reader.message_value[Struct]()
            elif field_name == "optionalAny" or field_name == "optional_any":
                if seen_305:
                    raise Error("proto json: duplicate field optionalAny")
                seen_305 = True
                if reader.read_null():
                    self.optional_any = None
                else:
                    self.optional_any = reader.message_value[Any]()
            elif field_name == "optionalValue" or field_name == "optional_value":
                if seen_306:
                    raise Error("proto json: duplicate field optionalValue")
                seen_306 = True
                if reader.read_null():
                    var null_optional_value = Value()
                    null_optional_value.null_value = 0
                    null_optional_value.kind_case = 1
                    self.optional_value = null_optional_value^
                else:
                    self.optional_value = reader.message_value[Value]()
            elif field_name == "optionalNullValue" or field_name == "optional_null_value":
                if seen_307:
                    raise Error("proto json: duplicate field optionalNullValue")
                seen_307 = True
                if reader.read_null():
                    self.optional_null_value = 0
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "NULL_VALUE":
                            self.optional_null_value = 0
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.optional_null_value = reader.int32_value()
            elif field_name == "repeatedDuration" or field_name == "repeated_duration":
                if seen_311:
                    raise Error("proto json: duplicate field repeatedDuration")
                seen_311 = True
                if reader.read_null():
                    self.repeated_duration = List[Duration]()
                else:
                    self.repeated_duration = List[Duration]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_duration.append(reader.message_value[Duration]())
            elif field_name == "repeatedTimestamp" or field_name == "repeated_timestamp":
                if seen_312:
                    raise Error("proto json: duplicate field repeatedTimestamp")
                seen_312 = True
                if reader.read_null():
                    self.repeated_timestamp = List[Timestamp]()
                else:
                    self.repeated_timestamp = List[Timestamp]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_timestamp.append(reader.message_value[Timestamp]())
            elif field_name == "repeatedFieldmask" or field_name == "repeated_fieldmask":
                if seen_313:
                    raise Error("proto json: duplicate field repeatedFieldmask")
                seen_313 = True
                if reader.read_null():
                    self.repeated_fieldmask = List[FieldMask]()
                else:
                    self.repeated_fieldmask = List[FieldMask]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_fieldmask.append(reader.message_value[FieldMask]())
            elif field_name == "repeatedStruct" or field_name == "repeated_struct":
                if seen_324:
                    raise Error("proto json: duplicate field repeatedStruct")
                seen_324 = True
                if reader.read_null():
                    self.repeated_struct = List[Struct]()
                else:
                    self.repeated_struct = List[Struct]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_struct.append(reader.message_value[Struct]())
            elif field_name == "repeatedAny" or field_name == "repeated_any":
                if seen_315:
                    raise Error("proto json: duplicate field repeatedAny")
                seen_315 = True
                if reader.read_null():
                    self.repeated_any = List[Any]()
                else:
                    self.repeated_any = List[Any]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_any.append(reader.message_value[Any]())
            elif field_name == "repeatedValue" or field_name == "repeated_value":
                if seen_316:
                    raise Error("proto json: duplicate field repeatedValue")
                seen_316 = True
                if reader.read_null():
                    self.repeated_value = List[Value]()
                else:
                    self.repeated_value = List[Value]()
                    reader.begin_array()
                    while reader.next_array_item():
                        self.repeated_value.append(reader.message_value[Value]())
            elif field_name == "repeatedListValue" or field_name == "repeated_list_value":
                if seen_317:
                    raise Error("proto json: duplicate field repeatedListValue")
                seen_317 = True
                if reader.read_null():
                    self.repeated_list_value = List[ListValue]()
                else:
                    self.repeated_list_value = List[ListValue]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.repeated_list_value.append(reader.message_value[ListValue]())
            elif field_name == "fieldname1" or field_name == "fieldname1":
                if seen_401:
                    raise Error("proto json: duplicate field fieldname1")
                seen_401 = True
                if reader.read_null():
                    self.fieldname1 = 0
                else:
                    self.fieldname1 = reader.int32_value()
            elif field_name == "fieldName2" or field_name == "field_name2":
                if seen_402:
                    raise Error("proto json: duplicate field fieldName2")
                seen_402 = True
                if reader.read_null():
                    self.field_name2 = 0
                else:
                    self.field_name2 = reader.int32_value()
            elif field_name == "FieldName3" or field_name == "_field_name3":
                if seen_403:
                    raise Error("proto json: duplicate field FieldName3")
                seen_403 = True
                if reader.read_null():
                    self._field_name3 = 0
                else:
                    self._field_name3 = reader.int32_value()
            elif field_name == "fieldName4" or field_name == "field__name4_":
                if seen_404:
                    raise Error("proto json: duplicate field fieldName4")
                seen_404 = True
                if reader.read_null():
                    self.field__name4_ = 0
                else:
                    self.field__name4_ = reader.int32_value()
            elif field_name == "field0name5" or field_name == "field0name5":
                if seen_405:
                    raise Error("proto json: duplicate field field0name5")
                seen_405 = True
                if reader.read_null():
                    self.field0name5 = 0
                else:
                    self.field0name5 = reader.int32_value()
            elif field_name == "field0Name6" or field_name == "field_0_name6":
                if seen_406:
                    raise Error("proto json: duplicate field field0Name6")
                seen_406 = True
                if reader.read_null():
                    self.field_0_name6 = 0
                else:
                    self.field_0_name6 = reader.int32_value()
            elif field_name == "fieldName7" or field_name == "fieldName7":
                if seen_407:
                    raise Error("proto json: duplicate field fieldName7")
                seen_407 = True
                if reader.read_null():
                    self.fieldName7 = 0
                else:
                    self.fieldName7 = reader.int32_value()
            elif field_name == "FieldName8" or field_name == "FieldName8":
                if seen_408:
                    raise Error("proto json: duplicate field FieldName8")
                seen_408 = True
                if reader.read_null():
                    self.FieldName8 = 0
                else:
                    self.FieldName8 = reader.int32_value()
            elif field_name == "fieldName9" or field_name == "field_Name9":
                if seen_409:
                    raise Error("proto json: duplicate field fieldName9")
                seen_409 = True
                if reader.read_null():
                    self.field_Name9 = 0
                else:
                    self.field_Name9 = reader.int32_value()
            elif field_name == "FieldName10" or field_name == "Field_Name10":
                if seen_410:
                    raise Error("proto json: duplicate field FieldName10")
                seen_410 = True
                if reader.read_null():
                    self.Field_Name10 = 0
                else:
                    self.Field_Name10 = reader.int32_value()
            elif field_name == "FIELDNAME11" or field_name == "FIELD_NAME11":
                if seen_411:
                    raise Error("proto json: duplicate field FIELDNAME11")
                seen_411 = True
                if reader.read_null():
                    self.FIELD_NAME11 = 0
                else:
                    self.FIELD_NAME11 = reader.int32_value()
            elif field_name == "FIELDName12" or field_name == "FIELD_name12":
                if seen_412:
                    raise Error("proto json: duplicate field FIELDName12")
                seen_412 = True
                if reader.read_null():
                    self.FIELD_name12 = 0
                else:
                    self.FIELD_name12 = reader.int32_value()
            elif field_name == "FieldName13" or field_name == "__field_name13":
                if seen_413:
                    raise Error("proto json: duplicate field FieldName13")
                seen_413 = True
                if reader.read_null():
                    self.__field_name13 = 0
                else:
                    self.__field_name13 = reader.int32_value()
            elif field_name == "FieldName14" or field_name == "__Field_name14":
                if seen_414:
                    raise Error("proto json: duplicate field FieldName14")
                seen_414 = True
                if reader.read_null():
                    self.__Field_name14 = 0
                else:
                    self.__Field_name14 = reader.int32_value()
            elif field_name == "fieldName15" or field_name == "field__name15":
                if seen_415:
                    raise Error("proto json: duplicate field fieldName15")
                seen_415 = True
                if reader.read_null():
                    self.field__name15 = 0
                else:
                    self.field__name15 = reader.int32_value()
            elif field_name == "fieldName16" or field_name == "field__Name16":
                if seen_416:
                    raise Error("proto json: duplicate field fieldName16")
                seen_416 = True
                if reader.read_null():
                    self.field__Name16 = 0
                else:
                    self.field__Name16 = reader.int32_value()
            elif field_name == "fieldName17" or field_name == "field_name17__":
                if seen_417:
                    raise Error("proto json: duplicate field fieldName17")
                seen_417 = True
                if reader.read_null():
                    self.field_name17__ = 0
                else:
                    self.field_name17__ = reader.int32_value()
            elif field_name == "FieldName18" or field_name == "Field_name18__":
                if seen_418:
                    raise Error("proto json: duplicate field FieldName18")
                seen_418 = True
                if reader.read_null():
                    self.Field_name18__ = 0
                else:
                    self.Field_name18__ = reader.int32_value()
            else:
                reader.skip_unknown_value()


struct ForeignMessage(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `ForeignMessage` protobuf message."""

    var c: Int32
    """Field `c` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.c = 0
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.c != 0:
            writer.int32(1, self.c)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.c = reader.int32_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.c != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("c", "c")
            writer.int32_value(self.c)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "c" or field_name == "c":
                if seen_1:
                    raise Error("proto json: duplicate field c")
                seen_1 = True
                if reader.read_null():
                    self.c = 0
                else:
                    self.c = reader.int32_value()
            else:
                reader.skip_unknown_value()


struct NullHypothesisProto3(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `NullHypothesisProto3` protobuf message."""

    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    def __init__(out self):
        """Initializes an empty message."""
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Args:
            writer: Destination wire-format writer.
        """
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            reader.capture_field(tag[0], tag[1], self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            reader.skip_unknown_value()


struct EnumOnlyProto3_Bool:
    """Values of the `Bool` protobuf enum (fields carry `Int32`)."""

    comptime kFalse = 0
    """`kFalse` = 0."""
    comptime kTrue = 1
    """`kTrue` = 1."""


struct EnumOnlyProto3(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `EnumOnlyProto3` protobuf message."""

    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    def __init__(out self):
        """Initializes an empty message."""
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Args:
            writer: Destination wire-format writer.
        """
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            reader.capture_field(tag[0], tag[1], self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            reader.skip_unknown_value()
