# GENERATED by protoc-gen-mojo from google/protobuf/test_messages_proto3.proto — do not edit.

"""Generated protobuf messages from `google/protobuf/test_messages_proto3.proto`.

Regenerate with tools/protoc-gen-mojo; do not edit by hand.
"""

from proto import (
    WIRE_FIXED32,
    WIRE_FIXED64,
    WIRE_LEN,
    WIRE_VARINT,
    ProtoMessage,
    WireReader,
    WireWriter,
    zigzag_encode32,
    zigzag_encode64,
)
from any_pb import Any
from duration_pb import Duration
from field_mask_pb import FieldMask
from struct_pb import ListValue, Struct, Value
from timestamp_pb import Timestamp
from wrappers_pb import (
    BoolValue,
    BytesValue,
    DoubleValue,
    FloatValue,
    Int32Value,
    Int64Value,
    StringValue,
    UInt32Value,
    UInt64Value,
)


struct ForeignEnum:
    """Values of the `ForeignEnum` protobuf enum (fields carry `Int32`)."""

    comptime FOREIGN_FOO = 0
    """`FOREIGN_FOO` = 0."""
    comptime FOREIGN_BAR = 1
    """`FOREIGN_BAR` = 1."""
    comptime FOREIGN_BAZ = 2
    """`FOREIGN_BAZ` = 2."""


struct TestAllTypesProto3_NestedEnum:
    """Values of the `NestedEnum` protobuf enum (fields carry `Int32`)."""

    comptime FOO = 0
    """`FOO` = 0."""
    comptime BAR = 1
    """`BAR` = 1."""
    comptime BAZ = 2
    """`BAZ` = 2."""
    comptime NEG = -1
    """`NEG` = -1."""


struct TestAllTypesProto3_AliasedEnum:
    """Values of the `AliasedEnum` protobuf enum (fields carry `Int32`)."""

    comptime ALIAS_FOO = 0
    """`ALIAS_FOO` = 0."""
    comptime ALIAS_BAR = 1
    """`ALIAS_BAR` = 1."""
    comptime ALIAS_BAZ = 2
    """`ALIAS_BAZ` = 2."""
    comptime MOO = 2
    """`MOO` = 2."""
    comptime moo = 2
    """`moo` = 2."""
    comptime bAz = 2
    """`bAz` = 2."""


struct TestAllTypesProto3_NestedMessage(
    Copyable, Defaultable, Movable, ProtoMessage
):
    """Generated from the `NestedMessage` protobuf message."""

    var a: Int32
    """Field `a` (number 1)."""
    var corecursive: List[TestAllTypesProto3]
    """Field `corecursive` (number 2, recursive: boxed as a 0-or-1 list)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.a = 0
        self.corecursive = List[TestAllTypesProto3]()
        self._unknown = List[Byte]()

    def __deinit__(deinit self):
        """Destroys the message (explicit: breaks the trait-synthesis cycle for recursive types).
        """
        pass

    def __init__(out self, *, copy: Self):
        """Deep-copies another message.

        Args:
            copy: The message to copy from.
        """
        self.a = copy.a
        self.corecursive = copy.corecursive.copy()
        self._unknown = copy._unknown.copy()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.a != 0:
            writer.int32(1, self.a)
        if len(self.corecursive) != 0:
            var sub = WireWriter()
            self.corecursive[0].encode_to(sub)
            writer.len_prefixed(2, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.a = reader.int32_value()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: TestAllTypesProto3
                    if len(self.corecursive) != 0:
                        m = self.corecursive.pop()
                    else:
                        m = TestAllTypesProto3()
                    m.merge_from(sub)
                    self.corecursive.append(m^)
            else:
                reader.capture_field(field, wire_type, self._unknown)


struct TestAllTypesProto3(Copyable, Defaultable, Movable, ProtoMessage):
    """Generated from the `TestAllTypesProto3` protobuf message."""

    var optional_int32: Int32
    """Field `optional_int32` (number 1)."""
    var optional_int64: Int64
    """Field `optional_int64` (number 2)."""
    var optional_uint32: UInt32
    """Field `optional_uint32` (number 3)."""
    var optional_uint64: UInt64
    """Field `optional_uint64` (number 4)."""
    var optional_sint32: Int32
    """Field `optional_sint32` (number 5)."""
    var optional_sint64: Int64
    """Field `optional_sint64` (number 6)."""
    var optional_fixed32: UInt32
    """Field `optional_fixed32` (number 7)."""
    var optional_fixed64: UInt64
    """Field `optional_fixed64` (number 8)."""
    var optional_sfixed32: Int32
    """Field `optional_sfixed32` (number 9)."""
    var optional_sfixed64: Int64
    """Field `optional_sfixed64` (number 10)."""
    var optional_float: Float32
    """Field `optional_float` (number 11)."""
    var optional_double: Float64
    """Field `optional_double` (number 12)."""
    var optional_bool: Bool
    """Field `optional_bool` (number 13)."""
    var optional_string: String
    """Field `optional_string` (number 14)."""
    var optional_bytes: List[Byte]
    """Field `optional_bytes` (number 15)."""
    var optional_nested_message: List[TestAllTypesProto3_NestedMessage]
    """Field `optional_nested_message` (number 18, recursive: boxed as a 0-or-1 list)."""
    var optional_foreign_message: Optional[ForeignMessage]
    """Field `optional_foreign_message` (number 19)."""
    var optional_nested_enum: Int32
    """Field `optional_nested_enum` (number 21)."""
    var optional_foreign_enum: Int32
    """Field `optional_foreign_enum` (number 22)."""
    var optional_aliased_enum: Int32
    """Field `optional_aliased_enum` (number 23)."""
    var optional_string_piece: String
    """Field `optional_string_piece` (number 24)."""
    var optional_cord: String
    """Field `optional_cord` (number 25)."""
    var recursive_message: List[TestAllTypesProto3]
    """Field `recursive_message` (number 27, recursive: boxed as a 0-or-1 list)."""
    var repeated_int32: List[Int32]
    """Field `repeated_int32` (number 31, repeated)."""
    var repeated_int64: List[Int64]
    """Field `repeated_int64` (number 32, repeated)."""
    var repeated_uint32: List[UInt32]
    """Field `repeated_uint32` (number 33, repeated)."""
    var repeated_uint64: List[UInt64]
    """Field `repeated_uint64` (number 34, repeated)."""
    var repeated_sint32: List[Int32]
    """Field `repeated_sint32` (number 35, repeated)."""
    var repeated_sint64: List[Int64]
    """Field `repeated_sint64` (number 36, repeated)."""
    var repeated_fixed32: List[UInt32]
    """Field `repeated_fixed32` (number 37, repeated)."""
    var repeated_fixed64: List[UInt64]
    """Field `repeated_fixed64` (number 38, repeated)."""
    var repeated_sfixed32: List[Int32]
    """Field `repeated_sfixed32` (number 39, repeated)."""
    var repeated_sfixed64: List[Int64]
    """Field `repeated_sfixed64` (number 40, repeated)."""
    var repeated_float: List[Float32]
    """Field `repeated_float` (number 41, repeated)."""
    var repeated_double: List[Float64]
    """Field `repeated_double` (number 42, repeated)."""
    var repeated_bool: List[Bool]
    """Field `repeated_bool` (number 43, repeated)."""
    var repeated_string: List[String]
    """Field `repeated_string` (number 44, repeated)."""
    var repeated_bytes: List[List[Byte]]
    """Field `repeated_bytes` (number 45, repeated)."""
    var repeated_nested_message: List[TestAllTypesProto3_NestedMessage]
    """Field `repeated_nested_message` (number 48, repeated)."""
    var repeated_foreign_message: List[ForeignMessage]
    """Field `repeated_foreign_message` (number 49, repeated)."""
    var repeated_nested_enum: List[Int32]
    """Field `repeated_nested_enum` (number 51, repeated)."""
    var repeated_foreign_enum: List[Int32]
    """Field `repeated_foreign_enum` (number 52, repeated)."""
    var repeated_string_piece: List[String]
    """Field `repeated_string_piece` (number 54, repeated)."""
    var repeated_cord: List[String]
    """Field `repeated_cord` (number 55, repeated)."""
    var packed_int32: List[Int32]
    """Field `packed_int32` (number 75, repeated)."""
    var packed_int64: List[Int64]
    """Field `packed_int64` (number 76, repeated)."""
    var packed_uint32: List[UInt32]
    """Field `packed_uint32` (number 77, repeated)."""
    var packed_uint64: List[UInt64]
    """Field `packed_uint64` (number 78, repeated)."""
    var packed_sint32: List[Int32]
    """Field `packed_sint32` (number 79, repeated)."""
    var packed_sint64: List[Int64]
    """Field `packed_sint64` (number 80, repeated)."""
    var packed_fixed32: List[UInt32]
    """Field `packed_fixed32` (number 81, repeated)."""
    var packed_fixed64: List[UInt64]
    """Field `packed_fixed64` (number 82, repeated)."""
    var packed_sfixed32: List[Int32]
    """Field `packed_sfixed32` (number 83, repeated)."""
    var packed_sfixed64: List[Int64]
    """Field `packed_sfixed64` (number 84, repeated)."""
    var packed_float: List[Float32]
    """Field `packed_float` (number 85, repeated)."""
    var packed_double: List[Float64]
    """Field `packed_double` (number 86, repeated)."""
    var packed_bool: List[Bool]
    """Field `packed_bool` (number 87, repeated)."""
    var packed_nested_enum: List[Int32]
    """Field `packed_nested_enum` (number 88, repeated)."""
    var unpacked_int32: List[Int32]
    """Field `unpacked_int32` (number 89, repeated)."""
    var unpacked_int64: List[Int64]
    """Field `unpacked_int64` (number 90, repeated)."""
    var unpacked_uint32: List[UInt32]
    """Field `unpacked_uint32` (number 91, repeated)."""
    var unpacked_uint64: List[UInt64]
    """Field `unpacked_uint64` (number 92, repeated)."""
    var unpacked_sint32: List[Int32]
    """Field `unpacked_sint32` (number 93, repeated)."""
    var unpacked_sint64: List[Int64]
    """Field `unpacked_sint64` (number 94, repeated)."""
    var unpacked_fixed32: List[UInt32]
    """Field `unpacked_fixed32` (number 95, repeated)."""
    var unpacked_fixed64: List[UInt64]
    """Field `unpacked_fixed64` (number 96, repeated)."""
    var unpacked_sfixed32: List[Int32]
    """Field `unpacked_sfixed32` (number 97, repeated)."""
    var unpacked_sfixed64: List[Int64]
    """Field `unpacked_sfixed64` (number 98, repeated)."""
    var unpacked_float: List[Float32]
    """Field `unpacked_float` (number 99, repeated)."""
    var unpacked_double: List[Float64]
    """Field `unpacked_double` (number 100, repeated)."""
    var unpacked_bool: List[Bool]
    """Field `unpacked_bool` (number 101, repeated)."""
    var unpacked_nested_enum: List[Int32]
    """Field `unpacked_nested_enum` (number 102, repeated)."""
    var map_int32_int32: Dict[Int32, Int32]
    """Field `map_int32_int32` (number 56, map field)."""
    var map_int64_int64: Dict[Int64, Int64]
    """Field `map_int64_int64` (number 57, map field)."""
    var map_uint32_uint32: Dict[UInt32, UInt32]
    """Field `map_uint32_uint32` (number 58, map field)."""
    var map_uint64_uint64: Dict[UInt64, UInt64]
    """Field `map_uint64_uint64` (number 59, map field)."""
    var map_sint32_sint32: Dict[Int32, Int32]
    """Field `map_sint32_sint32` (number 60, map field)."""
    var map_sint64_sint64: Dict[Int64, Int64]
    """Field `map_sint64_sint64` (number 61, map field)."""
    var map_fixed32_fixed32: Dict[UInt32, UInt32]
    """Field `map_fixed32_fixed32` (number 62, map field)."""
    var map_fixed64_fixed64: Dict[UInt64, UInt64]
    """Field `map_fixed64_fixed64` (number 63, map field)."""
    var map_sfixed32_sfixed32: Dict[Int32, Int32]
    """Field `map_sfixed32_sfixed32` (number 64, map field)."""
    var map_sfixed64_sfixed64: Dict[Int64, Int64]
    """Field `map_sfixed64_sfixed64` (number 65, map field)."""
    var map_int32_float: Dict[Int32, Float32]
    """Field `map_int32_float` (number 66, map field)."""
    var map_int32_double: Dict[Int32, Float64]
    """Field `map_int32_double` (number 67, map field)."""
    var map_bool_bool: Dict[Bool, Bool]
    """Field `map_bool_bool` (number 68, map field)."""
    var map_string_string: Dict[String, String]
    """Field `map_string_string` (number 69, map field)."""
    var map_string_bytes: Dict[String, List[Byte]]
    """Field `map_string_bytes` (number 70, map field)."""
    var map_string_nested_message: Dict[
        String, TestAllTypesProto3_NestedMessage
    ]
    """Field `map_string_nested_message` (number 71, map field)."""
    var map_string_foreign_message: Dict[String, ForeignMessage]
    """Field `map_string_foreign_message` (number 72, map field)."""
    var map_string_nested_enum: Dict[String, Int32]
    """Field `map_string_nested_enum` (number 73, map field)."""
    var map_string_foreign_enum: Dict[String, Int32]
    """Field `map_string_foreign_enum` (number 74, map field)."""
    var oneof_uint32: UInt32
    """Field `oneof_uint32` (number 111)."""
    var oneof_nested_message: List[TestAllTypesProto3_NestedMessage]
    """Field `oneof_nested_message` (number 112, recursive: boxed as a 0-or-1 list)."""
    var oneof_string: String
    """Field `oneof_string` (number 113)."""
    var oneof_bytes: List[Byte]
    """Field `oneof_bytes` (number 114)."""
    var oneof_bool: Bool
    """Field `oneof_bool` (number 115)."""
    var oneof_uint64: UInt64
    """Field `oneof_uint64` (number 116)."""
    var oneof_float: Float32
    """Field `oneof_float` (number 117)."""
    var oneof_double: Float64
    """Field `oneof_double` (number 118)."""
    var oneof_enum: Int32
    """Field `oneof_enum` (number 119)."""
    var oneof_null_value: Int32
    """Field `oneof_null_value` (number 120)."""
    var optional_bool_wrapper: Optional[BoolValue]
    """Field `optional_bool_wrapper` (number 201)."""
    var optional_int32_wrapper: Optional[Int32Value]
    """Field `optional_int32_wrapper` (number 202)."""
    var optional_int64_wrapper: Optional[Int64Value]
    """Field `optional_int64_wrapper` (number 203)."""
    var optional_uint32_wrapper: Optional[UInt32Value]
    """Field `optional_uint32_wrapper` (number 204)."""
    var optional_uint64_wrapper: Optional[UInt64Value]
    """Field `optional_uint64_wrapper` (number 205)."""
    var optional_float_wrapper: Optional[FloatValue]
    """Field `optional_float_wrapper` (number 206)."""
    var optional_double_wrapper: Optional[DoubleValue]
    """Field `optional_double_wrapper` (number 207)."""
    var optional_string_wrapper: Optional[StringValue]
    """Field `optional_string_wrapper` (number 208)."""
    var optional_bytes_wrapper: Optional[BytesValue]
    """Field `optional_bytes_wrapper` (number 209)."""
    var repeated_bool_wrapper: List[BoolValue]
    """Field `repeated_bool_wrapper` (number 211, repeated)."""
    var repeated_int32_wrapper: List[Int32Value]
    """Field `repeated_int32_wrapper` (number 212, repeated)."""
    var repeated_int64_wrapper: List[Int64Value]
    """Field `repeated_int64_wrapper` (number 213, repeated)."""
    var repeated_uint32_wrapper: List[UInt32Value]
    """Field `repeated_uint32_wrapper` (number 214, repeated)."""
    var repeated_uint64_wrapper: List[UInt64Value]
    """Field `repeated_uint64_wrapper` (number 215, repeated)."""
    var repeated_float_wrapper: List[FloatValue]
    """Field `repeated_float_wrapper` (number 216, repeated)."""
    var repeated_double_wrapper: List[DoubleValue]
    """Field `repeated_double_wrapper` (number 217, repeated)."""
    var repeated_string_wrapper: List[StringValue]
    """Field `repeated_string_wrapper` (number 218, repeated)."""
    var repeated_bytes_wrapper: List[BytesValue]
    """Field `repeated_bytes_wrapper` (number 219, repeated)."""
    var optional_duration: Optional[Duration]
    """Field `optional_duration` (number 301)."""
    var optional_timestamp: Optional[Timestamp]
    """Field `optional_timestamp` (number 302)."""
    var optional_field_mask: Optional[FieldMask]
    """Field `optional_field_mask` (number 303)."""
    var optional_struct: Optional[Struct]
    """Field `optional_struct` (number 304)."""
    var optional_any: Optional[Any]
    """Field `optional_any` (number 305)."""
    var optional_value: Optional[Value]
    """Field `optional_value` (number 306)."""
    var optional_null_value: Int32
    """Field `optional_null_value` (number 307)."""
    var repeated_duration: List[Duration]
    """Field `repeated_duration` (number 311, repeated)."""
    var repeated_timestamp: List[Timestamp]
    """Field `repeated_timestamp` (number 312, repeated)."""
    var repeated_fieldmask: List[FieldMask]
    """Field `repeated_fieldmask` (number 313, repeated)."""
    var repeated_struct: List[Struct]
    """Field `repeated_struct` (number 324, repeated)."""
    var repeated_any: List[Any]
    """Field `repeated_any` (number 315, repeated)."""
    var repeated_value: List[Value]
    """Field `repeated_value` (number 316, repeated)."""
    var repeated_list_value: List[ListValue]
    """Field `repeated_list_value` (number 317, repeated)."""
    var fieldname1: Int32
    """Field `fieldname1` (number 401)."""
    var field_name2: Int32
    """Field `field_name2` (number 402)."""
    var _field_name3: Int32
    """Field `_field_name3` (number 403)."""
    var field__name4_: Int32
    """Field `field__name4_` (number 404)."""
    var field0name5: Int32
    """Field `field0name5` (number 405)."""
    var field_0_name6: Int32
    """Field `field_0_name6` (number 406)."""
    var fieldName7: Int32
    """Field `fieldName7` (number 407)."""
    var FieldName8: Int32
    """Field `FieldName8` (number 408)."""
    var field_Name9: Int32
    """Field `field_Name9` (number 409)."""
    var Field_Name10: Int32
    """Field `Field_Name10` (number 410)."""
    var FIELD_NAME11: Int32
    """Field `FIELD_NAME11` (number 411)."""
    var FIELD_name12: Int32
    """Field `FIELD_name12` (number 412)."""
    var __field_name13: Int32
    """Field `__field_name13` (number 413)."""
    var __Field_name14: Int32
    """Field `__Field_name14` (number 414)."""
    var field__name15: Int32
    """Field `field__name15` (number 415)."""
    var field__Name16: Int32
    """Field `field__Name16` (number 416)."""
    var field_name17__: Int32
    """Field `field_name17__` (number 417)."""
    var Field_name18__: Int32
    """Field `Field_name18__` (number 418)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    var oneof_field_case: Int
    """Active member of oneof `oneof_field`: 0 when unset, else the set field number."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.optional_int32 = 0
        self.optional_int64 = 0
        self.optional_uint32 = 0
        self.optional_uint64 = 0
        self.optional_sint32 = 0
        self.optional_sint64 = 0
        self.optional_fixed32 = 0
        self.optional_fixed64 = 0
        self.optional_sfixed32 = 0
        self.optional_sfixed64 = 0
        self.optional_float = 0.0
        self.optional_double = 0.0
        self.optional_bool = False
        self.optional_string = String()
        self.optional_bytes = List[Byte]()
        self.optional_nested_message = List[TestAllTypesProto3_NestedMessage]()
        self.optional_foreign_message = None
        self.optional_nested_enum = 0
        self.optional_foreign_enum = 0
        self.optional_aliased_enum = 0
        self.optional_string_piece = String()
        self.optional_cord = String()
        self.recursive_message = List[TestAllTypesProto3]()
        self.repeated_int32 = List[Int32]()
        self.repeated_int64 = List[Int64]()
        self.repeated_uint32 = List[UInt32]()
        self.repeated_uint64 = List[UInt64]()
        self.repeated_sint32 = List[Int32]()
        self.repeated_sint64 = List[Int64]()
        self.repeated_fixed32 = List[UInt32]()
        self.repeated_fixed64 = List[UInt64]()
        self.repeated_sfixed32 = List[Int32]()
        self.repeated_sfixed64 = List[Int64]()
        self.repeated_float = List[Float32]()
        self.repeated_double = List[Float64]()
        self.repeated_bool = List[Bool]()
        self.repeated_string = List[String]()
        self.repeated_bytes = List[List[Byte]]()
        self.repeated_nested_message = List[TestAllTypesProto3_NestedMessage]()
        self.repeated_foreign_message = List[ForeignMessage]()
        self.repeated_nested_enum = List[Int32]()
        self.repeated_foreign_enum = List[Int32]()
        self.repeated_string_piece = List[String]()
        self.repeated_cord = List[String]()
        self.packed_int32 = List[Int32]()
        self.packed_int64 = List[Int64]()
        self.packed_uint32 = List[UInt32]()
        self.packed_uint64 = List[UInt64]()
        self.packed_sint32 = List[Int32]()
        self.packed_sint64 = List[Int64]()
        self.packed_fixed32 = List[UInt32]()
        self.packed_fixed64 = List[UInt64]()
        self.packed_sfixed32 = List[Int32]()
        self.packed_sfixed64 = List[Int64]()
        self.packed_float = List[Float32]()
        self.packed_double = List[Float64]()
        self.packed_bool = List[Bool]()
        self.packed_nested_enum = List[Int32]()
        self.unpacked_int32 = List[Int32]()
        self.unpacked_int64 = List[Int64]()
        self.unpacked_uint32 = List[UInt32]()
        self.unpacked_uint64 = List[UInt64]()
        self.unpacked_sint32 = List[Int32]()
        self.unpacked_sint64 = List[Int64]()
        self.unpacked_fixed32 = List[UInt32]()
        self.unpacked_fixed64 = List[UInt64]()
        self.unpacked_sfixed32 = List[Int32]()
        self.unpacked_sfixed64 = List[Int64]()
        self.unpacked_float = List[Float32]()
        self.unpacked_double = List[Float64]()
        self.unpacked_bool = List[Bool]()
        self.unpacked_nested_enum = List[Int32]()
        self.map_int32_int32 = Dict[Int32, Int32]()
        self.map_int64_int64 = Dict[Int64, Int64]()
        self.map_uint32_uint32 = Dict[UInt32, UInt32]()
        self.map_uint64_uint64 = Dict[UInt64, UInt64]()
        self.map_sint32_sint32 = Dict[Int32, Int32]()
        self.map_sint64_sint64 = Dict[Int64, Int64]()
        self.map_fixed32_fixed32 = Dict[UInt32, UInt32]()
        self.map_fixed64_fixed64 = Dict[UInt64, UInt64]()
        self.map_sfixed32_sfixed32 = Dict[Int32, Int32]()
        self.map_sfixed64_sfixed64 = Dict[Int64, Int64]()
        self.map_int32_float = Dict[Int32, Float32]()
        self.map_int32_double = Dict[Int32, Float64]()
        self.map_bool_bool = Dict[Bool, Bool]()
        self.map_string_string = Dict[String, String]()
        self.map_string_bytes = Dict[String, List[Byte]]()
        self.map_string_nested_message = Dict[
            String, TestAllTypesProto3_NestedMessage
        ]()
        self.map_string_foreign_message = Dict[String, ForeignMessage]()
        self.map_string_nested_enum = Dict[String, Int32]()
        self.map_string_foreign_enum = Dict[String, Int32]()
        self.oneof_uint32 = 0
        self.oneof_nested_message = List[TestAllTypesProto3_NestedMessage]()
        self.oneof_string = String()
        self.oneof_bytes = List[Byte]()
        self.oneof_bool = False
        self.oneof_uint64 = 0
        self.oneof_float = 0.0
        self.oneof_double = 0.0
        self.oneof_enum = 0
        self.oneof_null_value = 0
        self.optional_bool_wrapper = None
        self.optional_int32_wrapper = None
        self.optional_int64_wrapper = None
        self.optional_uint32_wrapper = None
        self.optional_uint64_wrapper = None
        self.optional_float_wrapper = None
        self.optional_double_wrapper = None
        self.optional_string_wrapper = None
        self.optional_bytes_wrapper = None
        self.repeated_bool_wrapper = List[BoolValue]()
        self.repeated_int32_wrapper = List[Int32Value]()
        self.repeated_int64_wrapper = List[Int64Value]()
        self.repeated_uint32_wrapper = List[UInt32Value]()
        self.repeated_uint64_wrapper = List[UInt64Value]()
        self.repeated_float_wrapper = List[FloatValue]()
        self.repeated_double_wrapper = List[DoubleValue]()
        self.repeated_string_wrapper = List[StringValue]()
        self.repeated_bytes_wrapper = List[BytesValue]()
        self.optional_duration = None
        self.optional_timestamp = None
        self.optional_field_mask = None
        self.optional_struct = None
        self.optional_any = None
        self.optional_value = None
        self.optional_null_value = 0
        self.repeated_duration = List[Duration]()
        self.repeated_timestamp = List[Timestamp]()
        self.repeated_fieldmask = List[FieldMask]()
        self.repeated_struct = List[Struct]()
        self.repeated_any = List[Any]()
        self.repeated_value = List[Value]()
        self.repeated_list_value = List[ListValue]()
        self.fieldname1 = 0
        self.field_name2 = 0
        self._field_name3 = 0
        self.field__name4_ = 0
        self.field0name5 = 0
        self.field_0_name6 = 0
        self.fieldName7 = 0
        self.FieldName8 = 0
        self.field_Name9 = 0
        self.Field_Name10 = 0
        self.FIELD_NAME11 = 0
        self.FIELD_name12 = 0
        self.__field_name13 = 0
        self.__Field_name14 = 0
        self.field__name15 = 0
        self.field__Name16 = 0
        self.field_name17__ = 0
        self.Field_name18__ = 0
        self._unknown = List[Byte]()
        self.oneof_field_case = 0

    def __deinit__(deinit self):
        """Destroys the message (explicit: breaks the trait-synthesis cycle for recursive types).
        """
        pass

    def __init__(out self, *, copy: Self):
        """Deep-copies another message.

        Args:
            copy: The message to copy from.
        """
        self.optional_int32 = copy.optional_int32
        self.optional_int64 = copy.optional_int64
        self.optional_uint32 = copy.optional_uint32
        self.optional_uint64 = copy.optional_uint64
        self.optional_sint32 = copy.optional_sint32
        self.optional_sint64 = copy.optional_sint64
        self.optional_fixed32 = copy.optional_fixed32
        self.optional_fixed64 = copy.optional_fixed64
        self.optional_sfixed32 = copy.optional_sfixed32
        self.optional_sfixed64 = copy.optional_sfixed64
        self.optional_float = copy.optional_float
        self.optional_double = copy.optional_double
        self.optional_bool = copy.optional_bool
        self.optional_string = copy.optional_string.copy()
        self.optional_bytes = copy.optional_bytes.copy()
        self.optional_nested_message = copy.optional_nested_message.copy()
        self.optional_foreign_message = copy.optional_foreign_message.copy()
        self.optional_nested_enum = copy.optional_nested_enum
        self.optional_foreign_enum = copy.optional_foreign_enum
        self.optional_aliased_enum = copy.optional_aliased_enum
        self.optional_string_piece = copy.optional_string_piece.copy()
        self.optional_cord = copy.optional_cord.copy()
        self.recursive_message = copy.recursive_message.copy()
        self.repeated_int32 = copy.repeated_int32.copy()
        self.repeated_int64 = copy.repeated_int64.copy()
        self.repeated_uint32 = copy.repeated_uint32.copy()
        self.repeated_uint64 = copy.repeated_uint64.copy()
        self.repeated_sint32 = copy.repeated_sint32.copy()
        self.repeated_sint64 = copy.repeated_sint64.copy()
        self.repeated_fixed32 = copy.repeated_fixed32.copy()
        self.repeated_fixed64 = copy.repeated_fixed64.copy()
        self.repeated_sfixed32 = copy.repeated_sfixed32.copy()
        self.repeated_sfixed64 = copy.repeated_sfixed64.copy()
        self.repeated_float = copy.repeated_float.copy()
        self.repeated_double = copy.repeated_double.copy()
        self.repeated_bool = copy.repeated_bool.copy()
        self.repeated_string = copy.repeated_string.copy()
        self.repeated_bytes = copy.repeated_bytes.copy()
        self.repeated_nested_message = copy.repeated_nested_message.copy()
        self.repeated_foreign_message = copy.repeated_foreign_message.copy()
        self.repeated_nested_enum = copy.repeated_nested_enum.copy()
        self.repeated_foreign_enum = copy.repeated_foreign_enum.copy()
        self.repeated_string_piece = copy.repeated_string_piece.copy()
        self.repeated_cord = copy.repeated_cord.copy()
        self.packed_int32 = copy.packed_int32.copy()
        self.packed_int64 = copy.packed_int64.copy()
        self.packed_uint32 = copy.packed_uint32.copy()
        self.packed_uint64 = copy.packed_uint64.copy()
        self.packed_sint32 = copy.packed_sint32.copy()
        self.packed_sint64 = copy.packed_sint64.copy()
        self.packed_fixed32 = copy.packed_fixed32.copy()
        self.packed_fixed64 = copy.packed_fixed64.copy()
        self.packed_sfixed32 = copy.packed_sfixed32.copy()
        self.packed_sfixed64 = copy.packed_sfixed64.copy()
        self.packed_float = copy.packed_float.copy()
        self.packed_double = copy.packed_double.copy()
        self.packed_bool = copy.packed_bool.copy()
        self.packed_nested_enum = copy.packed_nested_enum.copy()
        self.unpacked_int32 = copy.unpacked_int32.copy()
        self.unpacked_int64 = copy.unpacked_int64.copy()
        self.unpacked_uint32 = copy.unpacked_uint32.copy()
        self.unpacked_uint64 = copy.unpacked_uint64.copy()
        self.unpacked_sint32 = copy.unpacked_sint32.copy()
        self.unpacked_sint64 = copy.unpacked_sint64.copy()
        self.unpacked_fixed32 = copy.unpacked_fixed32.copy()
        self.unpacked_fixed64 = copy.unpacked_fixed64.copy()
        self.unpacked_sfixed32 = copy.unpacked_sfixed32.copy()
        self.unpacked_sfixed64 = copy.unpacked_sfixed64.copy()
        self.unpacked_float = copy.unpacked_float.copy()
        self.unpacked_double = copy.unpacked_double.copy()
        self.unpacked_bool = copy.unpacked_bool.copy()
        self.unpacked_nested_enum = copy.unpacked_nested_enum.copy()
        self.map_int32_int32 = copy.map_int32_int32.copy()
        self.map_int64_int64 = copy.map_int64_int64.copy()
        self.map_uint32_uint32 = copy.map_uint32_uint32.copy()
        self.map_uint64_uint64 = copy.map_uint64_uint64.copy()
        self.map_sint32_sint32 = copy.map_sint32_sint32.copy()
        self.map_sint64_sint64 = copy.map_sint64_sint64.copy()
        self.map_fixed32_fixed32 = copy.map_fixed32_fixed32.copy()
        self.map_fixed64_fixed64 = copy.map_fixed64_fixed64.copy()
        self.map_sfixed32_sfixed32 = copy.map_sfixed32_sfixed32.copy()
        self.map_sfixed64_sfixed64 = copy.map_sfixed64_sfixed64.copy()
        self.map_int32_float = copy.map_int32_float.copy()
        self.map_int32_double = copy.map_int32_double.copy()
        self.map_bool_bool = copy.map_bool_bool.copy()
        self.map_string_string = copy.map_string_string.copy()
        self.map_string_bytes = copy.map_string_bytes.copy()
        self.map_string_nested_message = copy.map_string_nested_message.copy()
        self.map_string_foreign_message = copy.map_string_foreign_message.copy()
        self.map_string_nested_enum = copy.map_string_nested_enum.copy()
        self.map_string_foreign_enum = copy.map_string_foreign_enum.copy()
        self.oneof_uint32 = copy.oneof_uint32
        self.oneof_nested_message = copy.oneof_nested_message.copy()
        self.oneof_string = copy.oneof_string.copy()
        self.oneof_bytes = copy.oneof_bytes.copy()
        self.oneof_bool = copy.oneof_bool
        self.oneof_uint64 = copy.oneof_uint64
        self.oneof_float = copy.oneof_float
        self.oneof_double = copy.oneof_double
        self.oneof_enum = copy.oneof_enum
        self.oneof_null_value = copy.oneof_null_value
        self.optional_bool_wrapper = copy.optional_bool_wrapper.copy()
        self.optional_int32_wrapper = copy.optional_int32_wrapper.copy()
        self.optional_int64_wrapper = copy.optional_int64_wrapper.copy()
        self.optional_uint32_wrapper = copy.optional_uint32_wrapper.copy()
        self.optional_uint64_wrapper = copy.optional_uint64_wrapper.copy()
        self.optional_float_wrapper = copy.optional_float_wrapper.copy()
        self.optional_double_wrapper = copy.optional_double_wrapper.copy()
        self.optional_string_wrapper = copy.optional_string_wrapper.copy()
        self.optional_bytes_wrapper = copy.optional_bytes_wrapper.copy()
        self.repeated_bool_wrapper = copy.repeated_bool_wrapper.copy()
        self.repeated_int32_wrapper = copy.repeated_int32_wrapper.copy()
        self.repeated_int64_wrapper = copy.repeated_int64_wrapper.copy()
        self.repeated_uint32_wrapper = copy.repeated_uint32_wrapper.copy()
        self.repeated_uint64_wrapper = copy.repeated_uint64_wrapper.copy()
        self.repeated_float_wrapper = copy.repeated_float_wrapper.copy()
        self.repeated_double_wrapper = copy.repeated_double_wrapper.copy()
        self.repeated_string_wrapper = copy.repeated_string_wrapper.copy()
        self.repeated_bytes_wrapper = copy.repeated_bytes_wrapper.copy()
        self.optional_duration = copy.optional_duration.copy()
        self.optional_timestamp = copy.optional_timestamp.copy()
        self.optional_field_mask = copy.optional_field_mask.copy()
        self.optional_struct = copy.optional_struct.copy()
        self.optional_any = copy.optional_any.copy()
        self.optional_value = copy.optional_value.copy()
        self.optional_null_value = copy.optional_null_value
        self.repeated_duration = copy.repeated_duration.copy()
        self.repeated_timestamp = copy.repeated_timestamp.copy()
        self.repeated_fieldmask = copy.repeated_fieldmask.copy()
        self.repeated_struct = copy.repeated_struct.copy()
        self.repeated_any = copy.repeated_any.copy()
        self.repeated_value = copy.repeated_value.copy()
        self.repeated_list_value = copy.repeated_list_value.copy()
        self.fieldname1 = copy.fieldname1
        self.field_name2 = copy.field_name2
        self._field_name3 = copy._field_name3
        self.field__name4_ = copy.field__name4_
        self.field0name5 = copy.field0name5
        self.field_0_name6 = copy.field_0_name6
        self.fieldName7 = copy.fieldName7
        self.FieldName8 = copy.FieldName8
        self.field_Name9 = copy.field_Name9
        self.Field_Name10 = copy.Field_Name10
        self.FIELD_NAME11 = copy.FIELD_NAME11
        self.FIELD_name12 = copy.FIELD_name12
        self.__field_name13 = copy.__field_name13
        self.__Field_name14 = copy.__Field_name14
        self.field__name15 = copy.field__name15
        self.field__Name16 = copy.field__Name16
        self.field_name17__ = copy.field_name17__
        self.Field_name18__ = copy.Field_name18__
        self.oneof_field_case = copy.oneof_field_case
        self._unknown = copy._unknown.copy()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.optional_int32 != 0:
            writer.int32(1, self.optional_int32)
        if self.optional_int64 != 0:
            writer.int64(2, self.optional_int64)
        if self.optional_uint32 != 0:
            writer.uint32(3, self.optional_uint32)
        if self.optional_uint64 != 0:
            writer.uint64(4, self.optional_uint64)
        if self.optional_sint32 != 0:
            writer.sint32(5, self.optional_sint32)
        if self.optional_sint64 != 0:
            writer.sint64(6, self.optional_sint64)
        if self.optional_fixed32 != 0:
            writer.fixed32_field(7, self.optional_fixed32)
        if self.optional_fixed64 != 0:
            writer.fixed64_field(8, self.optional_fixed64)
        if self.optional_sfixed32 != 0:
            writer.sfixed32_field(9, self.optional_sfixed32)
        if self.optional_sfixed64 != 0:
            writer.sfixed64_field(10, self.optional_sfixed64)
        if self.optional_float != 0.0:
            writer.float_field(11, self.optional_float)
        if self.optional_double != 0.0:
            writer.double_field(12, self.optional_double)
        if self.optional_bool:
            writer.bool_field(13, self.optional_bool)
        if self.optional_string.byte_length() != 0:
            writer.string_field(14, self.optional_string)
        if len(self.optional_bytes) != 0:
            writer.bytes_field(15, Span(self.optional_bytes))
        if len(self.optional_nested_message) != 0:
            var sub = WireWriter()
            self.optional_nested_message[0].encode_to(sub)
            writer.len_prefixed(18, Span(sub.buf))
        if self.optional_foreign_message:
            var sub = WireWriter()
            self.optional_foreign_message.value().encode_to(sub)
            writer.len_prefixed(19, Span(sub.buf))
        if self.optional_nested_enum != 0:
            writer.int32(21, self.optional_nested_enum)
        if self.optional_foreign_enum != 0:
            writer.int32(22, self.optional_foreign_enum)
        if self.optional_aliased_enum != 0:
            writer.int32(23, self.optional_aliased_enum)
        if self.optional_string_piece.byte_length() != 0:
            writer.string_field(24, self.optional_string_piece)
        if self.optional_cord.byte_length() != 0:
            writer.string_field(25, self.optional_cord)
        if len(self.recursive_message) != 0:
            var sub = WireWriter()
            self.recursive_message[0].encode_to(sub)
            writer.len_prefixed(27, Span(sub.buf))
        if len(self.repeated_int32) != 0:
            var sub = WireWriter()
            for v in self.repeated_int32:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(31, Span(sub.buf))
        if len(self.repeated_int64) != 0:
            var sub = WireWriter()
            for v in self.repeated_int64:
                sub.varint(UInt64(v))
            writer.len_prefixed(32, Span(sub.buf))
        if len(self.repeated_uint32) != 0:
            var sub = WireWriter()
            for v in self.repeated_uint32:
                sub.varint(UInt64(v))
            writer.len_prefixed(33, Span(sub.buf))
        if len(self.repeated_uint64) != 0:
            var sub = WireWriter()
            for v in self.repeated_uint64:
                sub.varint(v)
            writer.len_prefixed(34, Span(sub.buf))
        if len(self.repeated_sint32) != 0:
            var sub = WireWriter()
            for v in self.repeated_sint32:
                sub.varint(zigzag_encode64(Int64(v)))
            writer.len_prefixed(35, Span(sub.buf))
        if len(self.repeated_sint64) != 0:
            var sub = WireWriter()
            for v in self.repeated_sint64:
                sub.varint(zigzag_encode64(v))
            writer.len_prefixed(36, Span(sub.buf))
        if len(self.repeated_fixed32) != 0:
            var sub = WireWriter()
            for v in self.repeated_fixed32:
                sub.fixed32(v)
            writer.len_prefixed(37, Span(sub.buf))
        if len(self.repeated_fixed64) != 0:
            var sub = WireWriter()
            for v in self.repeated_fixed64:
                sub.fixed64(v)
            writer.len_prefixed(38, Span(sub.buf))
        if len(self.repeated_sfixed32) != 0:
            var sub = WireWriter()
            for v in self.repeated_sfixed32:
                sub.fixed32(UInt32(from_bits=v))
            writer.len_prefixed(39, Span(sub.buf))
        if len(self.repeated_sfixed64) != 0:
            var sub = WireWriter()
            for v in self.repeated_sfixed64:
                sub.fixed64(UInt64(from_bits=v))
            writer.len_prefixed(40, Span(sub.buf))
        if len(self.repeated_float) != 0:
            var sub = WireWriter()
            for v in self.repeated_float:
                sub.fixed32(UInt32(v.to_bits()))
            writer.len_prefixed(41, Span(sub.buf))
        if len(self.repeated_double) != 0:
            var sub = WireWriter()
            for v in self.repeated_double:
                sub.fixed64(UInt64(v.to_bits()))
            writer.len_prefixed(42, Span(sub.buf))
        if len(self.repeated_bool) != 0:
            var sub = WireWriter()
            for v in self.repeated_bool:
                sub.varint(UInt64(1) if v else UInt64(0))
            writer.len_prefixed(43, Span(sub.buf))
        for v in self.repeated_string:
            writer.string_field(44, v)
        for v in self.repeated_bytes:
            writer.bytes_field(45, Span(v))
        for v in self.repeated_nested_message:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(48, Span(sub.buf))
        for v in self.repeated_foreign_message:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(49, Span(sub.buf))
        if len(self.repeated_nested_enum) != 0:
            var sub = WireWriter()
            for v in self.repeated_nested_enum:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(51, Span(sub.buf))
        if len(self.repeated_foreign_enum) != 0:
            var sub = WireWriter()
            for v in self.repeated_foreign_enum:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(52, Span(sub.buf))
        for v in self.repeated_string_piece:
            writer.string_field(54, v)
        for v in self.repeated_cord:
            writer.string_field(55, v)
        for entry in self.map_int32_int32.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int32(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(56, Span(sub.buf))
        for entry in self.map_int64_int64.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int64(1, entry.key)
            if entry.value != 0:
                sub.int64(2, entry.value)
            writer.len_prefixed(57, Span(sub.buf))
        for entry in self.map_uint32_uint32.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.uint32(1, entry.key)
            if entry.value != 0:
                sub.uint32(2, entry.value)
            writer.len_prefixed(58, Span(sub.buf))
        for entry in self.map_uint64_uint64.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.uint64(1, entry.key)
            if entry.value != 0:
                sub.uint64(2, entry.value)
            writer.len_prefixed(59, Span(sub.buf))
        for entry in self.map_sint32_sint32.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.sint32(1, entry.key)
            if entry.value != 0:
                sub.sint32(2, entry.value)
            writer.len_prefixed(60, Span(sub.buf))
        for entry in self.map_sint64_sint64.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.sint64(1, entry.key)
            if entry.value != 0:
                sub.sint64(2, entry.value)
            writer.len_prefixed(61, Span(sub.buf))
        for entry in self.map_fixed32_fixed32.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.fixed32_field(1, entry.key)
            if entry.value != 0:
                sub.fixed32_field(2, entry.value)
            writer.len_prefixed(62, Span(sub.buf))
        for entry in self.map_fixed64_fixed64.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.fixed64_field(1, entry.key)
            if entry.value != 0:
                sub.fixed64_field(2, entry.value)
            writer.len_prefixed(63, Span(sub.buf))
        for entry in self.map_sfixed32_sfixed32.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.sfixed32_field(1, entry.key)
            if entry.value != 0:
                sub.sfixed32_field(2, entry.value)
            writer.len_prefixed(64, Span(sub.buf))
        for entry in self.map_sfixed64_sfixed64.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.sfixed64_field(1, entry.key)
            if entry.value != 0:
                sub.sfixed64_field(2, entry.value)
            writer.len_prefixed(65, Span(sub.buf))
        for entry in self.map_int32_float.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int32(1, entry.key)
            if entry.value != 0.0:
                sub.float_field(2, entry.value)
            writer.len_prefixed(66, Span(sub.buf))
        for entry in self.map_int32_double.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int32(1, entry.key)
            if entry.value != 0.0:
                sub.double_field(2, entry.value)
            writer.len_prefixed(67, Span(sub.buf))
        for entry in self.map_bool_bool.items():
            var sub = WireWriter()
            if entry.key:
                sub.bool_field(1, entry.key)
            if entry.value:
                sub.bool_field(2, entry.value)
            writer.len_prefixed(68, Span(sub.buf))
        for entry in self.map_string_string.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(69, Span(sub.buf))
        for entry in self.map_string_bytes.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if len(entry.value) != 0:
                sub.bytes_field(2, Span(entry.value))
            writer.len_prefixed(70, Span(sub.buf))
        for entry in self.map_string_nested_message.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            var vsub = WireWriter()
            entry.value.encode_to(vsub)
            sub.len_prefixed(2, Span(vsub.buf))
            writer.len_prefixed(71, Span(sub.buf))
        for entry in self.map_string_foreign_message.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            var vsub = WireWriter()
            entry.value.encode_to(vsub)
            sub.len_prefixed(2, Span(vsub.buf))
            writer.len_prefixed(72, Span(sub.buf))
        for entry in self.map_string_nested_enum.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(73, Span(sub.buf))
        for entry in self.map_string_foreign_enum.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(74, Span(sub.buf))
        if len(self.packed_int32) != 0:
            var sub = WireWriter()
            for v in self.packed_int32:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(75, Span(sub.buf))
        if len(self.packed_int64) != 0:
            var sub = WireWriter()
            for v in self.packed_int64:
                sub.varint(UInt64(v))
            writer.len_prefixed(76, Span(sub.buf))
        if len(self.packed_uint32) != 0:
            var sub = WireWriter()
            for v in self.packed_uint32:
                sub.varint(UInt64(v))
            writer.len_prefixed(77, Span(sub.buf))
        if len(self.packed_uint64) != 0:
            var sub = WireWriter()
            for v in self.packed_uint64:
                sub.varint(v)
            writer.len_prefixed(78, Span(sub.buf))
        if len(self.packed_sint32) != 0:
            var sub = WireWriter()
            for v in self.packed_sint32:
                sub.varint(zigzag_encode64(Int64(v)))
            writer.len_prefixed(79, Span(sub.buf))
        if len(self.packed_sint64) != 0:
            var sub = WireWriter()
            for v in self.packed_sint64:
                sub.varint(zigzag_encode64(v))
            writer.len_prefixed(80, Span(sub.buf))
        if len(self.packed_fixed32) != 0:
            var sub = WireWriter()
            for v in self.packed_fixed32:
                sub.fixed32(v)
            writer.len_prefixed(81, Span(sub.buf))
        if len(self.packed_fixed64) != 0:
            var sub = WireWriter()
            for v in self.packed_fixed64:
                sub.fixed64(v)
            writer.len_prefixed(82, Span(sub.buf))
        if len(self.packed_sfixed32) != 0:
            var sub = WireWriter()
            for v in self.packed_sfixed32:
                sub.fixed32(UInt32(from_bits=v))
            writer.len_prefixed(83, Span(sub.buf))
        if len(self.packed_sfixed64) != 0:
            var sub = WireWriter()
            for v in self.packed_sfixed64:
                sub.fixed64(UInt64(from_bits=v))
            writer.len_prefixed(84, Span(sub.buf))
        if len(self.packed_float) != 0:
            var sub = WireWriter()
            for v in self.packed_float:
                sub.fixed32(UInt32(v.to_bits()))
            writer.len_prefixed(85, Span(sub.buf))
        if len(self.packed_double) != 0:
            var sub = WireWriter()
            for v in self.packed_double:
                sub.fixed64(UInt64(v.to_bits()))
            writer.len_prefixed(86, Span(sub.buf))
        if len(self.packed_bool) != 0:
            var sub = WireWriter()
            for v in self.packed_bool:
                sub.varint(UInt64(1) if v else UInt64(0))
            writer.len_prefixed(87, Span(sub.buf))
        if len(self.packed_nested_enum) != 0:
            var sub = WireWriter()
            for v in self.packed_nested_enum:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(88, Span(sub.buf))
        for v in self.unpacked_int32:
            writer.int32(89, v)
        for v in self.unpacked_int64:
            writer.int64(90, v)
        for v in self.unpacked_uint32:
            writer.uint32(91, v)
        for v in self.unpacked_uint64:
            writer.uint64(92, v)
        for v in self.unpacked_sint32:
            writer.sint32(93, v)
        for v in self.unpacked_sint64:
            writer.sint64(94, v)
        for v in self.unpacked_fixed32:
            writer.fixed32_field(95, v)
        for v in self.unpacked_fixed64:
            writer.fixed64_field(96, v)
        for v in self.unpacked_sfixed32:
            writer.sfixed32_field(97, v)
        for v in self.unpacked_sfixed64:
            writer.sfixed64_field(98, v)
        for v in self.unpacked_float:
            writer.float_field(99, v)
        for v in self.unpacked_double:
            writer.double_field(100, v)
        for v in self.unpacked_bool:
            writer.bool_field(101, v)
        for v in self.unpacked_nested_enum:
            writer.int32(102, v)
        if self.optional_bool_wrapper:
            var sub = WireWriter()
            self.optional_bool_wrapper.value().encode_to(sub)
            writer.len_prefixed(201, Span(sub.buf))
        if self.optional_int32_wrapper:
            var sub = WireWriter()
            self.optional_int32_wrapper.value().encode_to(sub)
            writer.len_prefixed(202, Span(sub.buf))
        if self.optional_int64_wrapper:
            var sub = WireWriter()
            self.optional_int64_wrapper.value().encode_to(sub)
            writer.len_prefixed(203, Span(sub.buf))
        if self.optional_uint32_wrapper:
            var sub = WireWriter()
            self.optional_uint32_wrapper.value().encode_to(sub)
            writer.len_prefixed(204, Span(sub.buf))
        if self.optional_uint64_wrapper:
            var sub = WireWriter()
            self.optional_uint64_wrapper.value().encode_to(sub)
            writer.len_prefixed(205, Span(sub.buf))
        if self.optional_float_wrapper:
            var sub = WireWriter()
            self.optional_float_wrapper.value().encode_to(sub)
            writer.len_prefixed(206, Span(sub.buf))
        if self.optional_double_wrapper:
            var sub = WireWriter()
            self.optional_double_wrapper.value().encode_to(sub)
            writer.len_prefixed(207, Span(sub.buf))
        if self.optional_string_wrapper:
            var sub = WireWriter()
            self.optional_string_wrapper.value().encode_to(sub)
            writer.len_prefixed(208, Span(sub.buf))
        if self.optional_bytes_wrapper:
            var sub = WireWriter()
            self.optional_bytes_wrapper.value().encode_to(sub)
            writer.len_prefixed(209, Span(sub.buf))
        for v in self.repeated_bool_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(211, Span(sub.buf))
        for v in self.repeated_int32_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(212, Span(sub.buf))
        for v in self.repeated_int64_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(213, Span(sub.buf))
        for v in self.repeated_uint32_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(214, Span(sub.buf))
        for v in self.repeated_uint64_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(215, Span(sub.buf))
        for v in self.repeated_float_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(216, Span(sub.buf))
        for v in self.repeated_double_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(217, Span(sub.buf))
        for v in self.repeated_string_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(218, Span(sub.buf))
        for v in self.repeated_bytes_wrapper:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(219, Span(sub.buf))
        if self.optional_duration:
            var sub = WireWriter()
            self.optional_duration.value().encode_to(sub)
            writer.len_prefixed(301, Span(sub.buf))
        if self.optional_timestamp:
            var sub = WireWriter()
            self.optional_timestamp.value().encode_to(sub)
            writer.len_prefixed(302, Span(sub.buf))
        if self.optional_field_mask:
            var sub = WireWriter()
            self.optional_field_mask.value().encode_to(sub)
            writer.len_prefixed(303, Span(sub.buf))
        if self.optional_struct:
            var sub = WireWriter()
            self.optional_struct.value().encode_to(sub)
            writer.len_prefixed(304, Span(sub.buf))
        if self.optional_any:
            var sub = WireWriter()
            self.optional_any.value().encode_to(sub)
            writer.len_prefixed(305, Span(sub.buf))
        if self.optional_value:
            var sub = WireWriter()
            self.optional_value.value().encode_to(sub)
            writer.len_prefixed(306, Span(sub.buf))
        if self.optional_null_value != 0:
            writer.int32(307, self.optional_null_value)
        for v in self.repeated_duration:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(311, Span(sub.buf))
        for v in self.repeated_timestamp:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(312, Span(sub.buf))
        for v in self.repeated_fieldmask:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(313, Span(sub.buf))
        for v in self.repeated_any:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(315, Span(sub.buf))
        for v in self.repeated_value:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(316, Span(sub.buf))
        for v in self.repeated_list_value:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(317, Span(sub.buf))
        for v in self.repeated_struct:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(324, Span(sub.buf))
        if self.fieldname1 != 0:
            writer.int32(401, self.fieldname1)
        if self.field_name2 != 0:
            writer.int32(402, self.field_name2)
        if self._field_name3 != 0:
            writer.int32(403, self._field_name3)
        if self.field__name4_ != 0:
            writer.int32(404, self.field__name4_)
        if self.field0name5 != 0:
            writer.int32(405, self.field0name5)
        if self.field_0_name6 != 0:
            writer.int32(406, self.field_0_name6)
        if self.fieldName7 != 0:
            writer.int32(407, self.fieldName7)
        if self.FieldName8 != 0:
            writer.int32(408, self.FieldName8)
        if self.field_Name9 != 0:
            writer.int32(409, self.field_Name9)
        if self.Field_Name10 != 0:
            writer.int32(410, self.Field_Name10)
        if self.FIELD_NAME11 != 0:
            writer.int32(411, self.FIELD_NAME11)
        if self.FIELD_name12 != 0:
            writer.int32(412, self.FIELD_name12)
        if self.__field_name13 != 0:
            writer.int32(413, self.__field_name13)
        if self.__Field_name14 != 0:
            writer.int32(414, self.__Field_name14)
        if self.field__name15 != 0:
            writer.int32(415, self.field__name15)
        if self.field__Name16 != 0:
            writer.int32(416, self.field__Name16)
        if self.field_name17__ != 0:
            writer.int32(417, self.field_name17__)
        if self.Field_name18__ != 0:
            writer.int32(418, self.Field_name18__)
        if self.oneof_field_case == 111:
            writer.uint32(111, self.oneof_uint32)
        elif self.oneof_field_case == 112:
            if len(self.oneof_nested_message) != 0:
                var sub = WireWriter()
                self.oneof_nested_message[0].encode_to(sub)
                writer.len_prefixed(112, Span(sub.buf))
        elif self.oneof_field_case == 113:
            writer.string_field(113, self.oneof_string)
        elif self.oneof_field_case == 114:
            writer.bytes_field(114, Span(self.oneof_bytes))
        elif self.oneof_field_case == 115:
            writer.bool_field(115, self.oneof_bool)
        elif self.oneof_field_case == 116:
            writer.uint64(116, self.oneof_uint64)
        elif self.oneof_field_case == 117:
            writer.float_field(117, self.oneof_float)
        elif self.oneof_field_case == 118:
            writer.double_field(118, self.oneof_double)
        elif self.oneof_field_case == 119:
            writer.int32(119, self.oneof_enum)
        elif self.oneof_field_case == 120:
            writer.int32(120, self.oneof_null_value)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_int32 = reader.int32_value()
            elif field == 2:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_int64 = reader.int64_value()
            elif field == 3:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_uint32 = reader.uint32_value()
            elif field == 4:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_uint64 = reader.varint()
            elif field == 5:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_sint32 = reader.sint32_value()
            elif field == 6:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_sint64 = reader.sint64_value()
            elif field == 7:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_fixed32 = reader.fixed32()
            elif field == 8:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_fixed64 = reader.fixed64()
            elif field == 9:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_sfixed32 = reader.sfixed32_value()
            elif field == 10:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_sfixed64 = reader.sfixed64_value()
            elif field == 11:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_float = reader.float_value()
            elif field == 12:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_double = reader.double_value()
            elif field == 13:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_bool = reader.bool_value()
            elif field == 14:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_string = reader.string_value()
            elif field == 15:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_bytes = reader.bytes_value()
            elif field == 18:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: TestAllTypesProto3_NestedMessage
                    if len(self.optional_nested_message) != 0:
                        m = self.optional_nested_message.pop()
                    else:
                        m = TestAllTypesProto3_NestedMessage()
                    m.merge_from(sub)
                    self.optional_nested_message.append(m^)
            elif field == 19:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: ForeignMessage
                    if self.optional_foreign_message:
                        m = self.optional_foreign_message.take()
                    else:
                        m = ForeignMessage()
                    m.merge_from(sub)
                    self.optional_foreign_message = m^
            elif field == 21:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_nested_enum = reader.int32_value()
            elif field == 22:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_foreign_enum = reader.int32_value()
            elif field == 23:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_aliased_enum = reader.int32_value()
            elif field == 24:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_string_piece = reader.string_value()
            elif field == 25:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_cord = reader.string_value()
            elif field == 27:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: TestAllTypesProto3
                    if len(self.recursive_message) != 0:
                        m = self.recursive_message.pop()
                    else:
                        m = TestAllTypesProto3()
                    m.merge_from(sub)
                    self.recursive_message.append(m^)
            elif field == 31:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_int32.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_int32.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 32:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_int64.append(sub.int64_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_int64.append(reader.int64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 33:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_uint32.append(sub.uint32_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_uint32.append(reader.uint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 34:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_uint64.append(sub.varint())
                elif wire_type == WIRE_VARINT:
                    self.repeated_uint64.append(reader.varint())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 35:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_sint32.append(sub.sint32_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_sint32.append(reader.sint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 36:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_sint64.append(sub.sint64_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_sint64.append(reader.sint64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 37:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_fixed32.append(sub.fixed32())
                elif wire_type == WIRE_FIXED32:
                    self.repeated_fixed32.append(reader.fixed32())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 38:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_fixed64.append(sub.fixed64())
                elif wire_type == WIRE_FIXED64:
                    self.repeated_fixed64.append(reader.fixed64())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 39:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_sfixed32.append(sub.sfixed32_value())
                elif wire_type == WIRE_FIXED32:
                    self.repeated_sfixed32.append(reader.sfixed32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 40:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_sfixed64.append(sub.sfixed64_value())
                elif wire_type == WIRE_FIXED64:
                    self.repeated_sfixed64.append(reader.sfixed64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 41:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_float.append(sub.float_value())
                elif wire_type == WIRE_FIXED32:
                    self.repeated_float.append(reader.float_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 42:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_double.append(sub.double_value())
                elif wire_type == WIRE_FIXED64:
                    self.repeated_double.append(reader.double_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 43:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_bool.append(sub.bool_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_bool.append(reader.bool_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 44:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.repeated_string.append(reader.string_value())
            elif field == 45:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.repeated_bytes.append(reader.bytes_value())
            elif field == 48:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = TestAllTypesProto3_NestedMessage()
                    m.merge_from(sub)
                    self.repeated_nested_message.append(m^)
            elif field == 49:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = ForeignMessage()
                    m.merge_from(sub)
                    self.repeated_foreign_message.append(m^)
            elif field == 51:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_nested_enum.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_nested_enum.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 52:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.repeated_foreign_enum.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.repeated_foreign_enum.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 54:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.repeated_string_piece.append(reader.string_value())
            elif field == 55:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.repeated_cord.append(reader.string_value())
            elif field == 56:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: Int32 = 0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.int32_value()
                        elif etag[0] == 2:
                            value = sub.int32_value()
                        else:
                            sub.skip(etag[1])
                    self.map_int32_int32[key] = value
            elif field == 57:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: Int64 = 0
                    var value: Int64 = 0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.int64_value()
                        elif etag[0] == 2:
                            value = sub.int64_value()
                        else:
                            sub.skip(etag[1])
                    self.map_int64_int64[key] = value
            elif field == 58:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: UInt32 = 0
                    var value: UInt32 = 0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.uint32_value()
                        elif etag[0] == 2:
                            value = sub.uint32_value()
                        else:
                            sub.skip(etag[1])
                    self.map_uint32_uint32[key] = value
            elif field == 59:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: UInt64 = 0
                    var value: UInt64 = 0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.varint()
                        elif etag[0] == 2:
                            value = sub.varint()
                        else:
                            sub.skip(etag[1])
                    self.map_uint64_uint64[key] = value
            elif field == 60:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: Int32 = 0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.sint32_value()
                        elif etag[0] == 2:
                            value = sub.sint32_value()
                        else:
                            sub.skip(etag[1])
                    self.map_sint32_sint32[key] = value
            elif field == 61:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: Int64 = 0
                    var value: Int64 = 0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.sint64_value()
                        elif etag[0] == 2:
                            value = sub.sint64_value()
                        else:
                            sub.skip(etag[1])
                    self.map_sint64_sint64[key] = value
            elif field == 62:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: UInt32 = 0
                    var value: UInt32 = 0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.fixed32()
                        elif etag[0] == 2:
                            value = sub.fixed32()
                        else:
                            sub.skip(etag[1])
                    self.map_fixed32_fixed32[key] = value
            elif field == 63:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: UInt64 = 0
                    var value: UInt64 = 0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.fixed64()
                        elif etag[0] == 2:
                            value = sub.fixed64()
                        else:
                            sub.skip(etag[1])
                    self.map_fixed64_fixed64[key] = value
            elif field == 64:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: Int32 = 0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.sfixed32_value()
                        elif etag[0] == 2:
                            value = sub.sfixed32_value()
                        else:
                            sub.skip(etag[1])
                    self.map_sfixed32_sfixed32[key] = value
            elif field == 65:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: Int64 = 0
                    var value: Int64 = 0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.sfixed64_value()
                        elif etag[0] == 2:
                            value = sub.sfixed64_value()
                        else:
                            sub.skip(etag[1])
                    self.map_sfixed64_sfixed64[key] = value
            elif field == 66:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: Float32 = 0.0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.int32_value()
                        elif etag[0] == 2:
                            value = sub.float_value()
                        else:
                            sub.skip(etag[1])
                    self.map_int32_float[key] = value
            elif field == 67:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: Float64 = 0.0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.int32_value()
                        elif etag[0] == 2:
                            value = sub.double_value()
                        else:
                            sub.skip(etag[1])
                    self.map_int32_double[key] = value
            elif field == 68:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: Bool = False
                    var value: Bool = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.bool_value()
                        elif etag[0] == 2:
                            value = sub.bool_value()
                        else:
                            sub.skip(etag[1])
                    self.map_bool_bool[key] = value
            elif field == 69:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: String = String()
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.string_value()
                        elif etag[0] == 2:
                            value = sub.string_value()
                        else:
                            sub.skip(etag[1])
                    self.map_string_string[key^] = value^
            elif field == 70:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: List[Byte] = List[Byte]()
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.string_value()
                        elif etag[0] == 2:
                            value = sub.bytes_value()
                        else:
                            sub.skip(etag[1])
                    self.map_string_bytes[key^] = value^
            elif field == 71:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: TestAllTypesProto3_NestedMessage = (
                        TestAllTypesProto3_NestedMessage()
                    )
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.string_value()
                        elif etag[0] == 2:
                            var msub = sub.sub_reader()
                            value.merge_from(msub)
                        else:
                            sub.skip(etag[1])
                    self.map_string_nested_message[key^] = value^
            elif field == 72:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: ForeignMessage = ForeignMessage()
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.string_value()
                        elif etag[0] == 2:
                            var msub = sub.sub_reader()
                            value.merge_from(msub)
                        else:
                            sub.skip(etag[1])
                    self.map_string_foreign_message[key^] = value^
            elif field == 73:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.string_value()
                        elif etag[0] == 2:
                            value = sub.int32_value()
                        else:
                            sub.skip(etag[1])
                    self.map_string_nested_enum[key^] = value
            elif field == 74:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1:
                            key = sub.string_value()
                        elif etag[0] == 2:
                            value = sub.int32_value()
                        else:
                            sub.skip(etag[1])
                    self.map_string_foreign_enum[key^] = value
            elif field == 75:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_int32.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_int32.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 76:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_int64.append(sub.int64_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_int64.append(reader.int64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 77:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_uint32.append(sub.uint32_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_uint32.append(reader.uint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 78:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_uint64.append(sub.varint())
                elif wire_type == WIRE_VARINT:
                    self.packed_uint64.append(reader.varint())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 79:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_sint32.append(sub.sint32_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_sint32.append(reader.sint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 80:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_sint64.append(sub.sint64_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_sint64.append(reader.sint64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 81:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_fixed32.append(sub.fixed32())
                elif wire_type == WIRE_FIXED32:
                    self.packed_fixed32.append(reader.fixed32())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 82:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_fixed64.append(sub.fixed64())
                elif wire_type == WIRE_FIXED64:
                    self.packed_fixed64.append(reader.fixed64())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 83:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_sfixed32.append(sub.sfixed32_value())
                elif wire_type == WIRE_FIXED32:
                    self.packed_sfixed32.append(reader.sfixed32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 84:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_sfixed64.append(sub.sfixed64_value())
                elif wire_type == WIRE_FIXED64:
                    self.packed_sfixed64.append(reader.sfixed64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 85:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_float.append(sub.float_value())
                elif wire_type == WIRE_FIXED32:
                    self.packed_float.append(reader.float_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 86:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_double.append(sub.double_value())
                elif wire_type == WIRE_FIXED64:
                    self.packed_double.append(reader.double_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 87:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_bool.append(sub.bool_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_bool.append(reader.bool_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 88:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_nested_enum.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_nested_enum.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 89:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_int32.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_int32.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 90:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_int64.append(sub.int64_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_int64.append(reader.int64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 91:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_uint32.append(sub.uint32_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_uint32.append(reader.uint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 92:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_uint64.append(sub.varint())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_uint64.append(reader.varint())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 93:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_sint32.append(sub.sint32_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_sint32.append(reader.sint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 94:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_sint64.append(sub.sint64_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_sint64.append(reader.sint64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 95:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_fixed32.append(sub.fixed32())
                elif wire_type == WIRE_FIXED32:
                    self.unpacked_fixed32.append(reader.fixed32())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 96:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_fixed64.append(sub.fixed64())
                elif wire_type == WIRE_FIXED64:
                    self.unpacked_fixed64.append(reader.fixed64())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 97:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_sfixed32.append(sub.sfixed32_value())
                elif wire_type == WIRE_FIXED32:
                    self.unpacked_sfixed32.append(reader.sfixed32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 98:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_sfixed64.append(sub.sfixed64_value())
                elif wire_type == WIRE_FIXED64:
                    self.unpacked_sfixed64.append(reader.sfixed64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 99:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_float.append(sub.float_value())
                elif wire_type == WIRE_FIXED32:
                    self.unpacked_float.append(reader.float_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 100:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_double.append(sub.double_value())
                elif wire_type == WIRE_FIXED64:
                    self.unpacked_double.append(reader.double_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 101:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_bool.append(sub.bool_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_bool.append(reader.bool_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 102:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.unpacked_nested_enum.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.unpacked_nested_enum.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 111:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_uint32 = reader.uint32_value()
                    self.oneof_field_case = 111
                    self.oneof_nested_message = List[
                        TestAllTypesProto3_NestedMessage
                    ]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 112:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: TestAllTypesProto3_NestedMessage
                    if (
                        self.oneof_field_case == 112
                        and len(self.oneof_nested_message) != 0
                    ):
                        m = self.oneof_nested_message.pop()
                    else:
                        m = TestAllTypesProto3_NestedMessage()
                    m.merge_from(sub)
                    self.oneof_nested_message.append(m^)
                    self.oneof_field_case = 112
                    self.oneof_uint32 = 0
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 113:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_string = reader.string_value()
                    self.oneof_field_case = 113
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[
                        TestAllTypesProto3_NestedMessage
                    ]()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 114:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_bytes = reader.bytes_value()
                    self.oneof_field_case = 114
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[
                        TestAllTypesProto3_NestedMessage
                    ]()
                    self.oneof_string = String()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 115:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_bool = reader.bool_value()
                    self.oneof_field_case = 115
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[
                        TestAllTypesProto3_NestedMessage
                    ]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 116:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_uint64 = reader.varint()
                    self.oneof_field_case = 116
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[
                        TestAllTypesProto3_NestedMessage
                    ]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 117:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_float = reader.float_value()
                    self.oneof_field_case = 117
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[
                        TestAllTypesProto3_NestedMessage
                    ]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 118:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_double = reader.double_value()
                    self.oneof_field_case = 118
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[
                        TestAllTypesProto3_NestedMessage
                    ]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_enum = 0
                    self.oneof_null_value = 0
            elif field == 119:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_enum = reader.int32_value()
                    self.oneof_field_case = 119
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[
                        TestAllTypesProto3_NestedMessage
                    ]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_null_value = 0
            elif field == 120:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.oneof_null_value = reader.int32_value()
                    self.oneof_field_case = 120
                    self.oneof_uint32 = 0
                    self.oneof_nested_message = List[
                        TestAllTypesProto3_NestedMessage
                    ]()
                    self.oneof_string = String()
                    self.oneof_bytes = List[Byte]()
                    self.oneof_bool = False
                    self.oneof_uint64 = 0
                    self.oneof_float = 0.0
                    self.oneof_double = 0.0
                    self.oneof_enum = 0
            elif field == 201:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: BoolValue
                    if self.optional_bool_wrapper:
                        m = self.optional_bool_wrapper.take()
                    else:
                        m = BoolValue()
                    m.merge_from(sub)
                    self.optional_bool_wrapper = m^
            elif field == 202:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Int32Value
                    if self.optional_int32_wrapper:
                        m = self.optional_int32_wrapper.take()
                    else:
                        m = Int32Value()
                    m.merge_from(sub)
                    self.optional_int32_wrapper = m^
            elif field == 203:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Int64Value
                    if self.optional_int64_wrapper:
                        m = self.optional_int64_wrapper.take()
                    else:
                        m = Int64Value()
                    m.merge_from(sub)
                    self.optional_int64_wrapper = m^
            elif field == 204:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: UInt32Value
                    if self.optional_uint32_wrapper:
                        m = self.optional_uint32_wrapper.take()
                    else:
                        m = UInt32Value()
                    m.merge_from(sub)
                    self.optional_uint32_wrapper = m^
            elif field == 205:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: UInt64Value
                    if self.optional_uint64_wrapper:
                        m = self.optional_uint64_wrapper.take()
                    else:
                        m = UInt64Value()
                    m.merge_from(sub)
                    self.optional_uint64_wrapper = m^
            elif field == 206:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: FloatValue
                    if self.optional_float_wrapper:
                        m = self.optional_float_wrapper.take()
                    else:
                        m = FloatValue()
                    m.merge_from(sub)
                    self.optional_float_wrapper = m^
            elif field == 207:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: DoubleValue
                    if self.optional_double_wrapper:
                        m = self.optional_double_wrapper.take()
                    else:
                        m = DoubleValue()
                    m.merge_from(sub)
                    self.optional_double_wrapper = m^
            elif field == 208:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: StringValue
                    if self.optional_string_wrapper:
                        m = self.optional_string_wrapper.take()
                    else:
                        m = StringValue()
                    m.merge_from(sub)
                    self.optional_string_wrapper = m^
            elif field == 209:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: BytesValue
                    if self.optional_bytes_wrapper:
                        m = self.optional_bytes_wrapper.take()
                    else:
                        m = BytesValue()
                    m.merge_from(sub)
                    self.optional_bytes_wrapper = m^
            elif field == 211:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = BoolValue()
                    m.merge_from(sub)
                    self.repeated_bool_wrapper.append(m^)
            elif field == 212:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Int32Value()
                    m.merge_from(sub)
                    self.repeated_int32_wrapper.append(m^)
            elif field == 213:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Int64Value()
                    m.merge_from(sub)
                    self.repeated_int64_wrapper.append(m^)
            elif field == 214:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = UInt32Value()
                    m.merge_from(sub)
                    self.repeated_uint32_wrapper.append(m^)
            elif field == 215:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = UInt64Value()
                    m.merge_from(sub)
                    self.repeated_uint64_wrapper.append(m^)
            elif field == 216:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = FloatValue()
                    m.merge_from(sub)
                    self.repeated_float_wrapper.append(m^)
            elif field == 217:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = DoubleValue()
                    m.merge_from(sub)
                    self.repeated_double_wrapper.append(m^)
            elif field == 218:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = StringValue()
                    m.merge_from(sub)
                    self.repeated_string_wrapper.append(m^)
            elif field == 219:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = BytesValue()
                    m.merge_from(sub)
                    self.repeated_bytes_wrapper.append(m^)
            elif field == 301:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Duration
                    if self.optional_duration:
                        m = self.optional_duration.take()
                    else:
                        m = Duration()
                    m.merge_from(sub)
                    self.optional_duration = m^
            elif field == 302:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Timestamp
                    if self.optional_timestamp:
                        m = self.optional_timestamp.take()
                    else:
                        m = Timestamp()
                    m.merge_from(sub)
                    self.optional_timestamp = m^
            elif field == 303:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: FieldMask
                    if self.optional_field_mask:
                        m = self.optional_field_mask.take()
                    else:
                        m = FieldMask()
                    m.merge_from(sub)
                    self.optional_field_mask = m^
            elif field == 304:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Struct
                    if self.optional_struct:
                        m = self.optional_struct.take()
                    else:
                        m = Struct()
                    m.merge_from(sub)
                    self.optional_struct = m^
            elif field == 305:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Any
                    if self.optional_any:
                        m = self.optional_any.take()
                    else:
                        m = Any()
                    m.merge_from(sub)
                    self.optional_any = m^
            elif field == 306:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Value
                    if self.optional_value:
                        m = self.optional_value.take()
                    else:
                        m = Value()
                    m.merge_from(sub)
                    self.optional_value = m^
            elif field == 307:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.optional_null_value = reader.int32_value()
            elif field == 311:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Duration()
                    m.merge_from(sub)
                    self.repeated_duration.append(m^)
            elif field == 312:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Timestamp()
                    m.merge_from(sub)
                    self.repeated_timestamp.append(m^)
            elif field == 313:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = FieldMask()
                    m.merge_from(sub)
                    self.repeated_fieldmask.append(m^)
            elif field == 315:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Any()
                    m.merge_from(sub)
                    self.repeated_any.append(m^)
            elif field == 316:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Value()
                    m.merge_from(sub)
                    self.repeated_value.append(m^)
            elif field == 317:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = ListValue()
                    m.merge_from(sub)
                    self.repeated_list_value.append(m^)
            elif field == 324:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Struct()
                    m.merge_from(sub)
                    self.repeated_struct.append(m^)
            elif field == 401:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.fieldname1 = reader.int32_value()
            elif field == 402:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field_name2 = reader.int32_value()
            elif field == 403:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self._field_name3 = reader.int32_value()
            elif field == 404:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field__name4_ = reader.int32_value()
            elif field == 405:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field0name5 = reader.int32_value()
            elif field == 406:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field_0_name6 = reader.int32_value()
            elif field == 407:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.fieldName7 = reader.int32_value()
            elif field == 408:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.FieldName8 = reader.int32_value()
            elif field == 409:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field_Name9 = reader.int32_value()
            elif field == 410:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.Field_Name10 = reader.int32_value()
            elif field == 411:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.FIELD_NAME11 = reader.int32_value()
            elif field == 412:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.FIELD_name12 = reader.int32_value()
            elif field == 413:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.__field_name13 = reader.int32_value()
            elif field == 414:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.__Field_name14 = reader.int32_value()
            elif field == 415:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field__name15 = reader.int32_value()
            elif field == 416:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field__Name16 = reader.int32_value()
            elif field == 417:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.field_name17__ = reader.int32_value()
            elif field == 418:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.Field_name18__ = reader.int32_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)


struct ForeignMessage(Copyable, Defaultable, Movable, ProtoMessage):
    """Generated from the `ForeignMessage` protobuf message."""

    var c: Int32
    """Field `c` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.c = 0
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.c != 0:
            writer.int32(1, self.c)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.c = reader.int32_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)


struct NullHypothesisProto3(Copyable, Defaultable, Movable, ProtoMessage):
    """Generated from the `NullHypothesisProto3` protobuf message."""

    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes an empty message."""
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Args:
            writer: Destination wire-format writer.
        """
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            reader.capture_field(tag[0], tag[1], self._unknown)


struct EnumOnlyProto3_Bool:
    """Values of the `Bool` protobuf enum (fields carry `Int32`)."""

    comptime kFalse = 0
    """`kFalse` = 0."""
    comptime kTrue = 1
    """`kTrue` = 1."""


struct EnumOnlyProto3(Copyable, Defaultable, Movable, ProtoMessage):
    """Generated from the `EnumOnlyProto3` protobuf message."""

    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes an empty message."""
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Args:
            writer: Destination wire-format writer.
        """
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            reader.capture_field(tag[0], tag[1], self._unknown)
