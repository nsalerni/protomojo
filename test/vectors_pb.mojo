# Generated by protoc-gen-mojo from vectors.proto. Do not edit.

"""Generated protobuf messages from `vectors.proto`.

Regenerate with tools/protoc-gen-mojo; do not edit by hand.
"""

from proto import (
    WIRE_FIXED32,
    WIRE_FIXED64,
    WIRE_LEN,
    WIRE_VARINT,
    ProtoMessage,
    WireReader,
    WireWriter,
    zigzag_encode32,
    zigzag_encode64,
)
from proto import (
    ProtoJsonMessage,
    ProtoJsonReader,
    ProtoJsonWriter,
)
from duration_pb import Duration
from empty_pb import Empty
from field_mask_pb import FieldMask
from timestamp_pb import Timestamp
from wrappers_pb import BoolValue, BytesValue, DoubleValue, FloatValue, Int32Value, Int64Value, StringValue, UInt32Value, UInt64Value


struct Status:
    """Values of the `Status` protobuf enum (fields carry `Int32`)."""

    comptime STATUS_UNSPECIFIED = 0
    """`STATUS_UNSPECIFIED` = 0."""
    comptime STATUS_ACTIVE = 1
    """`STATUS_ACTIVE` = 1."""
    comptime STATUS_ENABLED = 1
    """`STATUS_ENABLED` = 1."""
    comptime STATUS_PAUSED = 2
    """`STATUS_PAUSED` = 2."""
    comptime STATUS_NEGATIVE = -1
    """`STATUS_NEGATIVE` = -1."""


struct Scalars(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `Scalars` protobuf message."""

    var f_int32: Int32
    """Field `f_int32` (number 1)."""
    var f_int64: Int64
    """Field `f_int64` (number 2)."""
    var f_uint32: UInt32
    """Field `f_uint32` (number 3)."""
    var f_uint64: UInt64
    """Field `f_uint64` (number 4)."""
    var f_sint32: Int32
    """Field `f_sint32` (number 5)."""
    var f_sint64: Int64
    """Field `f_sint64` (number 6)."""
    var f_bool: Bool
    """Field `f_bool` (number 7)."""
    var f_fixed32: UInt32
    """Field `f_fixed32` (number 8)."""
    var f_fixed64: UInt64
    """Field `f_fixed64` (number 9)."""
    var f_sfixed32: Int32
    """Field `f_sfixed32` (number 10)."""
    var f_sfixed64: Int64
    """Field `f_sfixed64` (number 11)."""
    var f_float: Float32
    """Field `f_float` (number 12)."""
    var f_double: Float64
    """Field `f_double` (number 13)."""
    var f_string: String
    """Field `f_string` (number 14)."""
    var f_bytes: List[Byte]
    """Field `f_bytes` (number 15)."""
    var f_big_field: Int32
    """Field `f_big_field` (number 1000)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.f_int32 = 0
        self.f_int64 = 0
        self.f_uint32 = 0
        self.f_uint64 = 0
        self.f_sint32 = 0
        self.f_sint64 = 0
        self.f_bool = False
        self.f_fixed32 = 0
        self.f_fixed64 = 0
        self.f_sfixed32 = 0
        self.f_sfixed64 = 0
        self.f_float = 0.0
        self.f_double = 0.0
        self.f_string = String()
        self.f_bytes = List[Byte]()
        self.f_big_field = 0
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.f_int32 != 0:
            writer.int32(1, self.f_int32)
        if self.f_int64 != 0:
            writer.int64(2, self.f_int64)
        if self.f_uint32 != 0:
            writer.uint32(3, self.f_uint32)
        if self.f_uint64 != 0:
            writer.uint64(4, self.f_uint64)
        if self.f_sint32 != 0:
            writer.sint32(5, self.f_sint32)
        if self.f_sint64 != 0:
            writer.sint64(6, self.f_sint64)
        if self.f_bool:
            writer.bool_field(7, self.f_bool)
        if self.f_fixed32 != 0:
            writer.fixed32_field(8, self.f_fixed32)
        if self.f_fixed64 != 0:
            writer.fixed64_field(9, self.f_fixed64)
        if self.f_sfixed32 != 0:
            writer.sfixed32_field(10, self.f_sfixed32)
        if self.f_sfixed64 != 0:
            writer.sfixed64_field(11, self.f_sfixed64)
        if UInt32(self.f_float.to_bits()) != 0:
            writer.float_field(12, self.f_float)
        if UInt64(self.f_double.to_bits()) != 0:
            writer.double_field(13, self.f_double)
        if self.f_string.byte_length() != 0:
            writer.string_field(14, self.f_string)
        if len(self.f_bytes) != 0:
            writer.bytes_field(15, Span(self.f_bytes))
        if self.f_big_field != 0:
            writer.int32(1000, self.f_big_field)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_int32 = reader.int32_value()
            elif field == 2:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_int64 = reader.int64_value()
            elif field == 3:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_uint32 = reader.uint32_value()
            elif field == 4:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_uint64 = reader.varint()
            elif field == 5:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_sint32 = reader.sint32_value()
            elif field == 6:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_sint64 = reader.sint64_value()
            elif field == 7:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_bool = reader.bool_value()
            elif field == 8:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_fixed32 = reader.fixed32()
            elif field == 9:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_fixed64 = reader.fixed64()
            elif field == 10:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_sfixed32 = reader.sfixed32_value()
            elif field == 11:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_sfixed64 = reader.sfixed64_value()
            elif field == 12:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_float = reader.float_value()
            elif field == 13:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_double = reader.double_value()
            elif field == 14:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_string = reader.string_value()
            elif field == 15:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_bytes = reader.bytes_value()
            elif field == 1000:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.f_big_field = reader.int32_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.f_int32 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fInt32", "f_int32")
            writer.int32_value(self.f_int32)
        if self.f_int64 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fInt64", "f_int64")
            writer.int64_value(self.f_int64)
        if self.f_uint32 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fUint32", "f_uint32")
            writer.uint32_value(self.f_uint32)
        if self.f_uint64 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fUint64", "f_uint64")
            writer.uint64_value(self.f_uint64)
        if self.f_sint32 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fSint32", "f_sint32")
            writer.int32_value(self.f_sint32)
        if self.f_sint64 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fSint64", "f_sint64")
            writer.int64_value(self.f_sint64)
        if self.f_bool or writer.options.always_print_fields_with_no_presence:
            writer.field("fBool", "f_bool")
            writer.bool_value(self.f_bool)
        if self.f_fixed32 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fFixed32", "f_fixed32")
            writer.uint32_value(self.f_fixed32)
        if self.f_fixed64 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fFixed64", "f_fixed64")
            writer.uint64_value(self.f_fixed64)
        if self.f_sfixed32 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fSfixed32", "f_sfixed32")
            writer.int32_value(self.f_sfixed32)
        if self.f_sfixed64 != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fSfixed64", "f_sfixed64")
            writer.int64_value(self.f_sfixed64)
        if UInt32(self.f_float.to_bits()) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fFloat", "f_float")
            writer.float32_value(self.f_float)
        if UInt64(self.f_double.to_bits()) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fDouble", "f_double")
            writer.float64_value(self.f_double)
        if self.f_string.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fString", "f_string")
            writer.string_value(self.f_string)
        if len(self.f_bytes) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fBytes", "f_bytes")
            writer.bytes_value(Span(self.f_bytes))
        if self.f_big_field != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fBigField", "f_big_field")
            writer.int32_value(self.f_big_field)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_6 = False
        var seen_7 = False
        var seen_8 = False
        var seen_9 = False
        var seen_10 = False
        var seen_11 = False
        var seen_12 = False
        var seen_13 = False
        var seen_14 = False
        var seen_15 = False
        var seen_1000 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "fInt32" or field_name == "f_int32":
                if seen_1:
                    raise Error("proto json: duplicate field fInt32")
                seen_1 = True
                if reader.read_null():
                    self.f_int32 = 0
                else:
                    self.f_int32 = reader.int32_value()
            elif field_name == "fInt64" or field_name == "f_int64":
                if seen_2:
                    raise Error("proto json: duplicate field fInt64")
                seen_2 = True
                if reader.read_null():
                    self.f_int64 = 0
                else:
                    self.f_int64 = reader.int64_value()
            elif field_name == "fUint32" or field_name == "f_uint32":
                if seen_3:
                    raise Error("proto json: duplicate field fUint32")
                seen_3 = True
                if reader.read_null():
                    self.f_uint32 = 0
                else:
                    self.f_uint32 = reader.uint32_value()
            elif field_name == "fUint64" or field_name == "f_uint64":
                if seen_4:
                    raise Error("proto json: duplicate field fUint64")
                seen_4 = True
                if reader.read_null():
                    self.f_uint64 = 0
                else:
                    self.f_uint64 = reader.uint64_value()
            elif field_name == "fSint32" or field_name == "f_sint32":
                if seen_5:
                    raise Error("proto json: duplicate field fSint32")
                seen_5 = True
                if reader.read_null():
                    self.f_sint32 = 0
                else:
                    self.f_sint32 = reader.int32_value()
            elif field_name == "fSint64" or field_name == "f_sint64":
                if seen_6:
                    raise Error("proto json: duplicate field fSint64")
                seen_6 = True
                if reader.read_null():
                    self.f_sint64 = 0
                else:
                    self.f_sint64 = reader.int64_value()
            elif field_name == "fBool" or field_name == "f_bool":
                if seen_7:
                    raise Error("proto json: duplicate field fBool")
                seen_7 = True
                if reader.read_null():
                    self.f_bool = False
                else:
                    self.f_bool = reader.bool_value()
            elif field_name == "fFixed32" or field_name == "f_fixed32":
                if seen_8:
                    raise Error("proto json: duplicate field fFixed32")
                seen_8 = True
                if reader.read_null():
                    self.f_fixed32 = 0
                else:
                    self.f_fixed32 = reader.uint32_value()
            elif field_name == "fFixed64" or field_name == "f_fixed64":
                if seen_9:
                    raise Error("proto json: duplicate field fFixed64")
                seen_9 = True
                if reader.read_null():
                    self.f_fixed64 = 0
                else:
                    self.f_fixed64 = reader.uint64_value()
            elif field_name == "fSfixed32" or field_name == "f_sfixed32":
                if seen_10:
                    raise Error("proto json: duplicate field fSfixed32")
                seen_10 = True
                if reader.read_null():
                    self.f_sfixed32 = 0
                else:
                    self.f_sfixed32 = reader.int32_value()
            elif field_name == "fSfixed64" or field_name == "f_sfixed64":
                if seen_11:
                    raise Error("proto json: duplicate field fSfixed64")
                seen_11 = True
                if reader.read_null():
                    self.f_sfixed64 = 0
                else:
                    self.f_sfixed64 = reader.int64_value()
            elif field_name == "fFloat" or field_name == "f_float":
                if seen_12:
                    raise Error("proto json: duplicate field fFloat")
                seen_12 = True
                if reader.read_null():
                    self.f_float = 0.0
                else:
                    self.f_float = reader.float32_value()
            elif field_name == "fDouble" or field_name == "f_double":
                if seen_13:
                    raise Error("proto json: duplicate field fDouble")
                seen_13 = True
                if reader.read_null():
                    self.f_double = 0.0
                else:
                    self.f_double = reader.float64_value()
            elif field_name == "fString" or field_name == "f_string":
                if seen_14:
                    raise Error("proto json: duplicate field fString")
                seen_14 = True
                if reader.read_null():
                    self.f_string = String()
                else:
                    self.f_string = reader.string_value()
            elif field_name == "fBytes" or field_name == "f_bytes":
                if seen_15:
                    raise Error("proto json: duplicate field fBytes")
                seen_15 = True
                if reader.read_null():
                    self.f_bytes = List[Byte]()
                else:
                    self.f_bytes = reader.bytes_value()
            elif field_name == "fBigField" or field_name == "f_big_field":
                if seen_1000:
                    raise Error("proto json: duplicate field fBigField")
                seen_1000 = True
                if reader.read_null():
                    self.f_big_field = 0
                else:
                    self.f_big_field = reader.int32_value()
            else:
                reader.skip_unknown_value()


struct Nested(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `Nested` protobuf message."""

    var inner: Optional[Scalars]
    """Field `inner` (number 1)."""
    var packed_ints: List[Int32]
    """Field `packed_ints` (number 2, repeated)."""
    var names: List[String]
    """Field `names` (number 3, repeated)."""
    var inners: List[Scalars]
    """Field `inners` (number 4, repeated)."""
    var counts: Dict[String, Int32]
    """Field `counts` (number 5, map field)."""
    var as_text: String
    """Field `as_text` (number 6)."""
    var as_num: Int64
    """Field `as_num` (number 7)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    var choice_case: Int
    """Active member of oneof `choice`: 0 when unset, else the set field number."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.inner = None
        self.packed_ints = List[Int32]()
        self.names = List[String]()
        self.inners = List[Scalars]()
        self.counts = Dict[String, Int32]()
        self.as_text = String()
        self.as_num = 0
        self._unknown = List[Byte]()
        self.choice_case = 0

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.inner:
            var sub = WireWriter()
            self.inner.value().encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        if len(self.packed_ints) != 0:
            var sub = WireWriter()
            for v in self.packed_ints:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(2, Span(sub.buf))
        for v in self.names:
            writer.string_field(3, v)
        for v in self.inners:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(4, Span(sub.buf))
        for entry in self.counts.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(5, Span(sub.buf))
        if self.choice_case == 6:
            writer.string_field(6, self.as_text)
        elif self.choice_case == 7:
            writer.int64(7, self.as_num)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Scalars
                    if self.inner:
                        m = self.inner.take()
                    else:
                        m = Scalars()
                    m.merge_from(sub)
                    self.inner = m^
            elif field == 2:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.packed_ints.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.packed_ints.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.names.append(reader.string_value())
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Scalars()
                    m.merge_from(sub)
                    self.inners.append(m^)
            elif field == 5:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.counts[key^] = value
            elif field == 6:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.as_text = reader.string_value()
                    self.choice_case = 6
                    self.as_num = 0
            elif field == 7:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.as_num = reader.int64_value()
                    self.choice_case = 7
                    self.as_text = String()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.inner:
            writer.field("inner", "inner")
            writer.message_value(self.inner.value())
        if len(self.packed_ints) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("packedInts", "packed_ints")
            writer.begin_array()
            for item in self.packed_ints:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.names) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("names", "names")
            writer.begin_array()
            for item in self.names:
                writer.array_item()
                writer.string_value(item)
            writer.end_array()
        if len(self.inners) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("inners", "inners")
            writer.begin_array()
            for item in self.inners:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.counts) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("counts", "counts")
            writer.begin_map()
            for entry in self.counts.items():
                writer.map_key(entry.key)
                writer.int32_value(entry.value)
            writer.end_map()
        if self.choice_case == 6:
            writer.field("asText", "as_text")
            writer.string_value(self.as_text)
        if self.choice_case == 7:
            writer.field("asNum", "as_num")
            writer.int64_value(self.as_num)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_6 = False
        var seen_7 = False
        var seen_oneof_0 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "inner" or field_name == "inner":
                if seen_1:
                    raise Error("proto json: duplicate field inner")
                seen_1 = True
                if reader.read_null():
                    self.inner = None
                else:
                    self.inner = reader.message_value[Scalars]()
            elif field_name == "packedInts" or field_name == "packed_ints":
                if seen_2:
                    raise Error("proto json: duplicate field packedInts")
                seen_2 = True
                if reader.read_null():
                    self.packed_ints = List[Int32]()
                else:
                    self.packed_ints = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.packed_ints.append(reader.int32_value())
            elif field_name == "names" or field_name == "names":
                if seen_3:
                    raise Error("proto json: duplicate field names")
                seen_3 = True
                if reader.read_null():
                    self.names = List[String]()
                else:
                    self.names = List[String]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.names.append(reader.string_value())
            elif field_name == "inners" or field_name == "inners":
                if seen_4:
                    raise Error("proto json: duplicate field inners")
                seen_4 = True
                if reader.read_null():
                    self.inners = List[Scalars]()
                else:
                    self.inners = List[Scalars]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.inners.append(reader.message_value[Scalars]())
            elif field_name == "counts" or field_name == "counts":
                if seen_5:
                    raise Error("proto json: duplicate field counts")
                seen_5 = True
                if reader.read_null():
                    self.counts = Dict[String, Int32]()
                else:
                    self.counts = Dict[String, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.counts[map_key^] = reader.int32_value()
            elif field_name == "asText" or field_name == "as_text":
                if seen_6:
                    raise Error("proto json: duplicate field asText")
                seen_6 = True
                if reader.read_null():
                    if self.choice_case == 6:
                        self.as_text = String()
                        self.choice_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.as_text = reader.string_value()
                    if self.choice_case != 0 and self.choice_case != 6:
                        raise Error("proto json: multiple oneof fields")
                    self.as_num = 0
                    self.choice_case = 6
            elif field_name == "asNum" or field_name == "as_num":
                if seen_7:
                    raise Error("proto json: duplicate field asNum")
                seen_7 = True
                if reader.read_null():
                    if self.choice_case == 7:
                        self.as_num = 0
                        self.choice_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.as_num = reader.int64_value()
                    if self.choice_case != 0 and self.choice_case != 7:
                        raise Error("proto json: multiple oneof fields")
                    self.as_text = String()
                    self.choice_case = 7
            else:
                reader.skip_unknown_value()


struct Tree(Copyable, Defaultable, Movable, ProtoMessage):
    """Generated from the `Tree` protobuf message."""

    var child: List[Tree]
    """Field `child` (number 1, recursive: boxed as a 0-or-1 list)."""
    var v: Int32
    """Field `v` (number 2)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.child = List[Tree]()
        self.v = 0
        self._unknown = List[Byte]()

    def __deinit__(deinit self):
        """Destroys the message (explicit: breaks the trait-synthesis cycle for recursive types)."""
        pass

    def __init__(out self, *, copy: Self):
        """Deep-copies another message.

        Args:
            copy: The message to copy from.
        """
        self.child = copy.child.copy()
        self.v = copy.v
        self._unknown = copy._unknown.copy()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if len(self.child) != 0:
            var sub = WireWriter()
            self.child[0].encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        if self.v != 0:
            writer.int32(2, self.v)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Tree
                    if len(self.child) != 0:
                        m = self.child.pop()
                    else:
                        m = Tree()
                    m.merge_from(sub)
                    self.child.append(m^)
            elif field == 2:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.v = reader.int32_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)


struct EchoRequest(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `EchoRequest` protobuf message."""

    var message: String
    """Field `message` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.message = String()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.message.byte_length() != 0:
            writer.string_field(1, self.message)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.message = reader.string_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.message.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("message", "message")
            writer.string_value(self.message)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "message" or field_name == "message":
                if seen_1:
                    raise Error("proto json: duplicate field message")
                seen_1 = True
                if reader.read_null():
                    self.message = String()
                else:
                    self.message = reader.string_value()
            else:
                reader.skip_unknown_value()


struct EchoResponse(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `EchoResponse` protobuf message."""

    var message: String
    """Field `message` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.message = String()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.message.byte_length() != 0:
            writer.string_field(1, self.message)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.message = reader.string_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.message.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("message", "message")
            writer.string_value(self.message)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "message" or field_name == "message":
                if seen_1:
                    raise Error("proto json: duplicate field message")
                seen_1 = True
                if reader.read_null():
                    self.message = String()
                else:
                    self.message = reader.string_value()
            else:
                reader.skip_unknown_value()


struct EnumValue(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `EnumValue` protobuf message."""

    var status: Int32
    """Field `status` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.status = 0
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.status != 0:
            writer.int32(1, self.status)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.status = reader.int32_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.status != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("status", "status")
            if self.status == 0:
                writer.string_value("STATUS_UNSPECIFIED")
            elif self.status == 1:
                writer.string_value("STATUS_ACTIVE")
            elif self.status == 2:
                writer.string_value("STATUS_PAUSED")
            elif self.status == -1:
                writer.string_value("STATUS_NEGATIVE")
            else:
                writer.int32_value(self.status)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "status" or field_name == "status":
                if seen_1:
                    raise Error("proto json: duplicate field status")
                seen_1 = True
                if reader.read_null():
                    self.status = 0
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "STATUS_UNSPECIFIED":
                            self.status = 0
                        elif enum_name.value() == "STATUS_ACTIVE":
                            self.status = 1
                        elif enum_name.value() == "STATUS_ENABLED":
                            self.status = 1
                        elif enum_name.value() == "STATUS_PAUSED":
                            self.status = 2
                        elif enum_name.value() == "STATUS_NEGATIVE":
                            self.status = -1
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.status = reader.int32_value()
            else:
                reader.skip_unknown_value()


struct JsonChild(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonChild` protobuf message."""

    var id: Int32
    """Field `id` (number 1)."""
    var note: String
    """Field `note` (number 2)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.id = 0
        self.note = String()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.id != 0:
            writer.int32(1, self.id)
        if self.note.byte_length() != 0:
            writer.string_field(2, self.note)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.id = reader.int32_value()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.note = reader.string_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.id != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("id", "id")
            writer.int32_value(self.id)
        if self.note.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("note", "note")
            writer.string_value(self.note)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "id" or field_name == "id":
                if seen_1:
                    raise Error("proto json: duplicate field id")
                seen_1 = True
                if reader.read_null():
                    self.id = 0
                else:
                    self.id = reader.int32_value()
            elif field_name == "note" or field_name == "note":
                if seen_2:
                    raise Error("proto json: duplicate field note")
                seen_2 = True
                if reader.read_null():
                    self.note = String()
                else:
                    self.note = reader.string_value()
            else:
                reader.skip_unknown_value()


struct JsonParent(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonParent` protobuf message."""

    var child: Optional[JsonChild]
    """Field `child` (number 1)."""
    var echo: Optional[EchoRequest]
    """Field `echo` (number 2)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.child = None
        self.echo = None
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.child:
            var sub = WireWriter()
            self.child.value().encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        if self.echo:
            var sub = WireWriter()
            self.echo.value().encode_to(sub)
            writer.len_prefixed(2, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: JsonChild
                    if self.child:
                        m = self.child.take()
                    else:
                        m = JsonChild()
                    m.merge_from(sub)
                    self.child = m^
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: EchoRequest
                    if self.echo:
                        m = self.echo.take()
                    else:
                        m = EchoRequest()
                    m.merge_from(sub)
                    self.echo = m^
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.child:
            writer.field("child", "child")
            writer.message_value(self.child.value())
        if self.echo:
            writer.field("echo", "echo")
            writer.message_value(self.echo.value())
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "child" or field_name == "child":
                if seen_1:
                    raise Error("proto json: duplicate field child")
                seen_1 = True
                if reader.read_null():
                    self.child = None
                else:
                    self.child = reader.message_value[JsonChild]()
            elif field_name == "echo" or field_name == "echo":
                if seen_2:
                    raise Error("proto json: duplicate field echo")
                seen_2 = True
                if reader.read_null():
                    self.echo = None
                else:
                    self.echo = reader.message_value[EchoRequest]()
            else:
                reader.skip_unknown_value()


struct JsonRepeated(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonRepeated` protobuf message."""

    var int32_values: List[Int32]
    """Field `int32_values` (number 1, repeated)."""
    var int64_values: List[Int64]
    """Field `int64_values` (number 2, repeated)."""
    var uint32_values: List[UInt32]
    """Field `uint32_values` (number 3, repeated)."""
    var uint64_values: List[UInt64]
    """Field `uint64_values` (number 4, repeated)."""
    var sint32_values: List[Int32]
    """Field `sint32_values` (number 5, repeated)."""
    var sint64_values: List[Int64]
    """Field `sint64_values` (number 6, repeated)."""
    var bool_values: List[Bool]
    """Field `bool_values` (number 7, repeated)."""
    var fixed32_values: List[UInt32]
    """Field `fixed32_values` (number 8, repeated)."""
    var fixed64_values: List[UInt64]
    """Field `fixed64_values` (number 9, repeated)."""
    var sfixed32_values: List[Int32]
    """Field `sfixed32_values` (number 10, repeated)."""
    var sfixed64_values: List[Int64]
    """Field `sfixed64_values` (number 11, repeated)."""
    var float_values: List[Float32]
    """Field `float_values` (number 12, repeated)."""
    var double_values: List[Float64]
    """Field `double_values` (number 13, repeated)."""
    var string_values: List[String]
    """Field `string_values` (number 14, repeated)."""
    var bytes_values: List[List[Byte]]
    """Field `bytes_values` (number 15, repeated)."""
    var status_values: List[Int32]
    """Field `status_values` (number 16, repeated)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.int32_values = List[Int32]()
        self.int64_values = List[Int64]()
        self.uint32_values = List[UInt32]()
        self.uint64_values = List[UInt64]()
        self.sint32_values = List[Int32]()
        self.sint64_values = List[Int64]()
        self.bool_values = List[Bool]()
        self.fixed32_values = List[UInt32]()
        self.fixed64_values = List[UInt64]()
        self.sfixed32_values = List[Int32]()
        self.sfixed64_values = List[Int64]()
        self.float_values = List[Float32]()
        self.double_values = List[Float64]()
        self.string_values = List[String]()
        self.bytes_values = List[List[Byte]]()
        self.status_values = List[Int32]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if len(self.int32_values) != 0:
            var sub = WireWriter()
            for v in self.int32_values:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(1, Span(sub.buf))
        if len(self.int64_values) != 0:
            var sub = WireWriter()
            for v in self.int64_values:
                sub.varint(UInt64(v))
            writer.len_prefixed(2, Span(sub.buf))
        if len(self.uint32_values) != 0:
            var sub = WireWriter()
            for v in self.uint32_values:
                sub.varint(UInt64(v))
            writer.len_prefixed(3, Span(sub.buf))
        if len(self.uint64_values) != 0:
            var sub = WireWriter()
            for v in self.uint64_values:
                sub.varint(v)
            writer.len_prefixed(4, Span(sub.buf))
        if len(self.sint32_values) != 0:
            var sub = WireWriter()
            for v in self.sint32_values:
                sub.varint(zigzag_encode64(Int64(v)))
            writer.len_prefixed(5, Span(sub.buf))
        if len(self.sint64_values) != 0:
            var sub = WireWriter()
            for v in self.sint64_values:
                sub.varint(zigzag_encode64(v))
            writer.len_prefixed(6, Span(sub.buf))
        if len(self.bool_values) != 0:
            var sub = WireWriter()
            for v in self.bool_values:
                sub.varint(UInt64(1) if v else UInt64(0))
            writer.len_prefixed(7, Span(sub.buf))
        if len(self.fixed32_values) != 0:
            var sub = WireWriter()
            for v in self.fixed32_values:
                sub.fixed32(v)
            writer.len_prefixed(8, Span(sub.buf))
        if len(self.fixed64_values) != 0:
            var sub = WireWriter()
            for v in self.fixed64_values:
                sub.fixed64(v)
            writer.len_prefixed(9, Span(sub.buf))
        if len(self.sfixed32_values) != 0:
            var sub = WireWriter()
            for v in self.sfixed32_values:
                sub.fixed32(UInt32(from_bits=v))
            writer.len_prefixed(10, Span(sub.buf))
        if len(self.sfixed64_values) != 0:
            var sub = WireWriter()
            for v in self.sfixed64_values:
                sub.fixed64(UInt64(from_bits=v))
            writer.len_prefixed(11, Span(sub.buf))
        if len(self.float_values) != 0:
            var sub = WireWriter()
            for v in self.float_values:
                sub.fixed32(UInt32(v.to_bits()))
            writer.len_prefixed(12, Span(sub.buf))
        if len(self.double_values) != 0:
            var sub = WireWriter()
            for v in self.double_values:
                sub.fixed64(UInt64(v.to_bits()))
            writer.len_prefixed(13, Span(sub.buf))
        for v in self.string_values:
            writer.string_field(14, v)
        for v in self.bytes_values:
            writer.bytes_field(15, Span(v))
        if len(self.status_values) != 0:
            var sub = WireWriter()
            for v in self.status_values:
                sub.varint(UInt64(Int64(v)))
            writer.len_prefixed(16, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.int32_values.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.int32_values.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 2:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.int64_values.append(sub.int64_value())
                elif wire_type == WIRE_VARINT:
                    self.int64_values.append(reader.int64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 3:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.uint32_values.append(sub.uint32_value())
                elif wire_type == WIRE_VARINT:
                    self.uint32_values.append(reader.uint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 4:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.uint64_values.append(sub.varint())
                elif wire_type == WIRE_VARINT:
                    self.uint64_values.append(reader.varint())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 5:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.sint32_values.append(sub.sint32_value())
                elif wire_type == WIRE_VARINT:
                    self.sint32_values.append(reader.sint32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 6:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.sint64_values.append(sub.sint64_value())
                elif wire_type == WIRE_VARINT:
                    self.sint64_values.append(reader.sint64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 7:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.bool_values.append(sub.bool_value())
                elif wire_type == WIRE_VARINT:
                    self.bool_values.append(reader.bool_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 8:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.fixed32_values.append(sub.fixed32())
                elif wire_type == WIRE_FIXED32:
                    self.fixed32_values.append(reader.fixed32())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 9:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.fixed64_values.append(sub.fixed64())
                elif wire_type == WIRE_FIXED64:
                    self.fixed64_values.append(reader.fixed64())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 10:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.sfixed32_values.append(sub.sfixed32_value())
                elif wire_type == WIRE_FIXED32:
                    self.sfixed32_values.append(reader.sfixed32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 11:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.sfixed64_values.append(sub.sfixed64_value())
                elif wire_type == WIRE_FIXED64:
                    self.sfixed64_values.append(reader.sfixed64_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 12:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.float_values.append(sub.float_value())
                elif wire_type == WIRE_FIXED32:
                    self.float_values.append(reader.float_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 13:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.double_values.append(sub.double_value())
                elif wire_type == WIRE_FIXED64:
                    self.double_values.append(reader.double_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            elif field == 14:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.string_values.append(reader.string_value())
            elif field == 15:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.bytes_values.append(reader.bytes_value())
            elif field == 16:
                if wire_type == WIRE_LEN:
                    var sub = reader.sub_reader()
                    while not sub.done():
                        self.status_values.append(sub.int32_value())
                elif wire_type == WIRE_VARINT:
                    self.status_values.append(reader.int32_value())
                else:
                    reader.capture_field(field, wire_type, self._unknown)
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if len(self.int32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("int32Values", "int32_values")
            writer.begin_array()
            for item in self.int32_values:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.int64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("int64Values", "int64_values")
            writer.begin_array()
            for item in self.int64_values:
                writer.array_item()
                writer.int64_value(item)
            writer.end_array()
        if len(self.uint32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("uint32Values", "uint32_values")
            writer.begin_array()
            for item in self.uint32_values:
                writer.array_item()
                writer.uint32_value(item)
            writer.end_array()
        if len(self.uint64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("uint64Values", "uint64_values")
            writer.begin_array()
            for item in self.uint64_values:
                writer.array_item()
                writer.uint64_value(item)
            writer.end_array()
        if len(self.sint32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("sint32Values", "sint32_values")
            writer.begin_array()
            for item in self.sint32_values:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.sint64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("sint64Values", "sint64_values")
            writer.begin_array()
            for item in self.sint64_values:
                writer.array_item()
                writer.int64_value(item)
            writer.end_array()
        if len(self.bool_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("boolValues", "bool_values")
            writer.begin_array()
            for item in self.bool_values:
                writer.array_item()
                writer.bool_value(item)
            writer.end_array()
        if len(self.fixed32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fixed32Values", "fixed32_values")
            writer.begin_array()
            for item in self.fixed32_values:
                writer.array_item()
                writer.uint32_value(item)
            writer.end_array()
        if len(self.fixed64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fixed64Values", "fixed64_values")
            writer.begin_array()
            for item in self.fixed64_values:
                writer.array_item()
                writer.uint64_value(item)
            writer.end_array()
        if len(self.sfixed32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("sfixed32Values", "sfixed32_values")
            writer.begin_array()
            for item in self.sfixed32_values:
                writer.array_item()
                writer.int32_value(item)
            writer.end_array()
        if len(self.sfixed64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("sfixed64Values", "sfixed64_values")
            writer.begin_array()
            for item in self.sfixed64_values:
                writer.array_item()
                writer.int64_value(item)
            writer.end_array()
        if len(self.float_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("floatValues", "float_values")
            writer.begin_array()
            for item in self.float_values:
                writer.array_item()
                writer.float32_value(item)
            writer.end_array()
        if len(self.double_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("doubleValues", "double_values")
            writer.begin_array()
            for item in self.double_values:
                writer.array_item()
                writer.float64_value(item)
            writer.end_array()
        if len(self.string_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("stringValues", "string_values")
            writer.begin_array()
            for item in self.string_values:
                writer.array_item()
                writer.string_value(item)
            writer.end_array()
        if len(self.bytes_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("bytesValues", "bytes_values")
            writer.begin_array()
            for item in self.bytes_values:
                writer.array_item()
                writer.bytes_value(Span(item))
            writer.end_array()
        if len(self.status_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("statusValues", "status_values")
            writer.begin_array()
            for item in self.status_values:
                writer.array_item()
                if item == 0:
                    writer.string_value("STATUS_UNSPECIFIED")
                elif item == 1:
                    writer.string_value("STATUS_ACTIVE")
                elif item == 2:
                    writer.string_value("STATUS_PAUSED")
                elif item == -1:
                    writer.string_value("STATUS_NEGATIVE")
                else:
                    writer.int32_value(item)
            writer.end_array()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_6 = False
        var seen_7 = False
        var seen_8 = False
        var seen_9 = False
        var seen_10 = False
        var seen_11 = False
        var seen_12 = False
        var seen_13 = False
        var seen_14 = False
        var seen_15 = False
        var seen_16 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "int32Values" or field_name == "int32_values":
                if seen_1:
                    raise Error("proto json: duplicate field int32Values")
                seen_1 = True
                if reader.read_null():
                    self.int32_values = List[Int32]()
                else:
                    self.int32_values = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.int32_values.append(reader.int32_value())
            elif field_name == "int64Values" or field_name == "int64_values":
                if seen_2:
                    raise Error("proto json: duplicate field int64Values")
                seen_2 = True
                if reader.read_null():
                    self.int64_values = List[Int64]()
                else:
                    self.int64_values = List[Int64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.int64_values.append(reader.int64_value())
            elif field_name == "uint32Values" or field_name == "uint32_values":
                if seen_3:
                    raise Error("proto json: duplicate field uint32Values")
                seen_3 = True
                if reader.read_null():
                    self.uint32_values = List[UInt32]()
                else:
                    self.uint32_values = List[UInt32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.uint32_values.append(reader.uint32_value())
            elif field_name == "uint64Values" or field_name == "uint64_values":
                if seen_4:
                    raise Error("proto json: duplicate field uint64Values")
                seen_4 = True
                if reader.read_null():
                    self.uint64_values = List[UInt64]()
                else:
                    self.uint64_values = List[UInt64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.uint64_values.append(reader.uint64_value())
            elif field_name == "sint32Values" or field_name == "sint32_values":
                if seen_5:
                    raise Error("proto json: duplicate field sint32Values")
                seen_5 = True
                if reader.read_null():
                    self.sint32_values = List[Int32]()
                else:
                    self.sint32_values = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.sint32_values.append(reader.int32_value())
            elif field_name == "sint64Values" or field_name == "sint64_values":
                if seen_6:
                    raise Error("proto json: duplicate field sint64Values")
                seen_6 = True
                if reader.read_null():
                    self.sint64_values = List[Int64]()
                else:
                    self.sint64_values = List[Int64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.sint64_values.append(reader.int64_value())
            elif field_name == "boolValues" or field_name == "bool_values":
                if seen_7:
                    raise Error("proto json: duplicate field boolValues")
                seen_7 = True
                if reader.read_null():
                    self.bool_values = List[Bool]()
                else:
                    self.bool_values = List[Bool]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.bool_values.append(reader.bool_value())
            elif field_name == "fixed32Values" or field_name == "fixed32_values":
                if seen_8:
                    raise Error("proto json: duplicate field fixed32Values")
                seen_8 = True
                if reader.read_null():
                    self.fixed32_values = List[UInt32]()
                else:
                    self.fixed32_values = List[UInt32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.fixed32_values.append(reader.uint32_value())
            elif field_name == "fixed64Values" or field_name == "fixed64_values":
                if seen_9:
                    raise Error("proto json: duplicate field fixed64Values")
                seen_9 = True
                if reader.read_null():
                    self.fixed64_values = List[UInt64]()
                else:
                    self.fixed64_values = List[UInt64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.fixed64_values.append(reader.uint64_value())
            elif field_name == "sfixed32Values" or field_name == "sfixed32_values":
                if seen_10:
                    raise Error("proto json: duplicate field sfixed32Values")
                seen_10 = True
                if reader.read_null():
                    self.sfixed32_values = List[Int32]()
                else:
                    self.sfixed32_values = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.sfixed32_values.append(reader.int32_value())
            elif field_name == "sfixed64Values" or field_name == "sfixed64_values":
                if seen_11:
                    raise Error("proto json: duplicate field sfixed64Values")
                seen_11 = True
                if reader.read_null():
                    self.sfixed64_values = List[Int64]()
                else:
                    self.sfixed64_values = List[Int64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.sfixed64_values.append(reader.int64_value())
            elif field_name == "floatValues" or field_name == "float_values":
                if seen_12:
                    raise Error("proto json: duplicate field floatValues")
                seen_12 = True
                if reader.read_null():
                    self.float_values = List[Float32]()
                else:
                    self.float_values = List[Float32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.float_values.append(reader.float32_value())
            elif field_name == "doubleValues" or field_name == "double_values":
                if seen_13:
                    raise Error("proto json: duplicate field doubleValues")
                seen_13 = True
                if reader.read_null():
                    self.double_values = List[Float64]()
                else:
                    self.double_values = List[Float64]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.double_values.append(reader.float64_value())
            elif field_name == "stringValues" or field_name == "string_values":
                if seen_14:
                    raise Error("proto json: duplicate field stringValues")
                seen_14 = True
                if reader.read_null():
                    self.string_values = List[String]()
                else:
                    self.string_values = List[String]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.string_values.append(reader.string_value())
            elif field_name == "bytesValues" or field_name == "bytes_values":
                if seen_15:
                    raise Error("proto json: duplicate field bytesValues")
                seen_15 = True
                if reader.read_null():
                    self.bytes_values = List[List[Byte]]()
                else:
                    self.bytes_values = List[List[Byte]]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.bytes_values.append(reader.bytes_value())
            elif field_name == "statusValues" or field_name == "status_values":
                if seen_16:
                    raise Error("proto json: duplicate field statusValues")
                seen_16 = True
                if reader.read_null():
                    self.status_values = List[Int32]()
                else:
                    self.status_values = List[Int32]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        var enum_name = reader.enum_name()
                        if enum_name:
                            if enum_name.value() == "STATUS_UNSPECIFIED":
                                self.status_values.append(0)
                            elif enum_name.value() == "STATUS_ACTIVE":
                                self.status_values.append(1)
                            elif enum_name.value() == "STATUS_ENABLED":
                                self.status_values.append(1)
                            elif enum_name.value() == "STATUS_PAUSED":
                                self.status_values.append(2)
                            elif enum_name.value() == "STATUS_NEGATIVE":
                                self.status_values.append(-1)
                            elif not reader.options.ignore_unknown_fields:
                                raise Error("proto json: unknown enum value")
                        else:
                            self.status_values.append(reader.int32_value())
            else:
                reader.skip_unknown_value()


struct JsonRepeatedMessages(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonRepeatedMessages` protobuf message."""

    var children: List[JsonChild]
    """Field `children` (number 1, repeated)."""
    var echoes: List[EchoRequest]
    """Field `echoes` (number 2, repeated)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.children = List[JsonChild]()
        self.echoes = List[EchoRequest]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        for v in self.children:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        for v in self.echoes:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(2, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = JsonChild()
                    m.merge_from(sub)
                    self.children.append(m^)
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = EchoRequest()
                    m.merge_from(sub)
                    self.echoes.append(m^)
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if len(self.children) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("children", "children")
            writer.begin_array()
            for item in self.children:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        if len(self.echoes) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("echoes", "echoes")
            writer.begin_array()
            for item in self.echoes:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "children" or field_name == "children":
                if seen_1:
                    raise Error("proto json: duplicate field children")
                seen_1 = True
                if reader.read_null():
                    self.children = List[JsonChild]()
                else:
                    self.children = List[JsonChild]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.children.append(reader.message_value[JsonChild]())
            elif field_name == "echoes" or field_name == "echoes":
                if seen_2:
                    raise Error("proto json: duplicate field echoes")
                seen_2 = True
                if reader.read_null():
                    self.echoes = List[EchoRequest]()
                else:
                    self.echoes = List[EchoRequest]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.echoes.append(reader.message_value[EchoRequest]())
            else:
                reader.skip_unknown_value()


struct JsonStringMaps(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonStringMaps` protobuf message."""

    var int32_values: Dict[String, Int32]
    """Field `int32_values` (number 1, map field)."""
    var int64_values: Dict[String, Int64]
    """Field `int64_values` (number 2, map field)."""
    var uint32_values: Dict[String, UInt32]
    """Field `uint32_values` (number 3, map field)."""
    var uint64_values: Dict[String, UInt64]
    """Field `uint64_values` (number 4, map field)."""
    var sint32_values: Dict[String, Int32]
    """Field `sint32_values` (number 5, map field)."""
    var sint64_values: Dict[String, Int64]
    """Field `sint64_values` (number 6, map field)."""
    var bool_values: Dict[String, Bool]
    """Field `bool_values` (number 7, map field)."""
    var fixed32_values: Dict[String, UInt32]
    """Field `fixed32_values` (number 8, map field)."""
    var fixed64_values: Dict[String, UInt64]
    """Field `fixed64_values` (number 9, map field)."""
    var sfixed32_values: Dict[String, Int32]
    """Field `sfixed32_values` (number 10, map field)."""
    var sfixed64_values: Dict[String, Int64]
    """Field `sfixed64_values` (number 11, map field)."""
    var float_values: Dict[String, Float32]
    """Field `float_values` (number 12, map field)."""
    var double_values: Dict[String, Float64]
    """Field `double_values` (number 13, map field)."""
    var string_values: Dict[String, String]
    """Field `string_values` (number 14, map field)."""
    var bytes_values: Dict[String, List[Byte]]
    """Field `bytes_values` (number 15, map field)."""
    var status_values: Dict[String, Int32]
    """Field `status_values` (number 16, map field)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.int32_values = Dict[String, Int32]()
        self.int64_values = Dict[String, Int64]()
        self.uint32_values = Dict[String, UInt32]()
        self.uint64_values = Dict[String, UInt64]()
        self.sint32_values = Dict[String, Int32]()
        self.sint64_values = Dict[String, Int64]()
        self.bool_values = Dict[String, Bool]()
        self.fixed32_values = Dict[String, UInt32]()
        self.fixed64_values = Dict[String, UInt64]()
        self.sfixed32_values = Dict[String, Int32]()
        self.sfixed64_values = Dict[String, Int64]()
        self.float_values = Dict[String, Float32]()
        self.double_values = Dict[String, Float64]()
        self.string_values = Dict[String, String]()
        self.bytes_values = Dict[String, List[Byte]]()
        self.status_values = Dict[String, Int32]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        for entry in self.int32_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(1, Span(sub.buf))
        for entry in self.int64_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int64(2, entry.value)
            writer.len_prefixed(2, Span(sub.buf))
        for entry in self.uint32_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.uint32(2, entry.value)
            writer.len_prefixed(3, Span(sub.buf))
        for entry in self.uint64_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.uint64(2, entry.value)
            writer.len_prefixed(4, Span(sub.buf))
        for entry in self.sint32_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.sint32(2, entry.value)
            writer.len_prefixed(5, Span(sub.buf))
        for entry in self.sint64_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.sint64(2, entry.value)
            writer.len_prefixed(6, Span(sub.buf))
        for entry in self.bool_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value:
                sub.bool_field(2, entry.value)
            writer.len_prefixed(7, Span(sub.buf))
        for entry in self.fixed32_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.fixed32_field(2, entry.value)
            writer.len_prefixed(8, Span(sub.buf))
        for entry in self.fixed64_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.fixed64_field(2, entry.value)
            writer.len_prefixed(9, Span(sub.buf))
        for entry in self.sfixed32_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.sfixed32_field(2, entry.value)
            writer.len_prefixed(10, Span(sub.buf))
        for entry in self.sfixed64_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.sfixed64_field(2, entry.value)
            writer.len_prefixed(11, Span(sub.buf))
        for entry in self.float_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if UInt32(entry.value.to_bits()) != 0:
                sub.float_field(2, entry.value)
            writer.len_prefixed(12, Span(sub.buf))
        for entry in self.double_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if UInt64(entry.value.to_bits()) != 0:
                sub.double_field(2, entry.value)
            writer.len_prefixed(13, Span(sub.buf))
        for entry in self.string_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(14, Span(sub.buf))
        for entry in self.bytes_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if len(entry.value) != 0:
                sub.bytes_field(2, Span(entry.value))
            writer.len_prefixed(15, Span(sub.buf))
        for entry in self.status_values.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(16, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.int32_values[key^] = value
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int64 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int64_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.int64_values[key^] = value
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: UInt32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.uint32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.uint32_values[key^] = value
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: UInt64 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.varint()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.uint64_values[key^] = value
            elif field == 5:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.sint32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.sint32_values[key^] = value
            elif field == 6:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int64 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.sint64_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.sint64_values[key^] = value
            elif field == 7:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Bool = False
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.bool_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.bool_values[key^] = value
            elif field == 8:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: UInt32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED32:
                            value = sub.fixed32()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.fixed32_values[key^] = value
            elif field == 9:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: UInt64 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED64:
                            value = sub.fixed64()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.fixed64_values[key^] = value
            elif field == 10:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED32:
                            value = sub.sfixed32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.sfixed32_values[key^] = value
            elif field == 11:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int64 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED64:
                            value = sub.sfixed64_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.sfixed64_values[key^] = value
            elif field == 12:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Float32 = 0.0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED32:
                            value = sub.float_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.float_values[key^] = value
            elif field == 13:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Float64 = 0.0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_FIXED64:
                            value = sub.double_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.double_values[key^] = value
            elif field == 14:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.string_values[key^] = value^
            elif field == 15:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: List[Byte] = List[Byte]()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.bytes_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.bytes_values[key^] = value^
            elif field == 16:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.status_values[key^] = value
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if len(self.int32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("int32Values", "int32_values")
            writer.begin_map()
            for entry in self.int32_values.items():
                writer.map_key(entry.key)
                writer.int32_value(entry.value)
            writer.end_map()
        if len(self.int64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("int64Values", "int64_values")
            writer.begin_map()
            for entry in self.int64_values.items():
                writer.map_key(entry.key)
                writer.int64_value(entry.value)
            writer.end_map()
        if len(self.uint32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("uint32Values", "uint32_values")
            writer.begin_map()
            for entry in self.uint32_values.items():
                writer.map_key(entry.key)
                writer.uint32_value(entry.value)
            writer.end_map()
        if len(self.uint64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("uint64Values", "uint64_values")
            writer.begin_map()
            for entry in self.uint64_values.items():
                writer.map_key(entry.key)
                writer.uint64_value(entry.value)
            writer.end_map()
        if len(self.sint32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("sint32Values", "sint32_values")
            writer.begin_map()
            for entry in self.sint32_values.items():
                writer.map_key(entry.key)
                writer.int32_value(entry.value)
            writer.end_map()
        if len(self.sint64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("sint64Values", "sint64_values")
            writer.begin_map()
            for entry in self.sint64_values.items():
                writer.map_key(entry.key)
                writer.int64_value(entry.value)
            writer.end_map()
        if len(self.bool_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("boolValues", "bool_values")
            writer.begin_map()
            for entry in self.bool_values.items():
                writer.map_key(entry.key)
                writer.bool_value(entry.value)
            writer.end_map()
        if len(self.fixed32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fixed32Values", "fixed32_values")
            writer.begin_map()
            for entry in self.fixed32_values.items():
                writer.map_key(entry.key)
                writer.uint32_value(entry.value)
            writer.end_map()
        if len(self.fixed64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fixed64Values", "fixed64_values")
            writer.begin_map()
            for entry in self.fixed64_values.items():
                writer.map_key(entry.key)
                writer.uint64_value(entry.value)
            writer.end_map()
        if len(self.sfixed32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("sfixed32Values", "sfixed32_values")
            writer.begin_map()
            for entry in self.sfixed32_values.items():
                writer.map_key(entry.key)
                writer.int32_value(entry.value)
            writer.end_map()
        if len(self.sfixed64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("sfixed64Values", "sfixed64_values")
            writer.begin_map()
            for entry in self.sfixed64_values.items():
                writer.map_key(entry.key)
                writer.int64_value(entry.value)
            writer.end_map()
        if len(self.float_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("floatValues", "float_values")
            writer.begin_map()
            for entry in self.float_values.items():
                writer.map_key(entry.key)
                writer.float32_value(entry.value)
            writer.end_map()
        if len(self.double_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("doubleValues", "double_values")
            writer.begin_map()
            for entry in self.double_values.items():
                writer.map_key(entry.key)
                writer.float64_value(entry.value)
            writer.end_map()
        if len(self.string_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("stringValues", "string_values")
            writer.begin_map()
            for entry in self.string_values.items():
                writer.map_key(entry.key)
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.bytes_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("bytesValues", "bytes_values")
            writer.begin_map()
            for entry in self.bytes_values.items():
                writer.map_key(entry.key)
                writer.bytes_value(Span(entry.value))
            writer.end_map()
        if len(self.status_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("statusValues", "status_values")
            writer.begin_map()
            for entry in self.status_values.items():
                writer.map_key(entry.key)
                if entry.value == 0:
                    writer.string_value("STATUS_UNSPECIFIED")
                elif entry.value == 1:
                    writer.string_value("STATUS_ACTIVE")
                elif entry.value == 2:
                    writer.string_value("STATUS_PAUSED")
                elif entry.value == -1:
                    writer.string_value("STATUS_NEGATIVE")
                else:
                    writer.int32_value(entry.value)
            writer.end_map()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_6 = False
        var seen_7 = False
        var seen_8 = False
        var seen_9 = False
        var seen_10 = False
        var seen_11 = False
        var seen_12 = False
        var seen_13 = False
        var seen_14 = False
        var seen_15 = False
        var seen_16 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "int32Values" or field_name == "int32_values":
                if seen_1:
                    raise Error("proto json: duplicate field int32Values")
                seen_1 = True
                if reader.read_null():
                    self.int32_values = Dict[String, Int32]()
                else:
                    self.int32_values = Dict[String, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.int32_values[map_key^] = reader.int32_value()
            elif field_name == "int64Values" or field_name == "int64_values":
                if seen_2:
                    raise Error("proto json: duplicate field int64Values")
                seen_2 = True
                if reader.read_null():
                    self.int64_values = Dict[String, Int64]()
                else:
                    self.int64_values = Dict[String, Int64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.int64_values[map_key^] = reader.int64_value()
            elif field_name == "uint32Values" or field_name == "uint32_values":
                if seen_3:
                    raise Error("proto json: duplicate field uint32Values")
                seen_3 = True
                if reader.read_null():
                    self.uint32_values = Dict[String, UInt32]()
                else:
                    self.uint32_values = Dict[String, UInt32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.uint32_values[map_key^] = reader.uint32_value()
            elif field_name == "uint64Values" or field_name == "uint64_values":
                if seen_4:
                    raise Error("proto json: duplicate field uint64Values")
                seen_4 = True
                if reader.read_null():
                    self.uint64_values = Dict[String, UInt64]()
                else:
                    self.uint64_values = Dict[String, UInt64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.uint64_values[map_key^] = reader.uint64_value()
            elif field_name == "sint32Values" or field_name == "sint32_values":
                if seen_5:
                    raise Error("proto json: duplicate field sint32Values")
                seen_5 = True
                if reader.read_null():
                    self.sint32_values = Dict[String, Int32]()
                else:
                    self.sint32_values = Dict[String, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.sint32_values[map_key^] = reader.int32_value()
            elif field_name == "sint64Values" or field_name == "sint64_values":
                if seen_6:
                    raise Error("proto json: duplicate field sint64Values")
                seen_6 = True
                if reader.read_null():
                    self.sint64_values = Dict[String, Int64]()
                else:
                    self.sint64_values = Dict[String, Int64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.sint64_values[map_key^] = reader.int64_value()
            elif field_name == "boolValues" or field_name == "bool_values":
                if seen_7:
                    raise Error("proto json: duplicate field boolValues")
                seen_7 = True
                if reader.read_null():
                    self.bool_values = Dict[String, Bool]()
                else:
                    self.bool_values = Dict[String, Bool]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.bool_values[map_key^] = reader.bool_value()
            elif field_name == "fixed32Values" or field_name == "fixed32_values":
                if seen_8:
                    raise Error("proto json: duplicate field fixed32Values")
                seen_8 = True
                if reader.read_null():
                    self.fixed32_values = Dict[String, UInt32]()
                else:
                    self.fixed32_values = Dict[String, UInt32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.fixed32_values[map_key^] = reader.uint32_value()
            elif field_name == "fixed64Values" or field_name == "fixed64_values":
                if seen_9:
                    raise Error("proto json: duplicate field fixed64Values")
                seen_9 = True
                if reader.read_null():
                    self.fixed64_values = Dict[String, UInt64]()
                else:
                    self.fixed64_values = Dict[String, UInt64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.fixed64_values[map_key^] = reader.uint64_value()
            elif field_name == "sfixed32Values" or field_name == "sfixed32_values":
                if seen_10:
                    raise Error("proto json: duplicate field sfixed32Values")
                seen_10 = True
                if reader.read_null():
                    self.sfixed32_values = Dict[String, Int32]()
                else:
                    self.sfixed32_values = Dict[String, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.sfixed32_values[map_key^] = reader.int32_value()
            elif field_name == "sfixed64Values" or field_name == "sfixed64_values":
                if seen_11:
                    raise Error("proto json: duplicate field sfixed64Values")
                seen_11 = True
                if reader.read_null():
                    self.sfixed64_values = Dict[String, Int64]()
                else:
                    self.sfixed64_values = Dict[String, Int64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.sfixed64_values[map_key^] = reader.int64_value()
            elif field_name == "floatValues" or field_name == "float_values":
                if seen_12:
                    raise Error("proto json: duplicate field floatValues")
                seen_12 = True
                if reader.read_null():
                    self.float_values = Dict[String, Float32]()
                else:
                    self.float_values = Dict[String, Float32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.float_values[map_key^] = reader.float32_value()
            elif field_name == "doubleValues" or field_name == "double_values":
                if seen_13:
                    raise Error("proto json: duplicate field doubleValues")
                seen_13 = True
                if reader.read_null():
                    self.double_values = Dict[String, Float64]()
                else:
                    self.double_values = Dict[String, Float64]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.double_values[map_key^] = reader.float64_value()
            elif field_name == "stringValues" or field_name == "string_values":
                if seen_14:
                    raise Error("proto json: duplicate field stringValues")
                seen_14 = True
                if reader.read_null():
                    self.string_values = Dict[String, String]()
                else:
                    self.string_values = Dict[String, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.string_values[map_key^] = reader.string_value()
            elif field_name == "bytesValues" or field_name == "bytes_values":
                if seen_15:
                    raise Error("proto json: duplicate field bytesValues")
                seen_15 = True
                if reader.read_null():
                    self.bytes_values = Dict[String, List[Byte]]()
                else:
                    self.bytes_values = Dict[String, List[Byte]]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.bytes_values[map_key^] = reader.bytes_value()
            elif field_name == "statusValues" or field_name == "status_values":
                if seen_16:
                    raise Error("proto json: duplicate field statusValues")
                seen_16 = True
                if reader.read_null():
                    self.status_values = Dict[String, Int32]()
                else:
                    self.status_values = Dict[String, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var enum_name = reader.enum_name()
                        var map_enum_value = Int32(0)
                        var map_enum_known = True
                        if enum_name:
                            if enum_name.value() == "STATUS_UNSPECIFIED":
                                map_enum_value = 0
                            elif enum_name.value() == "STATUS_ACTIVE":
                                map_enum_value = 1
                            elif enum_name.value() == "STATUS_ENABLED":
                                map_enum_value = 1
                            elif enum_name.value() == "STATUS_PAUSED":
                                map_enum_value = 2
                            elif enum_name.value() == "STATUS_NEGATIVE":
                                map_enum_value = -1
                            elif not reader.options.ignore_unknown_fields:
                                raise Error("proto json: unknown enum value")
                            else:
                                map_enum_known = False
                        else:
                            map_enum_value = reader.int32_value()
                        if map_enum_known:
                            self.status_values[map_key^] = map_enum_value
            else:
                reader.skip_unknown_value()


struct JsonKeyMaps(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonKeyMaps` protobuf message."""

    var int32_values: Dict[Int32, String]
    """Field `int32_values` (number 1, map field)."""
    var int64_values: Dict[Int64, String]
    """Field `int64_values` (number 2, map field)."""
    var uint32_values: Dict[UInt32, String]
    """Field `uint32_values` (number 3, map field)."""
    var uint64_values: Dict[UInt64, String]
    """Field `uint64_values` (number 4, map field)."""
    var sint32_values: Dict[Int32, String]
    """Field `sint32_values` (number 5, map field)."""
    var sint64_values: Dict[Int64, String]
    """Field `sint64_values` (number 6, map field)."""
    var bool_values: Dict[Bool, String]
    """Field `bool_values` (number 7, map field)."""
    var fixed32_values: Dict[UInt32, String]
    """Field `fixed32_values` (number 8, map field)."""
    var fixed64_values: Dict[UInt64, String]
    """Field `fixed64_values` (number 9, map field)."""
    var sfixed32_values: Dict[Int32, String]
    """Field `sfixed32_values` (number 10, map field)."""
    var sfixed64_values: Dict[Int64, String]
    """Field `sfixed64_values` (number 11, map field)."""
    var int32_status_values: Dict[Int32, Int32]
    """Field `int32_status_values` (number 12, map field)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.int32_values = Dict[Int32, String]()
        self.int64_values = Dict[Int64, String]()
        self.uint32_values = Dict[UInt32, String]()
        self.uint64_values = Dict[UInt64, String]()
        self.sint32_values = Dict[Int32, String]()
        self.sint64_values = Dict[Int64, String]()
        self.bool_values = Dict[Bool, String]()
        self.fixed32_values = Dict[UInt32, String]()
        self.fixed64_values = Dict[UInt64, String]()
        self.sfixed32_values = Dict[Int32, String]()
        self.sfixed64_values = Dict[Int64, String]()
        self.int32_status_values = Dict[Int32, Int32]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        for entry in self.int32_values.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int32(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(1, Span(sub.buf))
        for entry in self.int64_values.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int64(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(2, Span(sub.buf))
        for entry in self.uint32_values.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.uint32(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(3, Span(sub.buf))
        for entry in self.uint64_values.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.uint64(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(4, Span(sub.buf))
        for entry in self.sint32_values.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.sint32(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(5, Span(sub.buf))
        for entry in self.sint64_values.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.sint64(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(6, Span(sub.buf))
        for entry in self.bool_values.items():
            var sub = WireWriter()
            if entry.key:
                sub.bool_field(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(7, Span(sub.buf))
        for entry in self.fixed32_values.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.fixed32_field(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(8, Span(sub.buf))
        for entry in self.fixed64_values.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.fixed64_field(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(9, Span(sub.buf))
        for entry in self.sfixed32_values.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.sfixed32_field(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(10, Span(sub.buf))
        for entry in self.sfixed64_values.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.sfixed64_field(1, entry.key)
            if entry.value.byte_length() != 0:
                sub.string_field(2, entry.value)
            writer.len_prefixed(11, Span(sub.buf))
        for entry in self.int32_status_values.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int32(1, entry.key)
            if entry.value != 0:
                sub.int32(2, entry.value)
            writer.len_prefixed(12, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.int32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.int32_values[key] = value^
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int64 = 0
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.int64_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.int64_values[key] = value^
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: UInt32 = 0
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.uint32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.uint32_values[key] = value^
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: UInt64 = 0
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.varint()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.uint64_values[key] = value^
            elif field == 5:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.sint32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.sint32_values[key] = value^
            elif field == 6:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int64 = 0
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.sint64_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.sint64_values[key] = value^
            elif field == 7:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Bool = False
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.bool_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.bool_values[key] = value^
            elif field == 8:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: UInt32 = 0
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_FIXED32:
                            key = sub.fixed32()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.fixed32_values[key] = value^
            elif field == 9:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: UInt64 = 0
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_FIXED64:
                            key = sub.fixed64()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.fixed64_values[key] = value^
            elif field == 10:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_FIXED32:
                            key = sub.sfixed32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.sfixed32_values[key] = value^
            elif field == 11:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int64 = 0
                    var value: String = String()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_FIXED64:
                            key = sub.sfixed64_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            value = sub.string_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.sfixed64_values[key] = value^
            elif field == 12:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: Int32 = 0
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.int32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_VARINT:
                            value = sub.int32_value()
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.int32_status_values[key] = value
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if len(self.int32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("int32Values", "int32_values")
            writer.begin_map()
            for entry in self.int32_values.items():
                writer.map_key(String(entry.key))
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.int64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("int64Values", "int64_values")
            writer.begin_map()
            for entry in self.int64_values.items():
                writer.map_key(String(entry.key))
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.uint32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("uint32Values", "uint32_values")
            writer.begin_map()
            for entry in self.uint32_values.items():
                writer.map_key(String(entry.key))
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.uint64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("uint64Values", "uint64_values")
            writer.begin_map()
            for entry in self.uint64_values.items():
                writer.map_key(String(entry.key))
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.sint32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("sint32Values", "sint32_values")
            writer.begin_map()
            for entry in self.sint32_values.items():
                writer.map_key(String(entry.key))
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.sint64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("sint64Values", "sint64_values")
            writer.begin_map()
            for entry in self.sint64_values.items():
                writer.map_key(String(entry.key))
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.bool_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("boolValues", "bool_values")
            writer.begin_map()
            for entry in self.bool_values.items():
                writer.map_key("true" if entry.key else "false")
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.fixed32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fixed32Values", "fixed32_values")
            writer.begin_map()
            for entry in self.fixed32_values.items():
                writer.map_key(String(entry.key))
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.fixed64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("fixed64Values", "fixed64_values")
            writer.begin_map()
            for entry in self.fixed64_values.items():
                writer.map_key(String(entry.key))
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.sfixed32_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("sfixed32Values", "sfixed32_values")
            writer.begin_map()
            for entry in self.sfixed32_values.items():
                writer.map_key(String(entry.key))
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.sfixed64_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("sfixed64Values", "sfixed64_values")
            writer.begin_map()
            for entry in self.sfixed64_values.items():
                writer.map_key(String(entry.key))
                writer.string_value(entry.value)
            writer.end_map()
        if len(self.int32_status_values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("int32StatusValues", "int32_status_values")
            writer.begin_map()
            for entry in self.int32_status_values.items():
                writer.map_key(String(entry.key))
                if entry.value == 0:
                    writer.string_value("STATUS_UNSPECIFIED")
                elif entry.value == 1:
                    writer.string_value("STATUS_ACTIVE")
                elif entry.value == 2:
                    writer.string_value("STATUS_PAUSED")
                elif entry.value == -1:
                    writer.string_value("STATUS_NEGATIVE")
                else:
                    writer.int32_value(entry.value)
            writer.end_map()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_6 = False
        var seen_7 = False
        var seen_8 = False
        var seen_9 = False
        var seen_10 = False
        var seen_11 = False
        var seen_12 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "int32Values" or field_name == "int32_values":
                if seen_1:
                    raise Error("proto json: duplicate field int32Values")
                seen_1 = True
                if reader.read_null():
                    self.int32_values = Dict[Int32, String]()
                else:
                    self.int32_values = Dict[Int32, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int32_value()
                        map_key_reader.finish()
                        self.int32_values[parsed_map_key] = reader.string_value()
            elif field_name == "int64Values" or field_name == "int64_values":
                if seen_2:
                    raise Error("proto json: duplicate field int64Values")
                seen_2 = True
                if reader.read_null():
                    self.int64_values = Dict[Int64, String]()
                else:
                    self.int64_values = Dict[Int64, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int64_value()
                        map_key_reader.finish()
                        self.int64_values[parsed_map_key] = reader.string_value()
            elif field_name == "uint32Values" or field_name == "uint32_values":
                if seen_3:
                    raise Error("proto json: duplicate field uint32Values")
                seen_3 = True
                if reader.read_null():
                    self.uint32_values = Dict[UInt32, String]()
                else:
                    self.uint32_values = Dict[UInt32, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.uint32_value()
                        map_key_reader.finish()
                        self.uint32_values[parsed_map_key] = reader.string_value()
            elif field_name == "uint64Values" or field_name == "uint64_values":
                if seen_4:
                    raise Error("proto json: duplicate field uint64Values")
                seen_4 = True
                if reader.read_null():
                    self.uint64_values = Dict[UInt64, String]()
                else:
                    self.uint64_values = Dict[UInt64, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.uint64_value()
                        map_key_reader.finish()
                        self.uint64_values[parsed_map_key] = reader.string_value()
            elif field_name == "sint32Values" or field_name == "sint32_values":
                if seen_5:
                    raise Error("proto json: duplicate field sint32Values")
                seen_5 = True
                if reader.read_null():
                    self.sint32_values = Dict[Int32, String]()
                else:
                    self.sint32_values = Dict[Int32, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int32_value()
                        map_key_reader.finish()
                        self.sint32_values[parsed_map_key] = reader.string_value()
            elif field_name == "sint64Values" or field_name == "sint64_values":
                if seen_6:
                    raise Error("proto json: duplicate field sint64Values")
                seen_6 = True
                if reader.read_null():
                    self.sint64_values = Dict[Int64, String]()
                else:
                    self.sint64_values = Dict[Int64, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int64_value()
                        map_key_reader.finish()
                        self.sint64_values[parsed_map_key] = reader.string_value()
            elif field_name == "boolValues" or field_name == "bool_values":
                if seen_7:
                    raise Error("proto json: duplicate field boolValues")
                seen_7 = True
                if reader.read_null():
                    self.bool_values = Dict[Bool, String]()
                else:
                    self.bool_values = Dict[Bool, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.bool_value()
                        map_key_reader.finish()
                        self.bool_values[parsed_map_key] = reader.string_value()
            elif field_name == "fixed32Values" or field_name == "fixed32_values":
                if seen_8:
                    raise Error("proto json: duplicate field fixed32Values")
                seen_8 = True
                if reader.read_null():
                    self.fixed32_values = Dict[UInt32, String]()
                else:
                    self.fixed32_values = Dict[UInt32, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.uint32_value()
                        map_key_reader.finish()
                        self.fixed32_values[parsed_map_key] = reader.string_value()
            elif field_name == "fixed64Values" or field_name == "fixed64_values":
                if seen_9:
                    raise Error("proto json: duplicate field fixed64Values")
                seen_9 = True
                if reader.read_null():
                    self.fixed64_values = Dict[UInt64, String]()
                else:
                    self.fixed64_values = Dict[UInt64, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.uint64_value()
                        map_key_reader.finish()
                        self.fixed64_values[parsed_map_key] = reader.string_value()
            elif field_name == "sfixed32Values" or field_name == "sfixed32_values":
                if seen_10:
                    raise Error("proto json: duplicate field sfixed32Values")
                seen_10 = True
                if reader.read_null():
                    self.sfixed32_values = Dict[Int32, String]()
                else:
                    self.sfixed32_values = Dict[Int32, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int32_value()
                        map_key_reader.finish()
                        self.sfixed32_values[parsed_map_key] = reader.string_value()
            elif field_name == "sfixed64Values" or field_name == "sfixed64_values":
                if seen_11:
                    raise Error("proto json: duplicate field sfixed64Values")
                seen_11 = True
                if reader.read_null():
                    self.sfixed64_values = Dict[Int64, String]()
                else:
                    self.sfixed64_values = Dict[Int64, String]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int64_value()
                        map_key_reader.finish()
                        self.sfixed64_values[parsed_map_key] = reader.string_value()
            elif field_name == "int32StatusValues" or field_name == "int32_status_values":
                if seen_12:
                    raise Error("proto json: duplicate field int32StatusValues")
                seen_12 = True
                if reader.read_null():
                    self.int32_status_values = Dict[Int32, Int32]()
                else:
                    self.int32_status_values = Dict[Int32, Int32]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int32_value()
                        map_key_reader.finish()
                        var enum_name = reader.enum_name()
                        var map_enum_value = Int32(0)
                        var map_enum_known = True
                        if enum_name:
                            if enum_name.value() == "STATUS_UNSPECIFIED":
                                map_enum_value = 0
                            elif enum_name.value() == "STATUS_ACTIVE":
                                map_enum_value = 1
                            elif enum_name.value() == "STATUS_ENABLED":
                                map_enum_value = 1
                            elif enum_name.value() == "STATUS_PAUSED":
                                map_enum_value = 2
                            elif enum_name.value() == "STATUS_NEGATIVE":
                                map_enum_value = -1
                            elif not reader.options.ignore_unknown_fields:
                                raise Error("proto json: unknown enum value")
                            else:
                                map_enum_known = False
                        else:
                            map_enum_value = reader.int32_value()
                        if map_enum_known:
                            self.int32_status_values[parsed_map_key] = map_enum_value
            else:
                reader.skip_unknown_value()


struct JsonMessageMaps(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonMessageMaps` protobuf message."""

    var children: Dict[String, JsonChild]
    """Field `children` (number 1, map field)."""
    var echoes: Dict[Int32, EchoRequest]
    """Field `echoes` (number 2, map field)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.children = Dict[String, JsonChild]()
        self.echoes = Dict[Int32, EchoRequest]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        for entry in self.children.items():
            var sub = WireWriter()
            if entry.key.byte_length() != 0:
                sub.string_field(1, entry.key)
            var vsub = WireWriter()
            entry.value.encode_to(vsub)
            sub.len_prefixed(2, Span(vsub.buf))
            writer.len_prefixed(1, Span(sub.buf))
        for entry in self.echoes.items():
            var sub = WireWriter()
            if entry.key != 0:
                sub.int32(1, entry.key)
            var vsub = WireWriter()
            entry.value.encode_to(vsub)
            sub.len_prefixed(2, Span(vsub.buf))
            writer.len_prefixed(2, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: String = String()
                    var value: JsonChild = JsonChild()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_LEN:
                            key = sub.string_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            var msub = sub.sub_reader()
                            value.merge_from(msub)
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.children[key^] = value^
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var entry_start = reader.pos
                    var sub = reader.sub_reader()
                    var key: Int32 = 0
                    var value: EchoRequest = EchoRequest()
                    var entry_unknown = False
                    while not sub.done():
                        var etag = sub.read_tag()
                        if etag[0] == 1 and etag[1] == WIRE_VARINT:
                            key = sub.int32_value()
                        elif etag[0] == 2 and etag[1] == WIRE_LEN:
                            var msub = sub.sub_reader()
                            value.merge_from(msub)
                        else:
                            entry_unknown = True
                            sub.skip(etag[1])
                    if entry_unknown:
                        reader.pos = entry_start
                        reader.capture_field(field, wire_type, self._unknown)
                    else:
                        self.echoes[key] = value^
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if len(self.children) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("children", "children")
            writer.begin_map()
            for entry in self.children.items():
                writer.map_key(entry.key)
                writer.message_value(entry.value)
            writer.end_map()
        if len(self.echoes) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("echoes", "echoes")
            writer.begin_map()
            for entry in self.echoes.items():
                writer.map_key(String(entry.key))
                writer.message_value(entry.value)
            writer.end_map()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "children" or field_name == "children":
                if seen_1:
                    raise Error("proto json: duplicate field children")
                seen_1 = True
                if reader.read_null():
                    self.children = Dict[String, JsonChild]()
                else:
                    self.children = Dict[String, JsonChild]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        self.children[map_key^] = reader.message_value[JsonChild]()
            elif field_name == "echoes" or field_name == "echoes":
                if seen_2:
                    raise Error("proto json: duplicate field echoes")
                seen_2 = True
                if reader.read_null():
                    self.echoes = Dict[Int32, EchoRequest]()
                else:
                    self.echoes = Dict[Int32, EchoRequest]()
                    var seen_map_keys = List[String]()
                    reader.begin_map()
                    while True:
                        var next_map_key = reader.next_map_key()
                        if not next_map_key:
                            break
                        var map_key = next_map_key.value()
                        for seen_map_key in seen_map_keys:
                            if map_key == seen_map_key:
                                raise Error("proto json: duplicate map key")
                        seen_map_keys.append(String(map_key))
                        if reader.read_null():
                            raise Error("proto json: null map value")
                        var map_key_reader = ProtoJsonReader(map_key)
                        var parsed_map_key = map_key_reader.int32_value()
                        map_key_reader.finish()
                        self.echoes[parsed_map_key] = reader.message_value[EchoRequest]()
            else:
                reader.skip_unknown_value()


struct JsonOneof(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonOneof` protobuf message."""

    var int32_value: Int32
    """Field `int32_value` (number 1)."""
    var int64_value: Int64
    """Field `int64_value` (number 2)."""
    var string_value: String
    """Field `string_value` (number 3)."""
    var bytes_value: List[Byte]
    """Field `bytes_value` (number 4)."""
    var bool_value: Bool
    """Field `bool_value` (number 5)."""
    var status_value: Int32
    """Field `status_value` (number 6)."""
    var child_value: Optional[JsonChild]
    """Field `child_value` (number 7)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    var selection_case: Int
    """Active member of oneof `selection`: 0 when unset, else the set field number."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.int32_value = 0
        self.int64_value = 0
        self.string_value = String()
        self.bytes_value = List[Byte]()
        self.bool_value = False
        self.status_value = 0
        self.child_value = None
        self._unknown = List[Byte]()
        self.selection_case = 0

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.selection_case == 1:
            writer.int32(1, self.int32_value)
        elif self.selection_case == 2:
            writer.int64(2, self.int64_value)
        elif self.selection_case == 3:
            writer.string_field(3, self.string_value)
        elif self.selection_case == 4:
            writer.bytes_field(4, Span(self.bytes_value))
        elif self.selection_case == 5:
            writer.bool_field(5, self.bool_value)
        elif self.selection_case == 6:
            writer.int32(6, self.status_value)
        elif self.selection_case == 7:
            if self.child_value:
                var sub = WireWriter()
                self.child_value.value().encode_to(sub)
                writer.len_prefixed(7, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.int32_value = reader.int32_value()
                    self.selection_case = 1
                    self.int64_value = 0
                    self.string_value = String()
                    self.bytes_value = List[Byte]()
                    self.bool_value = False
                    self.status_value = 0
                    self.child_value = None
            elif field == 2:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.int64_value = reader.int64_value()
                    self.selection_case = 2
                    self.int32_value = 0
                    self.string_value = String()
                    self.bytes_value = List[Byte]()
                    self.bool_value = False
                    self.status_value = 0
                    self.child_value = None
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.string_value = reader.string_value()
                    self.selection_case = 3
                    self.int32_value = 0
                    self.int64_value = 0
                    self.bytes_value = List[Byte]()
                    self.bool_value = False
                    self.status_value = 0
                    self.child_value = None
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.bytes_value = reader.bytes_value()
                    self.selection_case = 4
                    self.int32_value = 0
                    self.int64_value = 0
                    self.string_value = String()
                    self.bool_value = False
                    self.status_value = 0
                    self.child_value = None
            elif field == 5:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.bool_value = reader.bool_value()
                    self.selection_case = 5
                    self.int32_value = 0
                    self.int64_value = 0
                    self.string_value = String()
                    self.bytes_value = List[Byte]()
                    self.status_value = 0
                    self.child_value = None
            elif field == 6:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.status_value = reader.int32_value()
                    self.selection_case = 6
                    self.int32_value = 0
                    self.int64_value = 0
                    self.string_value = String()
                    self.bytes_value = List[Byte]()
                    self.bool_value = False
                    self.child_value = None
            elif field == 7:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: JsonChild
                    if self.selection_case == 7 and self.child_value:
                        m = self.child_value.take()
                    else:
                        m = JsonChild()
                    m.merge_from(sub)
                    self.child_value = m^
                    self.selection_case = 7
                    self.int32_value = 0
                    self.int64_value = 0
                    self.string_value = String()
                    self.bytes_value = List[Byte]()
                    self.bool_value = False
                    self.status_value = 0
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.selection_case == 1:
            writer.field("int32Value", "int32_value")
            writer.int32_value(self.int32_value)
        if self.selection_case == 2:
            writer.field("int64Value", "int64_value")
            writer.int64_value(self.int64_value)
        if self.selection_case == 3:
            writer.field("stringValue", "string_value")
            writer.string_value(self.string_value)
        if self.selection_case == 4:
            writer.field("bytesValue", "bytes_value")
            writer.bytes_value(Span(self.bytes_value))
        if self.selection_case == 5:
            writer.field("boolValue", "bool_value")
            writer.bool_value(self.bool_value)
        if self.selection_case == 6:
            writer.field("statusValue", "status_value")
            if self.status_value == 0:
                writer.string_value("STATUS_UNSPECIFIED")
            elif self.status_value == 1:
                writer.string_value("STATUS_ACTIVE")
            elif self.status_value == 2:
                writer.string_value("STATUS_PAUSED")
            elif self.status_value == -1:
                writer.string_value("STATUS_NEGATIVE")
            else:
                writer.int32_value(self.status_value)
        if self.selection_case == 7:
            writer.field("childValue", "child_value")
            writer.message_value(self.child_value.value())
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_6 = False
        var seen_7 = False
        var seen_oneof_0 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "int32Value" or field_name == "int32_value":
                if seen_1:
                    raise Error("proto json: duplicate field int32Value")
                seen_1 = True
                if reader.read_null():
                    if self.selection_case == 1:
                        self.int32_value = 0
                        self.selection_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.int32_value = reader.int32_value()
                    if self.selection_case != 0 and self.selection_case != 1:
                        raise Error("proto json: multiple oneof fields")
                    self.int64_value = 0
                    self.string_value = String()
                    self.bytes_value = List[Byte]()
                    self.bool_value = False
                    self.status_value = 0
                    self.child_value = None
                    self.selection_case = 1
            elif field_name == "int64Value" or field_name == "int64_value":
                if seen_2:
                    raise Error("proto json: duplicate field int64Value")
                seen_2 = True
                if reader.read_null():
                    if self.selection_case == 2:
                        self.int64_value = 0
                        self.selection_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.int64_value = reader.int64_value()
                    if self.selection_case != 0 and self.selection_case != 2:
                        raise Error("proto json: multiple oneof fields")
                    self.int32_value = 0
                    self.string_value = String()
                    self.bytes_value = List[Byte]()
                    self.bool_value = False
                    self.status_value = 0
                    self.child_value = None
                    self.selection_case = 2
            elif field_name == "stringValue" or field_name == "string_value":
                if seen_3:
                    raise Error("proto json: duplicate field stringValue")
                seen_3 = True
                if reader.read_null():
                    if self.selection_case == 3:
                        self.string_value = String()
                        self.selection_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.string_value = reader.string_value()
                    if self.selection_case != 0 and self.selection_case != 3:
                        raise Error("proto json: multiple oneof fields")
                    self.int32_value = 0
                    self.int64_value = 0
                    self.bytes_value = List[Byte]()
                    self.bool_value = False
                    self.status_value = 0
                    self.child_value = None
                    self.selection_case = 3
            elif field_name == "bytesValue" or field_name == "bytes_value":
                if seen_4:
                    raise Error("proto json: duplicate field bytesValue")
                seen_4 = True
                if reader.read_null():
                    if self.selection_case == 4:
                        self.bytes_value = List[Byte]()
                        self.selection_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.bytes_value = reader.bytes_value()
                    if self.selection_case != 0 and self.selection_case != 4:
                        raise Error("proto json: multiple oneof fields")
                    self.int32_value = 0
                    self.int64_value = 0
                    self.string_value = String()
                    self.bool_value = False
                    self.status_value = 0
                    self.child_value = None
                    self.selection_case = 4
            elif field_name == "boolValue" or field_name == "bool_value":
                if seen_5:
                    raise Error("proto json: duplicate field boolValue")
                seen_5 = True
                if reader.read_null():
                    if self.selection_case == 5:
                        self.bool_value = False
                        self.selection_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.bool_value = reader.bool_value()
                    if self.selection_case != 0 and self.selection_case != 5:
                        raise Error("proto json: multiple oneof fields")
                    self.int32_value = 0
                    self.int64_value = 0
                    self.string_value = String()
                    self.bytes_value = List[Byte]()
                    self.status_value = 0
                    self.child_value = None
                    self.selection_case = 5
            elif field_name == "statusValue" or field_name == "status_value":
                if seen_6:
                    raise Error("proto json: duplicate field statusValue")
                seen_6 = True
                if reader.read_null():
                    if self.selection_case == 6:
                        self.status_value = 0
                        self.selection_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    var oneof_value_known_6 = True
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "STATUS_UNSPECIFIED":
                            self.status_value = 0
                        elif enum_name.value() == "STATUS_ACTIVE":
                            self.status_value = 1
                        elif enum_name.value() == "STATUS_ENABLED":
                            self.status_value = 1
                        elif enum_name.value() == "STATUS_PAUSED":
                            self.status_value = 2
                        elif enum_name.value() == "STATUS_NEGATIVE":
                            self.status_value = -1
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                        else:
                            oneof_value_known_6 = False
                    else:
                        self.status_value = reader.int32_value()
                    if oneof_value_known_6:
                        if self.selection_case != 0 and self.selection_case != 6:
                            raise Error("proto json: multiple oneof fields")
                        self.int32_value = 0
                        self.int64_value = 0
                        self.string_value = String()
                        self.bytes_value = List[Byte]()
                        self.bool_value = False
                        self.child_value = None
                        self.selection_case = 6
            elif field_name == "childValue" or field_name == "child_value":
                if seen_7:
                    raise Error("proto json: duplicate field childValue")
                seen_7 = True
                if reader.read_null():
                    if self.selection_case == 7:
                        self.child_value = None
                        self.selection_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.child_value = reader.message_value[JsonChild]()
                    if self.selection_case != 0 and self.selection_case != 7:
                        raise Error("proto json: multiple oneof fields")
                    self.int32_value = 0
                    self.int64_value = 0
                    self.string_value = String()
                    self.bytes_value = List[Byte]()
                    self.bool_value = False
                    self.status_value = 0
                    self.selection_case = 7
            else:
                reader.skip_unknown_value()


struct JsonOptional(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonOptional` protobuf message."""

    var int32_value: Optional[Int32]
    """Field `int32_value` (number 1, proto3 optional: explicit presence)."""
    var int64_value: Optional[Int64]
    """Field `int64_value` (number 2, proto3 optional: explicit presence)."""
    var uint32_value: Optional[UInt32]
    """Field `uint32_value` (number 3, proto3 optional: explicit presence)."""
    var uint64_value: Optional[UInt64]
    """Field `uint64_value` (number 4, proto3 optional: explicit presence)."""
    var sint32_value: Optional[Int32]
    """Field `sint32_value` (number 5, proto3 optional: explicit presence)."""
    var sint64_value: Optional[Int64]
    """Field `sint64_value` (number 6, proto3 optional: explicit presence)."""
    var bool_value: Optional[Bool]
    """Field `bool_value` (number 7, proto3 optional: explicit presence)."""
    var fixed32_value: Optional[UInt32]
    """Field `fixed32_value` (number 8, proto3 optional: explicit presence)."""
    var fixed64_value: Optional[UInt64]
    """Field `fixed64_value` (number 9, proto3 optional: explicit presence)."""
    var sfixed32_value: Optional[Int32]
    """Field `sfixed32_value` (number 10, proto3 optional: explicit presence)."""
    var sfixed64_value: Optional[Int64]
    """Field `sfixed64_value` (number 11, proto3 optional: explicit presence)."""
    var float_value: Optional[Float32]
    """Field `float_value` (number 12, proto3 optional: explicit presence)."""
    var double_value: Optional[Float64]
    """Field `double_value` (number 13, proto3 optional: explicit presence)."""
    var string_value: Optional[String]
    """Field `string_value` (number 14, proto3 optional: explicit presence)."""
    var bytes_value: Optional[List[Byte]]
    """Field `bytes_value` (number 15, proto3 optional: explicit presence)."""
    var status_value: Optional[Int32]
    """Field `status_value` (number 16, proto3 optional: explicit presence)."""
    var child_value: Optional[JsonChild]
    """Field `child_value` (number 17, proto3 optional: explicit presence)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.int32_value = None
        self.int64_value = None
        self.uint32_value = None
        self.uint64_value = None
        self.sint32_value = None
        self.sint64_value = None
        self.bool_value = None
        self.fixed32_value = None
        self.fixed64_value = None
        self.sfixed32_value = None
        self.sfixed64_value = None
        self.float_value = None
        self.double_value = None
        self.string_value = None
        self.bytes_value = None
        self.status_value = None
        self.child_value = None
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.int32_value:
            writer.int32(1, self.int32_value.value())
        if self.int64_value:
            writer.int64(2, self.int64_value.value())
        if self.uint32_value:
            writer.uint32(3, self.uint32_value.value())
        if self.uint64_value:
            writer.uint64(4, self.uint64_value.value())
        if self.sint32_value:
            writer.sint32(5, self.sint32_value.value())
        if self.sint64_value:
            writer.sint64(6, self.sint64_value.value())
        if self.bool_value:
            writer.bool_field(7, self.bool_value.value())
        if self.fixed32_value:
            writer.fixed32_field(8, self.fixed32_value.value())
        if self.fixed64_value:
            writer.fixed64_field(9, self.fixed64_value.value())
        if self.sfixed32_value:
            writer.sfixed32_field(10, self.sfixed32_value.value())
        if self.sfixed64_value:
            writer.sfixed64_field(11, self.sfixed64_value.value())
        if self.float_value:
            writer.float_field(12, self.float_value.value())
        if self.double_value:
            writer.double_field(13, self.double_value.value())
        if self.string_value:
            writer.string_field(14, self.string_value.value())
        if self.bytes_value:
            writer.bytes_field(15, Span(self.bytes_value.value()))
        if self.status_value:
            writer.int32(16, self.status_value.value())
        if self.child_value:
            var sub = WireWriter()
            self.child_value.value().encode_to(sub)
            writer.len_prefixed(17, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.int32_value = reader.int32_value()
            elif field == 2:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.int64_value = reader.int64_value()
            elif field == 3:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.uint32_value = reader.uint32_value()
            elif field == 4:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.uint64_value = reader.varint()
            elif field == 5:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.sint32_value = reader.sint32_value()
            elif field == 6:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.sint64_value = reader.sint64_value()
            elif field == 7:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.bool_value = reader.bool_value()
            elif field == 8:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.fixed32_value = reader.fixed32()
            elif field == 9:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.fixed64_value = reader.fixed64()
            elif field == 10:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.sfixed32_value = reader.sfixed32_value()
            elif field == 11:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.sfixed64_value = reader.sfixed64_value()
            elif field == 12:
                if wire_type != WIRE_FIXED32:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.float_value = reader.float_value()
            elif field == 13:
                if wire_type != WIRE_FIXED64:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.double_value = reader.double_value()
            elif field == 14:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.string_value = reader.string_value()
            elif field == 15:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.bytes_value = reader.bytes_value()
            elif field == 16:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.status_value = reader.int32_value()
            elif field == 17:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: JsonChild
                    if self.child_value:
                        m = self.child_value.take()
                    else:
                        m = JsonChild()
                    m.merge_from(sub)
                    self.child_value = m^
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.int32_value:
            writer.field("int32Value", "int32_value")
            writer.int32_value(self.int32_value.value())
        if self.int64_value:
            writer.field("int64Value", "int64_value")
            writer.int64_value(self.int64_value.value())
        if self.uint32_value:
            writer.field("uint32Value", "uint32_value")
            writer.uint32_value(self.uint32_value.value())
        if self.uint64_value:
            writer.field("uint64Value", "uint64_value")
            writer.uint64_value(self.uint64_value.value())
        if self.sint32_value:
            writer.field("sint32Value", "sint32_value")
            writer.int32_value(self.sint32_value.value())
        if self.sint64_value:
            writer.field("sint64Value", "sint64_value")
            writer.int64_value(self.sint64_value.value())
        if self.bool_value:
            writer.field("boolValue", "bool_value")
            writer.bool_value(self.bool_value.value())
        if self.fixed32_value:
            writer.field("fixed32Value", "fixed32_value")
            writer.uint32_value(self.fixed32_value.value())
        if self.fixed64_value:
            writer.field("fixed64Value", "fixed64_value")
            writer.uint64_value(self.fixed64_value.value())
        if self.sfixed32_value:
            writer.field("sfixed32Value", "sfixed32_value")
            writer.int32_value(self.sfixed32_value.value())
        if self.sfixed64_value:
            writer.field("sfixed64Value", "sfixed64_value")
            writer.int64_value(self.sfixed64_value.value())
        if self.float_value:
            writer.field("floatValue", "float_value")
            writer.float32_value(self.float_value.value())
        if self.double_value:
            writer.field("doubleValue", "double_value")
            writer.float64_value(self.double_value.value())
        if self.string_value:
            writer.field("stringValue", "string_value")
            writer.string_value(self.string_value.value())
        if self.bytes_value:
            writer.field("bytesValue", "bytes_value")
            writer.bytes_value(Span(self.bytes_value.value()))
        if self.status_value:
            writer.field("statusValue", "status_value")
            if self.status_value.value() == 0:
                writer.string_value("STATUS_UNSPECIFIED")
            elif self.status_value.value() == 1:
                writer.string_value("STATUS_ACTIVE")
            elif self.status_value.value() == 2:
                writer.string_value("STATUS_PAUSED")
            elif self.status_value.value() == -1:
                writer.string_value("STATUS_NEGATIVE")
            else:
                writer.int32_value(self.status_value.value())
        if self.child_value:
            writer.field("childValue", "child_value")
            writer.message_value(self.child_value.value())
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_6 = False
        var seen_7 = False
        var seen_8 = False
        var seen_9 = False
        var seen_10 = False
        var seen_11 = False
        var seen_12 = False
        var seen_13 = False
        var seen_14 = False
        var seen_15 = False
        var seen_16 = False
        var seen_17 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "int32Value" or field_name == "int32_value":
                if seen_1:
                    raise Error("proto json: duplicate field int32Value")
                seen_1 = True
                if reader.read_null():
                    self.int32_value = None
                else:
                    self.int32_value = reader.int32_value()
            elif field_name == "int64Value" or field_name == "int64_value":
                if seen_2:
                    raise Error("proto json: duplicate field int64Value")
                seen_2 = True
                if reader.read_null():
                    self.int64_value = None
                else:
                    self.int64_value = reader.int64_value()
            elif field_name == "uint32Value" or field_name == "uint32_value":
                if seen_3:
                    raise Error("proto json: duplicate field uint32Value")
                seen_3 = True
                if reader.read_null():
                    self.uint32_value = None
                else:
                    self.uint32_value = reader.uint32_value()
            elif field_name == "uint64Value" or field_name == "uint64_value":
                if seen_4:
                    raise Error("proto json: duplicate field uint64Value")
                seen_4 = True
                if reader.read_null():
                    self.uint64_value = None
                else:
                    self.uint64_value = reader.uint64_value()
            elif field_name == "sint32Value" or field_name == "sint32_value":
                if seen_5:
                    raise Error("proto json: duplicate field sint32Value")
                seen_5 = True
                if reader.read_null():
                    self.sint32_value = None
                else:
                    self.sint32_value = reader.int32_value()
            elif field_name == "sint64Value" or field_name == "sint64_value":
                if seen_6:
                    raise Error("proto json: duplicate field sint64Value")
                seen_6 = True
                if reader.read_null():
                    self.sint64_value = None
                else:
                    self.sint64_value = reader.int64_value()
            elif field_name == "boolValue" or field_name == "bool_value":
                if seen_7:
                    raise Error("proto json: duplicate field boolValue")
                seen_7 = True
                if reader.read_null():
                    self.bool_value = None
                else:
                    self.bool_value = reader.bool_value()
            elif field_name == "fixed32Value" or field_name == "fixed32_value":
                if seen_8:
                    raise Error("proto json: duplicate field fixed32Value")
                seen_8 = True
                if reader.read_null():
                    self.fixed32_value = None
                else:
                    self.fixed32_value = reader.uint32_value()
            elif field_name == "fixed64Value" or field_name == "fixed64_value":
                if seen_9:
                    raise Error("proto json: duplicate field fixed64Value")
                seen_9 = True
                if reader.read_null():
                    self.fixed64_value = None
                else:
                    self.fixed64_value = reader.uint64_value()
            elif field_name == "sfixed32Value" or field_name == "sfixed32_value":
                if seen_10:
                    raise Error("proto json: duplicate field sfixed32Value")
                seen_10 = True
                if reader.read_null():
                    self.sfixed32_value = None
                else:
                    self.sfixed32_value = reader.int32_value()
            elif field_name == "sfixed64Value" or field_name == "sfixed64_value":
                if seen_11:
                    raise Error("proto json: duplicate field sfixed64Value")
                seen_11 = True
                if reader.read_null():
                    self.sfixed64_value = None
                else:
                    self.sfixed64_value = reader.int64_value()
            elif field_name == "floatValue" or field_name == "float_value":
                if seen_12:
                    raise Error("proto json: duplicate field floatValue")
                seen_12 = True
                if reader.read_null():
                    self.float_value = None
                else:
                    self.float_value = reader.float32_value()
            elif field_name == "doubleValue" or field_name == "double_value":
                if seen_13:
                    raise Error("proto json: duplicate field doubleValue")
                seen_13 = True
                if reader.read_null():
                    self.double_value = None
                else:
                    self.double_value = reader.float64_value()
            elif field_name == "stringValue" or field_name == "string_value":
                if seen_14:
                    raise Error("proto json: duplicate field stringValue")
                seen_14 = True
                if reader.read_null():
                    self.string_value = None
                else:
                    self.string_value = reader.string_value()
            elif field_name == "bytesValue" or field_name == "bytes_value":
                if seen_15:
                    raise Error("proto json: duplicate field bytesValue")
                seen_15 = True
                if reader.read_null():
                    self.bytes_value = None
                else:
                    self.bytes_value = reader.bytes_value()
            elif field_name == "statusValue" or field_name == "status_value":
                if seen_16:
                    raise Error("proto json: duplicate field statusValue")
                seen_16 = True
                if reader.read_null():
                    self.status_value = None
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "STATUS_UNSPECIFIED":
                            self.status_value = Int32(0)
                        elif enum_name.value() == "STATUS_ACTIVE":
                            self.status_value = Int32(1)
                        elif enum_name.value() == "STATUS_ENABLED":
                            self.status_value = Int32(1)
                        elif enum_name.value() == "STATUS_PAUSED":
                            self.status_value = Int32(2)
                        elif enum_name.value() == "STATUS_NEGATIVE":
                            self.status_value = Int32(-1)
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.status_value = reader.int32_value()
            elif field_name == "childValue" or field_name == "child_value":
                if seen_17:
                    raise Error("proto json: duplicate field childValue")
                seen_17 = True
                if reader.read_null():
                    self.child_value = None
                else:
                    self.child_value = reader.message_value[JsonChild]()
            else:
                reader.skip_unknown_value()


struct JsonEmptyParent(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonEmptyParent` protobuf message."""

    var value: Optional[Empty]
    """Field `value` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.value = None
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.value:
            var sub = WireWriter()
            self.value.value().encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Empty
                    if self.value:
                        m = self.value.take()
                    else:
                        m = Empty()
                    m.merge_from(sub)
                    self.value = m^
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.value:
            writer.field("value", "value")
            writer.message_value(self.value.value())
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "value" or field_name == "value":
                if seen_1:
                    raise Error("proto json: duplicate field value")
                seen_1 = True
                if reader.read_null():
                    self.value = None
                else:
                    self.value = reader.message_value[Empty]()
            else:
                reader.skip_unknown_value()


struct JsonWrappers(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonWrappers` protobuf message."""

    var double_value: Optional[DoubleValue]
    """Field `double_value` (number 1)."""
    var float_value: Optional[FloatValue]
    """Field `float_value` (number 2)."""
    var int64_value: Optional[Int64Value]
    """Field `int64_value` (number 3)."""
    var uint64_value: Optional[UInt64Value]
    """Field `uint64_value` (number 4)."""
    var int32_value: Optional[Int32Value]
    """Field `int32_value` (number 5)."""
    var uint32_value: Optional[UInt32Value]
    """Field `uint32_value` (number 6)."""
    var bool_value: Optional[BoolValue]
    """Field `bool_value` (number 7)."""
    var string_value: Optional[StringValue]
    """Field `string_value` (number 8)."""
    var bytes_value: Optional[BytesValue]
    """Field `bytes_value` (number 9)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.double_value = None
        self.float_value = None
        self.int64_value = None
        self.uint64_value = None
        self.int32_value = None
        self.uint32_value = None
        self.bool_value = None
        self.string_value = None
        self.bytes_value = None
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.double_value:
            var sub = WireWriter()
            self.double_value.value().encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        if self.float_value:
            var sub = WireWriter()
            self.float_value.value().encode_to(sub)
            writer.len_prefixed(2, Span(sub.buf))
        if self.int64_value:
            var sub = WireWriter()
            self.int64_value.value().encode_to(sub)
            writer.len_prefixed(3, Span(sub.buf))
        if self.uint64_value:
            var sub = WireWriter()
            self.uint64_value.value().encode_to(sub)
            writer.len_prefixed(4, Span(sub.buf))
        if self.int32_value:
            var sub = WireWriter()
            self.int32_value.value().encode_to(sub)
            writer.len_prefixed(5, Span(sub.buf))
        if self.uint32_value:
            var sub = WireWriter()
            self.uint32_value.value().encode_to(sub)
            writer.len_prefixed(6, Span(sub.buf))
        if self.bool_value:
            var sub = WireWriter()
            self.bool_value.value().encode_to(sub)
            writer.len_prefixed(7, Span(sub.buf))
        if self.string_value:
            var sub = WireWriter()
            self.string_value.value().encode_to(sub)
            writer.len_prefixed(8, Span(sub.buf))
        if self.bytes_value:
            var sub = WireWriter()
            self.bytes_value.value().encode_to(sub)
            writer.len_prefixed(9, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: DoubleValue
                    if self.double_value:
                        m = self.double_value.take()
                    else:
                        m = DoubleValue()
                    m.merge_from(sub)
                    self.double_value = m^
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: FloatValue
                    if self.float_value:
                        m = self.float_value.take()
                    else:
                        m = FloatValue()
                    m.merge_from(sub)
                    self.float_value = m^
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Int64Value
                    if self.int64_value:
                        m = self.int64_value.take()
                    else:
                        m = Int64Value()
                    m.merge_from(sub)
                    self.int64_value = m^
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: UInt64Value
                    if self.uint64_value:
                        m = self.uint64_value.take()
                    else:
                        m = UInt64Value()
                    m.merge_from(sub)
                    self.uint64_value = m^
            elif field == 5:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Int32Value
                    if self.int32_value:
                        m = self.int32_value.take()
                    else:
                        m = Int32Value()
                    m.merge_from(sub)
                    self.int32_value = m^
            elif field == 6:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: UInt32Value
                    if self.uint32_value:
                        m = self.uint32_value.take()
                    else:
                        m = UInt32Value()
                    m.merge_from(sub)
                    self.uint32_value = m^
            elif field == 7:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: BoolValue
                    if self.bool_value:
                        m = self.bool_value.take()
                    else:
                        m = BoolValue()
                    m.merge_from(sub)
                    self.bool_value = m^
            elif field == 8:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: StringValue
                    if self.string_value:
                        m = self.string_value.take()
                    else:
                        m = StringValue()
                    m.merge_from(sub)
                    self.string_value = m^
            elif field == 9:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: BytesValue
                    if self.bytes_value:
                        m = self.bytes_value.take()
                    else:
                        m = BytesValue()
                    m.merge_from(sub)
                    self.bytes_value = m^
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.double_value:
            writer.field("doubleValue", "double_value")
            writer.message_value(self.double_value.value())
        if self.float_value:
            writer.field("floatValue", "float_value")
            writer.message_value(self.float_value.value())
        if self.int64_value:
            writer.field("int64Value", "int64_value")
            writer.message_value(self.int64_value.value())
        if self.uint64_value:
            writer.field("uint64Value", "uint64_value")
            writer.message_value(self.uint64_value.value())
        if self.int32_value:
            writer.field("int32Value", "int32_value")
            writer.message_value(self.int32_value.value())
        if self.uint32_value:
            writer.field("uint32Value", "uint32_value")
            writer.message_value(self.uint32_value.value())
        if self.bool_value:
            writer.field("boolValue", "bool_value")
            writer.message_value(self.bool_value.value())
        if self.string_value:
            writer.field("stringValue", "string_value")
            writer.message_value(self.string_value.value())
        if self.bytes_value:
            writer.field("bytesValue", "bytes_value")
            writer.message_value(self.bytes_value.value())
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_6 = False
        var seen_7 = False
        var seen_8 = False
        var seen_9 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "doubleValue" or field_name == "double_value":
                if seen_1:
                    raise Error("proto json: duplicate field doubleValue")
                seen_1 = True
                if reader.read_null():
                    self.double_value = None
                else:
                    self.double_value = reader.message_value[DoubleValue]()
            elif field_name == "floatValue" or field_name == "float_value":
                if seen_2:
                    raise Error("proto json: duplicate field floatValue")
                seen_2 = True
                if reader.read_null():
                    self.float_value = None
                else:
                    self.float_value = reader.message_value[FloatValue]()
            elif field_name == "int64Value" or field_name == "int64_value":
                if seen_3:
                    raise Error("proto json: duplicate field int64Value")
                seen_3 = True
                if reader.read_null():
                    self.int64_value = None
                else:
                    self.int64_value = reader.message_value[Int64Value]()
            elif field_name == "uint64Value" or field_name == "uint64_value":
                if seen_4:
                    raise Error("proto json: duplicate field uint64Value")
                seen_4 = True
                if reader.read_null():
                    self.uint64_value = None
                else:
                    self.uint64_value = reader.message_value[UInt64Value]()
            elif field_name == "int32Value" or field_name == "int32_value":
                if seen_5:
                    raise Error("proto json: duplicate field int32Value")
                seen_5 = True
                if reader.read_null():
                    self.int32_value = None
                else:
                    self.int32_value = reader.message_value[Int32Value]()
            elif field_name == "uint32Value" or field_name == "uint32_value":
                if seen_6:
                    raise Error("proto json: duplicate field uint32Value")
                seen_6 = True
                if reader.read_null():
                    self.uint32_value = None
                else:
                    self.uint32_value = reader.message_value[UInt32Value]()
            elif field_name == "boolValue" or field_name == "bool_value":
                if seen_7:
                    raise Error("proto json: duplicate field boolValue")
                seen_7 = True
                if reader.read_null():
                    self.bool_value = None
                else:
                    self.bool_value = reader.message_value[BoolValue]()
            elif field_name == "stringValue" or field_name == "string_value":
                if seen_8:
                    raise Error("proto json: duplicate field stringValue")
                seen_8 = True
                if reader.read_null():
                    self.string_value = None
                else:
                    self.string_value = reader.message_value[StringValue]()
            elif field_name == "bytesValue" or field_name == "bytes_value":
                if seen_9:
                    raise Error("proto json: duplicate field bytesValue")
                seen_9 = True
                if reader.read_null():
                    self.bytes_value = None
                else:
                    self.bytes_value = reader.message_value[BytesValue]()
            else:
                reader.skip_unknown_value()


struct JsonTimestamp(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonTimestamp` protobuf message."""

    var value: Optional[Timestamp]
    """Field `value` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.value = None
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.value:
            var sub = WireWriter()
            self.value.value().encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Timestamp
                    if self.value:
                        m = self.value.take()
                    else:
                        m = Timestamp()
                    m.merge_from(sub)
                    self.value = m^
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.value:
            writer.field("value", "value")
            writer.message_value(self.value.value())
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "value" or field_name == "value":
                if seen_1:
                    raise Error("proto json: duplicate field value")
                seen_1 = True
                if reader.read_null():
                    self.value = None
                else:
                    self.value = reader.message_value[Timestamp]()
            else:
                reader.skip_unknown_value()


struct JsonDuration(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonDuration` protobuf message."""

    var value: Optional[Duration]
    """Field `value` (number 1)."""
    var values: List[Duration]
    """Field `values` (number 2, repeated)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.value = None
        self.values = List[Duration]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.value:
            var sub = WireWriter()
            self.value.value().encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        for v in self.values:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(2, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: Duration
                    if self.value:
                        m = self.value.take()
                    else:
                        m = Duration()
                    m.merge_from(sub)
                    self.value = m^
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = Duration()
                    m.merge_from(sub)
                    self.values.append(m^)
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.value:
            writer.field("value", "value")
            writer.message_value(self.value.value())
        if len(self.values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("values", "values")
            writer.begin_array()
            for item in self.values:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "value" or field_name == "value":
                if seen_1:
                    raise Error("proto json: duplicate field value")
                seen_1 = True
                if reader.read_null():
                    self.value = None
                else:
                    self.value = reader.message_value[Duration]()
            elif field_name == "values" or field_name == "values":
                if seen_2:
                    raise Error("proto json: duplicate field values")
                seen_2 = True
                if reader.read_null():
                    self.values = List[Duration]()
                else:
                    self.values = List[Duration]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.values.append(reader.message_value[Duration]())
            else:
                reader.skip_unknown_value()


struct JsonFieldMask(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JsonFieldMask` protobuf message."""

    var value: Optional[FieldMask]
    """Field `value` (number 1)."""
    var values: List[FieldMask]
    """Field `values` (number 2, repeated)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.value = None
        self.values = List[FieldMask]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.value:
            var sub = WireWriter()
            self.value.value().encode_to(sub)
            writer.len_prefixed(1, Span(sub.buf))
        for v in self.values:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(2, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: FieldMask
                    if self.value:
                        m = self.value.take()
                    else:
                        m = FieldMask()
                    m.merge_from(sub)
                    self.value = m^
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = FieldMask()
                    m.merge_from(sub)
                    self.values.append(m^)
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.value:
            writer.field("value", "value")
            writer.message_value(self.value.value())
        if len(self.values) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("values", "values")
            writer.begin_array()
            for item in self.values:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "value" or field_name == "value":
                if seen_1:
                    raise Error("proto json: duplicate field value")
                seen_1 = True
                if reader.read_null():
                    self.value = None
                else:
                    self.value = reader.message_value[FieldMask]()
            elif field_name == "values" or field_name == "values":
                if seen_2:
                    raise Error("proto json: duplicate field values")
                seen_2 = True
                if reader.read_null():
                    self.values = List[FieldMask]()
                else:
                    self.values = List[FieldMask]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.values.append(reader.message_value[FieldMask]())
            else:
                reader.skip_unknown_value()
