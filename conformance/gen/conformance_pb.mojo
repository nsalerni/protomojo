# GENERATED by protoc-gen-mojo from conformance.proto — do not edit.

"""Generated protobuf messages from `conformance.proto`.

Regenerate with tools/protoc-gen-mojo; do not edit by hand.
"""

from proto import (
    WIRE_FIXED32,
    WIRE_FIXED64,
    WIRE_LEN,
    WIRE_VARINT,
    ProtoMessage,
    WireReader,
    WireWriter,
    zigzag_encode32,
    zigzag_encode64,
)


struct WireFormat:
    """Values of the `WireFormat` protobuf enum (fields carry `Int32`)."""

    comptime UNSPECIFIED = 0
    """`UNSPECIFIED` = 0."""
    comptime PROTOBUF = 1
    """`PROTOBUF` = 1."""
    comptime JSON = 2
    """`JSON` = 2."""
    comptime JSPB = 3
    """`JSPB` = 3."""
    comptime TEXT_FORMAT = 4
    """`TEXT_FORMAT` = 4."""


struct TestCategory:
    """Values of the `TestCategory` protobuf enum (fields carry `Int32`)."""

    comptime UNSPECIFIED_TEST = 0
    """`UNSPECIFIED_TEST` = 0."""
    comptime BINARY_TEST = 1
    """`BINARY_TEST` = 1."""
    comptime JSON_TEST = 2
    """`JSON_TEST` = 2."""
    comptime JSON_IGNORE_UNKNOWN_PARSING_TEST = 3
    """`JSON_IGNORE_UNKNOWN_PARSING_TEST` = 3."""
    comptime JSPB_TEST = 4
    """`JSPB_TEST` = 4."""
    comptime TEXT_FORMAT_TEST = 5
    """`TEXT_FORMAT_TEST` = 5."""


struct TestStatus(Copyable, Defaultable, Movable, ProtoMessage):
    """Generated from the `TestStatus` protobuf message."""

    var name: String
    """Field `name` (number 1)."""
    var failure_message: String
    """Field `failure_message` (number 2)."""
    var matched_name: String
    """Field `matched_name` (number 3)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.name = String()
        self.failure_message = String()
        self.matched_name = String()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.name.byte_length() != 0:
            writer.string_field(1, self.name)
        if self.failure_message.byte_length() != 0:
            writer.string_field(2, self.failure_message)
        if self.matched_name.byte_length() != 0:
            writer.string_field(3, self.matched_name)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.name = reader.string_value()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.failure_message = reader.string_value()
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.matched_name = reader.string_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)


struct FailureSet(Copyable, Defaultable, Movable, ProtoMessage):
    """Generated from the `FailureSet` protobuf message."""

    var test: List[TestStatus]
    """Field `test` (number 2, repeated)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.test = List[TestStatus]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        for v in self.test:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(2, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = TestStatus()
                    m.merge_from(sub)
                    self.test.append(m^)
            else:
                reader.capture_field(field, wire_type, self._unknown)


struct ConformanceRequest(Copyable, Defaultable, Movable, ProtoMessage):
    """Generated from the `ConformanceRequest` protobuf message."""

    var protobuf_payload: List[Byte]
    """Field `protobuf_payload` (number 1)."""
    var json_payload: String
    """Field `json_payload` (number 2)."""
    var jspb_payload: String
    """Field `jspb_payload` (number 7)."""
    var text_payload: String
    """Field `text_payload` (number 8)."""
    var requested_output_format: Int32
    """Field `requested_output_format` (number 3)."""
    var message_type: String
    """Field `message_type` (number 4)."""
    var test_category: Int32
    """Field `test_category` (number 5)."""
    var jspb_encoding_options: Optional[JspbEncodingConfig]
    """Field `jspb_encoding_options` (number 6)."""
    var print_unknown_fields: Bool
    """Field `print_unknown_fields` (number 9)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    var payload_case: Int
    """Active member of oneof `payload`: 0 when unset, else the set field number."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.protobuf_payload = List[Byte]()
        self.json_payload = String()
        self.jspb_payload = String()
        self.text_payload = String()
        self.requested_output_format = 0
        self.message_type = String()
        self.test_category = 0
        self.jspb_encoding_options = None
        self.print_unknown_fields = False
        self._unknown = List[Byte]()
        self.payload_case = 0

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.requested_output_format != 0:
            writer.int32(3, self.requested_output_format)
        if self.message_type.byte_length() != 0:
            writer.string_field(4, self.message_type)
        if self.test_category != 0:
            writer.int32(5, self.test_category)
        if self.jspb_encoding_options:
            var sub = WireWriter()
            self.jspb_encoding_options.value().encode_to(sub)
            writer.len_prefixed(6, Span(sub.buf))
        if self.print_unknown_fields:
            writer.bool_field(9, self.print_unknown_fields)
        if self.payload_case == 1:
            writer.bytes_field(1, Span(self.protobuf_payload))
        elif self.payload_case == 2:
            writer.string_field(2, self.json_payload)
        elif self.payload_case == 7:
            writer.string_field(7, self.jspb_payload)
        elif self.payload_case == 8:
            writer.string_field(8, self.text_payload)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.protobuf_payload = reader.bytes_value()
                    self.payload_case = 1
                    self.json_payload = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.json_payload = reader.string_value()
                    self.payload_case = 2
                    self.protobuf_payload = List[Byte]()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 3:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.requested_output_format = reader.int32_value()
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.message_type = reader.string_value()
            elif field == 5:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.test_category = reader.int32_value()
            elif field == 6:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: JspbEncodingConfig
                    if self.jspb_encoding_options:
                        m = self.jspb_encoding_options.take()
                    else:
                        m = JspbEncodingConfig()
                    m.merge_from(sub)
                    self.jspb_encoding_options = m^
            elif field == 7:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.jspb_payload = reader.string_value()
                    self.payload_case = 7
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.text_payload = String()
            elif field == 8:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.text_payload = reader.string_value()
                    self.payload_case = 8
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.jspb_payload = String()
            elif field == 9:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.print_unknown_fields = reader.bool_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)


struct ConformanceResponse(Copyable, Defaultable, Movable, ProtoMessage):
    """Generated from the `ConformanceResponse` protobuf message."""

    var parse_error: String
    """Field `parse_error` (number 1)."""
    var serialize_error: String
    """Field `serialize_error` (number 6)."""
    var timeout_error: String
    """Field `timeout_error` (number 9)."""
    var runtime_error: String
    """Field `runtime_error` (number 2)."""
    var protobuf_payload: List[Byte]
    """Field `protobuf_payload` (number 3)."""
    var json_payload: String
    """Field `json_payload` (number 4)."""
    var skipped: String
    """Field `skipped` (number 5)."""
    var jspb_payload: String
    """Field `jspb_payload` (number 7)."""
    var text_payload: String
    """Field `text_payload` (number 8)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    var result_case: Int
    """Active member of oneof `result`: 0 when unset, else the set field number."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.parse_error = String()
        self.serialize_error = String()
        self.timeout_error = String()
        self.runtime_error = String()
        self.protobuf_payload = List[Byte]()
        self.json_payload = String()
        self.skipped = String()
        self.jspb_payload = String()
        self.text_payload = String()
        self._unknown = List[Byte]()
        self.result_case = 0

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.result_case == 1:
            writer.string_field(1, self.parse_error)
        elif self.result_case == 6:
            writer.string_field(6, self.serialize_error)
        elif self.result_case == 9:
            writer.string_field(9, self.timeout_error)
        elif self.result_case == 2:
            writer.string_field(2, self.runtime_error)
        elif self.result_case == 3:
            writer.bytes_field(3, Span(self.protobuf_payload))
        elif self.result_case == 4:
            writer.string_field(4, self.json_payload)
        elif self.result_case == 5:
            writer.string_field(5, self.skipped)
        elif self.result_case == 7:
            writer.string_field(7, self.jspb_payload)
        elif self.result_case == 8:
            writer.string_field(8, self.text_payload)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.parse_error = reader.string_value()
                    self.result_case = 1
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.runtime_error = reader.string_value()
                    self.result_case = 2
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.protobuf_payload = reader.bytes_value()
                    self.result_case = 3
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.json_payload = reader.string_value()
                    self.result_case = 4
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 5:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.skipped = reader.string_value()
                    self.result_case = 5
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 6:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.serialize_error = reader.string_value()
                    self.result_case = 6
                    self.parse_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 7:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.jspb_payload = reader.string_value()
                    self.result_case = 7
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.text_payload = String()
            elif field == 8:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.text_payload = reader.string_value()
                    self.result_case = 8
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
            elif field == 9:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.timeout_error = reader.string_value()
                    self.result_case = 9
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            else:
                reader.capture_field(field, wire_type, self._unknown)


struct JspbEncodingConfig(Copyable, Defaultable, Movable, ProtoMessage):
    """Generated from the `JspbEncodingConfig` protobuf message."""

    var use_jspb_array_any_format: Bool
    """Field `use_jspb_array_any_format` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.use_jspb_array_any_format = False
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.use_jspb_array_any_format:
            writer.bool_field(1, self.use_jspb_array_any_format)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.use_jspb_array_any_format = reader.bool_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)
