# Generated by protoc-gen-mojo from conformance.proto. Do not edit.

"""Generated protobuf messages from `conformance.proto`.

Regenerate with tools/protoc-gen-mojo; do not edit by hand.
"""

from proto import (
    WIRE_FIXED32,
    WIRE_FIXED64,
    WIRE_LEN,
    WIRE_VARINT,
    ProtoMessage,
    WireReader,
    WireWriter,
    zigzag_encode32,
    zigzag_encode64,
)
from proto import (
    ProtoJsonMessage,
    ProtoJsonReader,
    ProtoJsonWriter,
)


struct WireFormat(Copyable, ImplicitlyCopyable, Movable, Equatable):
    """Proto3 open enum `WireFormat`. Unknown numeric values are preserved."""

    var value: Int32
    """The wire number, including values not named in the .proto."""

    comptime UNSPECIFIED: Int32 = 0
    """`UNSPECIFIED` = 0."""
    comptime PROTOBUF: Int32 = 1
    """`PROTOBUF` = 1."""
    comptime JSON: Int32 = 2
    """`JSON` = 2."""
    comptime JSPB: Int32 = 3
    """`JSPB` = 3."""
    comptime TEXT_FORMAT: Int32 = 4
    """`TEXT_FORMAT` = 4."""

    def __init__(out self, value: Int32 = 0):
        """Wraps a proto3 enum number, including unknown values.

        Args:
            value: The wire number. Defaults to 0 (the unspecified value).
        """
        self.value = value

    def __eq__(self, other: Self) -> Bool:
        """Compares two enum wrappers by wire number."""
        return self.value == other.value

    def name(self) -> Optional[String]:
        """Returns the first declared name for this number, or None if unknown."""
        if self.value == 0:
            return String("UNSPECIFIED")
        elif self.value == 1:
            return String("PROTOBUF")
        elif self.value == 2:
            return String("JSON")
        elif self.value == 3:
            return String("JSPB")
        elif self.value == 4:
            return String("TEXT_FORMAT")
        return None

    @staticmethod
    def from_name(name: StringSpan) -> Optional[Self]:
        """Looks up a declared name. Unknown names return None."""
        if name == "UNSPECIFIED":
            return Self(value=0)
        elif name == "PROTOBUF":
            return Self(value=1)
        elif name == "JSON":
            return Self(value=2)
        elif name == "JSPB":
            return Self(value=3)
        elif name == "TEXT_FORMAT":
            return Self(value=4)
        return None


struct TestCategory(Copyable, ImplicitlyCopyable, Movable, Equatable):
    """Proto3 open enum `TestCategory`. Unknown numeric values are preserved."""

    var value: Int32
    """The wire number, including values not named in the .proto."""

    comptime UNSPECIFIED_TEST: Int32 = 0
    """`UNSPECIFIED_TEST` = 0."""
    comptime BINARY_TEST: Int32 = 1
    """`BINARY_TEST` = 1."""
    comptime JSON_TEST: Int32 = 2
    """`JSON_TEST` = 2."""
    comptime JSON_IGNORE_UNKNOWN_PARSING_TEST: Int32 = 3
    """`JSON_IGNORE_UNKNOWN_PARSING_TEST` = 3."""
    comptime JSPB_TEST: Int32 = 4
    """`JSPB_TEST` = 4."""
    comptime TEXT_FORMAT_TEST: Int32 = 5
    """`TEXT_FORMAT_TEST` = 5."""

    def __init__(out self, value: Int32 = 0):
        """Wraps a proto3 enum number, including unknown values.

        Args:
            value: The wire number. Defaults to 0 (the unspecified value).
        """
        self.value = value

    def __eq__(self, other: Self) -> Bool:
        """Compares two enum wrappers by wire number."""
        return self.value == other.value

    def name(self) -> Optional[String]:
        """Returns the first declared name for this number, or None if unknown."""
        if self.value == 0:
            return String("UNSPECIFIED_TEST")
        elif self.value == 1:
            return String("BINARY_TEST")
        elif self.value == 2:
            return String("JSON_TEST")
        elif self.value == 3:
            return String("JSON_IGNORE_UNKNOWN_PARSING_TEST")
        elif self.value == 4:
            return String("JSPB_TEST")
        elif self.value == 5:
            return String("TEXT_FORMAT_TEST")
        return None

    @staticmethod
    def from_name(name: StringSpan) -> Optional[Self]:
        """Looks up a declared name. Unknown names return None."""
        if name == "UNSPECIFIED_TEST":
            return Self(value=0)
        elif name == "BINARY_TEST":
            return Self(value=1)
        elif name == "JSON_TEST":
            return Self(value=2)
        elif name == "JSON_IGNORE_UNKNOWN_PARSING_TEST":
            return Self(value=3)
        elif name == "JSPB_TEST":
            return Self(value=4)
        elif name == "TEXT_FORMAT_TEST":
            return Self(value=5)
        return None


struct TestStatus(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `TestStatus` protobuf message."""

    var name: String
    """Field `name` (number 1)."""
    var failure_message: String
    """Field `failure_message` (number 2)."""
    var matched_name: String
    """Field `matched_name` (number 3)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.name = String()
        self.failure_message = String()
        self.matched_name = String()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.name.byte_length() != 0:
            writer.string_field(1, self.name)
        if self.failure_message.byte_length() != 0:
            writer.string_field(2, self.failure_message)
        if self.matched_name.byte_length() != 0:
            writer.string_field(3, self.matched_name)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.name = reader.string_value()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.failure_message = reader.string_value()
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.matched_name = reader.string_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.name.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("name", "name")
            writer.string_value(self.name)
        if self.failure_message.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("failureMessage", "failure_message")
            writer.string_value(self.failure_message)
        if self.matched_name.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("matchedName", "matched_name")
            writer.string_value(self.matched_name)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_3 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "name" or field_name == "name":
                if seen_1:
                    raise Error("proto json: duplicate field name")
                seen_1 = True
                if reader.read_null():
                    self.name = String()
                else:
                    self.name = reader.string_value()
            elif field_name == "failureMessage" or field_name == "failure_message":
                if seen_2:
                    raise Error("proto json: duplicate field failureMessage")
                seen_2 = True
                if reader.read_null():
                    self.failure_message = String()
                else:
                    self.failure_message = reader.string_value()
            elif field_name == "matchedName" or field_name == "matched_name":
                if seen_3:
                    raise Error("proto json: duplicate field matchedName")
                seen_3 = True
                if reader.read_null():
                    self.matched_name = String()
                else:
                    self.matched_name = reader.string_value()
            else:
                reader.skip_unknown_value()


struct FailureSet(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `FailureSet` protobuf message."""

    var test: List[TestStatus]
    """Field `test` (number 2, repeated)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.test = List[TestStatus]()
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        for v in self.test:
            var sub = WireWriter()
            v.encode_to(sub)
            writer.len_prefixed(2, Span(sub.buf))
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m = TestStatus()
                    m.merge_from(sub)
                    self.test.append(m^)
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if len(self.test) != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("test", "test")
            writer.begin_array()
            for item in self.test:
                writer.array_item()
                writer.message_value(item)
            writer.end_array()
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_2 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "test" or field_name == "test":
                if seen_2:
                    raise Error("proto json: duplicate field test")
                seen_2 = True
                if reader.read_null():
                    self.test = List[TestStatus]()
                else:
                    self.test = List[TestStatus]()
                    reader.begin_array()
                    while reader.next_array_item():
                        if reader.read_null():
                            raise Error("proto json: null array element")
                        self.test.append(reader.message_value[TestStatus]())
            else:
                reader.skip_unknown_value()


struct ConformanceRequest(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `ConformanceRequest` protobuf message."""

    var protobuf_payload: List[Byte]
    """Field `protobuf_payload` (number 1)."""
    var json_payload: String
    """Field `json_payload` (number 2)."""
    var jspb_payload: String
    """Field `jspb_payload` (number 7)."""
    var text_payload: String
    """Field `text_payload` (number 8)."""
    var requested_output_format: WireFormat
    """Field `requested_output_format` (number 3)."""
    var message_type: String
    """Field `message_type` (number 4)."""
    var test_category: TestCategory
    """Field `test_category` (number 5)."""
    var jspb_encoding_options: Optional[JspbEncodingConfig]
    """Field `jspb_encoding_options` (number 6)."""
    var print_unknown_fields: Bool
    """Field `print_unknown_fields` (number 9)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    var payload_case: Int
    """Active member of oneof `payload`: 0 when unset, else the set field number."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.protobuf_payload = List[Byte]()
        self.json_payload = String()
        self.jspb_payload = String()
        self.text_payload = String()
        self.requested_output_format = WireFormat()
        self.message_type = String()
        self.test_category = TestCategory()
        self.jspb_encoding_options = None
        self.print_unknown_fields = False
        self._unknown = List[Byte]()
        self.payload_case = 0

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.requested_output_format.value != 0:
            writer.int32(3, self.requested_output_format.value)
        if self.message_type.byte_length() != 0:
            writer.string_field(4, self.message_type)
        if self.test_category.value != 0:
            writer.int32(5, self.test_category.value)
        if self.jspb_encoding_options:
            var sub = WireWriter()
            self.jspb_encoding_options.value().encode_to(sub)
            writer.len_prefixed(6, Span(sub.buf))
        if self.print_unknown_fields:
            writer.bool_field(9, self.print_unknown_fields)
        if self.payload_case == 1:
            writer.bytes_field(1, Span(self.protobuf_payload))
        elif self.payload_case == 2:
            writer.string_field(2, self.json_payload)
        elif self.payload_case == 7:
            writer.string_field(7, self.jspb_payload)
        elif self.payload_case == 8:
            writer.string_field(8, self.text_payload)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.protobuf_payload = reader.bytes_value()
                    self.payload_case = 1
                    self.json_payload = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.json_payload = reader.string_value()
                    self.payload_case = 2
                    self.protobuf_payload = List[Byte]()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 3:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.requested_output_format = WireFormat(value=reader.int32_value())
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.message_type = reader.string_value()
            elif field == 5:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.test_category = TestCategory(value=reader.int32_value())
            elif field == 6:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    var sub = reader.sub_reader()
                    var m: JspbEncodingConfig
                    if self.jspb_encoding_options:
                        m = self.jspb_encoding_options.take()
                    else:
                        m = JspbEncodingConfig()
                    m.merge_from(sub)
                    self.jspb_encoding_options = m^
            elif field == 7:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.jspb_payload = reader.string_value()
                    self.payload_case = 7
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.text_payload = String()
            elif field == 8:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.text_payload = reader.string_value()
                    self.payload_case = 8
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.jspb_payload = String()
            elif field == 9:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.print_unknown_fields = reader.bool_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.payload_case == 1:
            writer.field("protobufPayload", "protobuf_payload")
            writer.bytes_value(Span(self.protobuf_payload))
        if self.payload_case == 2:
            writer.field("jsonPayload", "json_payload")
            writer.string_value(self.json_payload)
        if self.payload_case == 7:
            writer.field("jspbPayload", "jspb_payload")
            writer.string_value(self.jspb_payload)
        if self.payload_case == 8:
            writer.field("textPayload", "text_payload")
            writer.string_value(self.text_payload)
        if self.requested_output_format.value != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("requestedOutputFormat", "requested_output_format")
            if self.requested_output_format.value == 0:
                writer.string_value("UNSPECIFIED")
            elif self.requested_output_format.value == 1:
                writer.string_value("PROTOBUF")
            elif self.requested_output_format.value == 2:
                writer.string_value("JSON")
            elif self.requested_output_format.value == 3:
                writer.string_value("JSPB")
            elif self.requested_output_format.value == 4:
                writer.string_value("TEXT_FORMAT")
            else:
                writer.int32_value(self.requested_output_format.value)
        if self.message_type.byte_length() != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("messageType", "message_type")
            writer.string_value(self.message_type)
        if self.test_category.value != 0 or writer.options.always_print_fields_with_no_presence:
            writer.field("testCategory", "test_category")
            if self.test_category.value == 0:
                writer.string_value("UNSPECIFIED_TEST")
            elif self.test_category.value == 1:
                writer.string_value("BINARY_TEST")
            elif self.test_category.value == 2:
                writer.string_value("JSON_TEST")
            elif self.test_category.value == 3:
                writer.string_value("JSON_IGNORE_UNKNOWN_PARSING_TEST")
            elif self.test_category.value == 4:
                writer.string_value("JSPB_TEST")
            elif self.test_category.value == 5:
                writer.string_value("TEXT_FORMAT_TEST")
            else:
                writer.int32_value(self.test_category.value)
        if self.jspb_encoding_options:
            writer.field("jspbEncodingOptions", "jspb_encoding_options")
            writer.message_value(self.jspb_encoding_options.value())
        if self.print_unknown_fields or writer.options.always_print_fields_with_no_presence:
            writer.field("printUnknownFields", "print_unknown_fields")
            writer.bool_value(self.print_unknown_fields)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_2 = False
        var seen_7 = False
        var seen_8 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_6 = False
        var seen_9 = False
        var seen_oneof_0 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "protobufPayload" or field_name == "protobuf_payload":
                if seen_1:
                    raise Error("proto json: duplicate field protobufPayload")
                seen_1 = True
                if reader.read_null():
                    if self.payload_case == 1:
                        self.protobuf_payload = List[Byte]()
                        self.payload_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.protobuf_payload = reader.bytes_value()
                    if self.payload_case != 0 and self.payload_case != 1:
                        raise Error("proto json: multiple oneof fields")
                    self.json_payload = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
                    self.payload_case = 1
            elif field_name == "jsonPayload" or field_name == "json_payload":
                if seen_2:
                    raise Error("proto json: duplicate field jsonPayload")
                seen_2 = True
                if reader.read_null():
                    if self.payload_case == 2:
                        self.json_payload = String()
                        self.payload_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.json_payload = reader.string_value()
                    if self.payload_case != 0 and self.payload_case != 2:
                        raise Error("proto json: multiple oneof fields")
                    self.protobuf_payload = List[Byte]()
                    self.jspb_payload = String()
                    self.text_payload = String()
                    self.payload_case = 2
            elif field_name == "jspbPayload" or field_name == "jspb_payload":
                if seen_7:
                    raise Error("proto json: duplicate field jspbPayload")
                seen_7 = True
                if reader.read_null():
                    if self.payload_case == 7:
                        self.jspb_payload = String()
                        self.payload_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.jspb_payload = reader.string_value()
                    if self.payload_case != 0 and self.payload_case != 7:
                        raise Error("proto json: multiple oneof fields")
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.text_payload = String()
                    self.payload_case = 7
            elif field_name == "textPayload" or field_name == "text_payload":
                if seen_8:
                    raise Error("proto json: duplicate field textPayload")
                seen_8 = True
                if reader.read_null():
                    if self.payload_case == 8:
                        self.text_payload = String()
                        self.payload_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.text_payload = reader.string_value()
                    if self.payload_case != 0 and self.payload_case != 8:
                        raise Error("proto json: multiple oneof fields")
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.jspb_payload = String()
                    self.payload_case = 8
            elif field_name == "requestedOutputFormat" or field_name == "requested_output_format":
                if seen_3:
                    raise Error("proto json: duplicate field requestedOutputFormat")
                seen_3 = True
                if reader.read_null():
                    self.requested_output_format = WireFormat()
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "UNSPECIFIED":
                            self.requested_output_format = WireFormat(value=0)
                        elif enum_name.value() == "PROTOBUF":
                            self.requested_output_format = WireFormat(value=1)
                        elif enum_name.value() == "JSON":
                            self.requested_output_format = WireFormat(value=2)
                        elif enum_name.value() == "JSPB":
                            self.requested_output_format = WireFormat(value=3)
                        elif enum_name.value() == "TEXT_FORMAT":
                            self.requested_output_format = WireFormat(value=4)
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.requested_output_format = WireFormat(value=reader.int32_value())
            elif field_name == "messageType" or field_name == "message_type":
                if seen_4:
                    raise Error("proto json: duplicate field messageType")
                seen_4 = True
                if reader.read_null():
                    self.message_type = String()
                else:
                    self.message_type = reader.string_value()
            elif field_name == "testCategory" or field_name == "test_category":
                if seen_5:
                    raise Error("proto json: duplicate field testCategory")
                seen_5 = True
                if reader.read_null():
                    self.test_category = TestCategory()
                else:
                    var enum_name = reader.enum_name()
                    if enum_name:
                        if enum_name.value() == "UNSPECIFIED_TEST":
                            self.test_category = TestCategory(value=0)
                        elif enum_name.value() == "BINARY_TEST":
                            self.test_category = TestCategory(value=1)
                        elif enum_name.value() == "JSON_TEST":
                            self.test_category = TestCategory(value=2)
                        elif enum_name.value() == "JSON_IGNORE_UNKNOWN_PARSING_TEST":
                            self.test_category = TestCategory(value=3)
                        elif enum_name.value() == "JSPB_TEST":
                            self.test_category = TestCategory(value=4)
                        elif enum_name.value() == "TEXT_FORMAT_TEST":
                            self.test_category = TestCategory(value=5)
                        elif not reader.options.ignore_unknown_fields:
                            raise Error("proto json: unknown enum value")
                    else:
                        self.test_category = TestCategory(value=reader.int32_value())
            elif field_name == "jspbEncodingOptions" or field_name == "jspb_encoding_options":
                if seen_6:
                    raise Error("proto json: duplicate field jspbEncodingOptions")
                seen_6 = True
                if reader.read_null():
                    self.jspb_encoding_options = None
                else:
                    self.jspb_encoding_options = reader.message_value[JspbEncodingConfig]()
            elif field_name == "printUnknownFields" or field_name == "print_unknown_fields":
                if seen_9:
                    raise Error("proto json: duplicate field printUnknownFields")
                seen_9 = True
                if reader.read_null():
                    self.print_unknown_fields = False
                else:
                    self.print_unknown_fields = reader.bool_value()
            else:
                reader.skip_unknown_value()


struct ConformanceResponse(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `ConformanceResponse` protobuf message."""

    var parse_error: String
    """Field `parse_error` (number 1)."""
    var serialize_error: String
    """Field `serialize_error` (number 6)."""
    var timeout_error: String
    """Field `timeout_error` (number 9)."""
    var runtime_error: String
    """Field `runtime_error` (number 2)."""
    var protobuf_payload: List[Byte]
    """Field `protobuf_payload` (number 3)."""
    var json_payload: String
    """Field `json_payload` (number 4)."""
    var skipped: String
    """Field `skipped` (number 5)."""
    var jspb_payload: String
    """Field `jspb_payload` (number 7)."""
    var text_payload: String
    """Field `text_payload` (number 8)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""
    var result_case: Int
    """Active member of oneof `result`: 0 when unset, else the set field number."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.parse_error = String()
        self.serialize_error = String()
        self.timeout_error = String()
        self.runtime_error = String()
        self.protobuf_payload = List[Byte]()
        self.json_payload = String()
        self.skipped = String()
        self.jspb_payload = String()
        self.text_payload = String()
        self._unknown = List[Byte]()
        self.result_case = 0

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.result_case == 1:
            writer.string_field(1, self.parse_error)
        elif self.result_case == 6:
            writer.string_field(6, self.serialize_error)
        elif self.result_case == 9:
            writer.string_field(9, self.timeout_error)
        elif self.result_case == 2:
            writer.string_field(2, self.runtime_error)
        elif self.result_case == 3:
            writer.bytes_field(3, Span(self.protobuf_payload))
        elif self.result_case == 4:
            writer.string_field(4, self.json_payload)
        elif self.result_case == 5:
            writer.string_field(5, self.skipped)
        elif self.result_case == 7:
            writer.string_field(7, self.jspb_payload)
        elif self.result_case == 8:
            writer.string_field(8, self.text_payload)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.parse_error = reader.string_value()
                    self.result_case = 1
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 2:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.runtime_error = reader.string_value()
                    self.result_case = 2
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 3:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.protobuf_payload = reader.bytes_value()
                    self.result_case = 3
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 4:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.json_payload = reader.string_value()
                    self.result_case = 4
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 5:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.skipped = reader.string_value()
                    self.result_case = 5
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 6:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.serialize_error = reader.string_value()
                    self.result_case = 6
                    self.parse_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            elif field == 7:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.jspb_payload = reader.string_value()
                    self.result_case = 7
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.text_payload = String()
            elif field == 8:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.text_payload = reader.string_value()
                    self.result_case = 8
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
            elif field == 9:
                if wire_type != WIRE_LEN:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.timeout_error = reader.string_value()
                    self.result_case = 9
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.result_case == 1:
            writer.field("parseError", "parse_error")
            writer.string_value(self.parse_error)
        if self.result_case == 6:
            writer.field("serializeError", "serialize_error")
            writer.string_value(self.serialize_error)
        if self.result_case == 9:
            writer.field("timeoutError", "timeout_error")
            writer.string_value(self.timeout_error)
        if self.result_case == 2:
            writer.field("runtimeError", "runtime_error")
            writer.string_value(self.runtime_error)
        if self.result_case == 3:
            writer.field("protobufPayload", "protobuf_payload")
            writer.bytes_value(Span(self.protobuf_payload))
        if self.result_case == 4:
            writer.field("jsonPayload", "json_payload")
            writer.string_value(self.json_payload)
        if self.result_case == 5:
            writer.field("skipped", "skipped")
            writer.string_value(self.skipped)
        if self.result_case == 7:
            writer.field("jspbPayload", "jspb_payload")
            writer.string_value(self.jspb_payload)
        if self.result_case == 8:
            writer.field("textPayload", "text_payload")
            writer.string_value(self.text_payload)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        var seen_6 = False
        var seen_9 = False
        var seen_2 = False
        var seen_3 = False
        var seen_4 = False
        var seen_5 = False
        var seen_7 = False
        var seen_8 = False
        var seen_oneof_0 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "parseError" or field_name == "parse_error":
                if seen_1:
                    raise Error("proto json: duplicate field parseError")
                seen_1 = True
                if reader.read_null():
                    if self.result_case == 1:
                        self.parse_error = String()
                        self.result_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.parse_error = reader.string_value()
                    if self.result_case != 0 and self.result_case != 1:
                        raise Error("proto json: multiple oneof fields")
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
                    self.result_case = 1
            elif field_name == "serializeError" or field_name == "serialize_error":
                if seen_6:
                    raise Error("proto json: duplicate field serializeError")
                seen_6 = True
                if reader.read_null():
                    if self.result_case == 6:
                        self.serialize_error = String()
                        self.result_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.serialize_error = reader.string_value()
                    if self.result_case != 0 and self.result_case != 6:
                        raise Error("proto json: multiple oneof fields")
                    self.parse_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
                    self.result_case = 6
            elif field_name == "timeoutError" or field_name == "timeout_error":
                if seen_9:
                    raise Error("proto json: duplicate field timeoutError")
                seen_9 = True
                if reader.read_null():
                    if self.result_case == 9:
                        self.timeout_error = String()
                        self.result_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.timeout_error = reader.string_value()
                    if self.result_case != 0 and self.result_case != 9:
                        raise Error("proto json: multiple oneof fields")
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
                    self.result_case = 9
            elif field_name == "runtimeError" or field_name == "runtime_error":
                if seen_2:
                    raise Error("proto json: duplicate field runtimeError")
                seen_2 = True
                if reader.read_null():
                    if self.result_case == 2:
                        self.runtime_error = String()
                        self.result_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.runtime_error = reader.string_value()
                    if self.result_case != 0 and self.result_case != 2:
                        raise Error("proto json: multiple oneof fields")
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
                    self.result_case = 2
            elif field_name == "protobufPayload" or field_name == "protobuf_payload":
                if seen_3:
                    raise Error("proto json: duplicate field protobufPayload")
                seen_3 = True
                if reader.read_null():
                    if self.result_case == 3:
                        self.protobuf_payload = List[Byte]()
                        self.result_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.protobuf_payload = reader.bytes_value()
                    if self.result_case != 0 and self.result_case != 3:
                        raise Error("proto json: multiple oneof fields")
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
                    self.result_case = 3
            elif field_name == "jsonPayload" or field_name == "json_payload":
                if seen_4:
                    raise Error("proto json: duplicate field jsonPayload")
                seen_4 = True
                if reader.read_null():
                    if self.result_case == 4:
                        self.json_payload = String()
                        self.result_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.json_payload = reader.string_value()
                    if self.result_case != 0 and self.result_case != 4:
                        raise Error("proto json: multiple oneof fields")
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
                    self.result_case = 4
            elif field_name == "skipped" or field_name == "skipped":
                if seen_5:
                    raise Error("proto json: duplicate field skipped")
                seen_5 = True
                if reader.read_null():
                    if self.result_case == 5:
                        self.skipped = String()
                        self.result_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.skipped = reader.string_value()
                    if self.result_case != 0 and self.result_case != 5:
                        raise Error("proto json: multiple oneof fields")
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.jspb_payload = String()
                    self.text_payload = String()
                    self.result_case = 5
            elif field_name == "jspbPayload" or field_name == "jspb_payload":
                if seen_7:
                    raise Error("proto json: duplicate field jspbPayload")
                seen_7 = True
                if reader.read_null():
                    if self.result_case == 7:
                        self.jspb_payload = String()
                        self.result_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.jspb_payload = reader.string_value()
                    if self.result_case != 0 and self.result_case != 7:
                        raise Error("proto json: multiple oneof fields")
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.text_payload = String()
                    self.result_case = 7
            elif field_name == "textPayload" or field_name == "text_payload":
                if seen_8:
                    raise Error("proto json: duplicate field textPayload")
                seen_8 = True
                if reader.read_null():
                    if self.result_case == 8:
                        self.text_payload = String()
                        self.result_case = 0
                else:
                    if seen_oneof_0:
                        raise Error("proto json: multiple oneof fields")
                    seen_oneof_0 = True
                    self.text_payload = reader.string_value()
                    if self.result_case != 0 and self.result_case != 8:
                        raise Error("proto json: multiple oneof fields")
                    self.parse_error = String()
                    self.serialize_error = String()
                    self.timeout_error = String()
                    self.runtime_error = String()
                    self.protobuf_payload = List[Byte]()
                    self.json_payload = String()
                    self.skipped = String()
                    self.jspb_payload = String()
                    self.result_case = 8
            else:
                reader.skip_unknown_value()


struct JspbEncodingConfig(Copyable, Defaultable, Movable, ProtoMessage, ProtoJsonMessage):
    """Generated from the `JspbEncodingConfig` protobuf message."""

    var use_jspb_array_any_format: Bool
    """Field `use_jspb_array_any_format` (number 1)."""
    var _unknown: List[Byte]
    """Preserved unknown fields, re-emitted on encode."""

    def __init__(out self):
        """Initializes all fields to their proto3 defaults."""
        self.use_jspb_array_any_format = False
        self._unknown = List[Byte]()

    def encode_to(self, mut writer: WireWriter):
        """Appends the wire-format bytes to the writer.

        Fields set to their proto3 default are omitted; preserved
        unknown fields are re-emitted at the end.

        Args:
            writer: Destination wire-format writer.
        """
        if self.use_jspb_array_any_format:
            writer.bool_field(1, self.use_jspb_array_any_format)
        writer.buf.extend(Span(self._unknown))

    def merge_from(mut self, mut reader: WireReader) raises:
        """Merges fields decoded from the reader into this message.

        Later singular values overwrite earlier ones, repeated fields
        append, submessages merge, and unknown fields are preserved.

        Args:
            reader: Source wire-format reader.

        Raises:
            Error: If the input is not valid protobuf wire data.
        """
        while not reader.done():
            var tag = reader.read_tag()
            var field = tag[0]
            var wire_type = tag[1]
            if field == 1:
                if wire_type != WIRE_VARINT:
                    reader.capture_field(field, wire_type, self._unknown)
                else:
                    self.use_jspb_array_any_format = reader.bool_value()
            else:
                reader.capture_field(field, wire_type, self._unknown)

    def encode_json_to(
        self, mut writer: ProtoJsonWriter
    ) raises:
        """Writes this message using the proto3 JSON mapping.

        Args:
            writer: Destination JSON writer.

        Raises:
            Error: If a field cannot be written as valid JSON.
        """
        writer.begin_object()
        if self.use_jspb_array_any_format or writer.options.always_print_fields_with_no_presence:
            writer.field("useJspbArrayAnyFormat", "use_jspb_array_any_format")
            writer.bool_value(self.use_jspb_array_any_format)
        writer.end_object()

    def merge_json_from(
        mut self, mut reader: ProtoJsonReader
    ) raises:
        """Merges fields from one proto3 JSON object.

        Args:
            reader: Source JSON reader.

        Raises:
            Error: If the input is not valid proto3 JSON.
        """
        var seen_1 = False
        reader.begin_object()
        while True:
            var next_field = reader.next_field()
            if not next_field:
                break
            var field_name = next_field.value()
            if field_name == "useJspbArrayAnyFormat" or field_name == "use_jspb_array_any_format":
                if seen_1:
                    raise Error("proto json: duplicate field useJspbArrayAnyFormat")
                seen_1 = True
                if reader.read_null():
                    self.use_jspb_array_any_format = False
                else:
                    self.use_jspb_array_any_format = reader.bool_value()
            else:
                reader.skip_unknown_value()
